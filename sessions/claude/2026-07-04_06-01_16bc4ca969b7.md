# Claude session `16bc4ca9-69b7-413f-9b23-6d1172acb90d`

- **Agent:** claude
- **Model(s):** claude-fable-5
- **Started:** 2026-07-04 06:01:20Z  |  **Last activity:** 2026-07-04 06:20:52Z
- **Turns:** 10 human / 23 assistant  |  **Tool calls:** 28
- **Source:** `/home/<user>/.claude/projects/-var-data-bootstrap/16bc4ca9-69b7-413f-9b23-6d1172acb90d.jsonl`

## Token cost

- output: **24,919**  |  fresh input: 37,995  |  cache write: 110,063  |  cache read: 2,265,947
- **cost: **$5.27**** (priced as `anthropic/claude-fable-5` — $10/$50 in/out per Mtok, $1 cache-read, $12.5 cache-write)

## Timing

- wall-clock: **19m31s**  ·  active (model+tool): **6m51s**  ·  waiting for user: 12m40s
- model generation: 6m34s  ·  tool execution: 16.9s
- tool time by name: `ScheduleWakeup` 6.9s/2, `Agent` 5.7s/2, `Bash` 3.9s/8, `Edit` 398ms/9, `Read` 205ms/6, `Write` 58ms/1
- slowest calls: `ScheduleWakeup` 5.5s, `Agent` 4.1s, `Bash` 2.0s

## Summary

**First request:**

> read the docs from @docs/README.md @docs/RESUME-PROGSIM.md assess the proof complexity and redundancy in existing lean code. assess what needs to be done to reduce the proof complexity between the spec and the program. what would be the next rung?

**Final response:**

> Done. The docs now carry the re-assessed plan, committed as `553a490`:
> 
> **New: `docs/PROOF-COMPLEXITY.md`** (indexed under Analyses in `docs/README.md`) — the measured assessment: where the 27k lines go (flat-PC = 48% for two programs), the four findings (F1 flat-PC scales super-linearly and is retired by `compile_sim`; F2 the hex0 decoder argument is proved 3× heading for 4× — hence one-proof-per-program policy; F3 the three IL factoring leaks including Ctrl's missing frame theorem; F4 `lower_sim`'s superseded tail at `StmtSim.lean:1003`), and the §3 priority ladder: close `compile_sim` → Pro
> 
> … [truncated 830 chars]

---

## Transcript

### 👤 **User**  ·  06:01:20

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### 👤 **User**  ·  06:01:20

<command-name>/clear</command-name>
            <command-message>clear</command-message>
            <command-args></command-args>

### 👤 **User**  ·  06:01:31; waited 10.6s

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### 👤 **User**  ·  06:01:31

<command-name>/model</command-name>
            <command-message>model</command-message>
            <command-args></command-args>

### 👤 **User**  ·  06:01:31

<local-command-stdout>Set model to [1mFable 5[22m and saved as your default for new sessions</local-command-stdout>

### 👤 **User**  ·  06:03:43; waited 2m12s

read the docs from @docs/README.md @docs/RESUME-PROGSIM.md assess the proof complexity and redundancy in existing lean code. assess what needs to be done to reduce the proof complexity between the spec and the program. what would be the next rung?

### 🤖 **Assistant**  ·  06:03:52; model 10.1s

> 💭 (thinking)
> 

I'll start by mapping the Lean corpus — layout doc, file sizes, and sorry/axiom status — then dig into the redundancy question.

**🔧 tool → Read**  ·  5ms
```json
{"file_path": "/var/data/bootstrap/docs/LEAN-LAYOUT.md"}
```

### 🛠️ **Tool result**  ·  06:03:53; tool 5ms

````
1	# Lean corpus layout — the language tower
2	
3	Map of `lean/`, organized as a **tower of languages** over a shared spec. Read
4	top-down: each rung is a language, and each program is proved once per rung it
5	reaches. One top-level directory per rung, one Lake lib per directory (see
6	`lean/lakefile.toml`); the disjoint top-level namespaces make every module
7	belong to exactly one lib.
8	
9	```
10	lean/
11	  Spec/     functional specifications — the refinement targets (rung-independent)
12	  RawAsm/   bare RV64I model + hex0/hex1 verified at the flat-PC altitude
13	  LowIR/    structured IL (Core → Ctrl → Prog) + verified compiler + proofs
14	  LowSSA/   the SSA variant of the Prog IR (experiment)
15	  {Spec,RawAsm,LowIR,LowSSA}.lean   rung aggregators (Lake roots)
16	  DumpProgMain.lean                 tool: emits bare/progmain.* (not a lib module)
17	```
18	
19	Build a whole rung with `lake build <Spec|RawAsm|LowIR|LowSSA>`, or one module
20	with `lake build <Module.Name>`. `defaultTargets` is the four rung libs; the
21	pre-reorg names (`Hex0`, `Hex1`, `LowIRCompile`, `LowIRProgSim`, `LowIRSSA`)
22	survive as back-compat lib aliases outside `defaultTargets`.
23	
24	## The rungs
25	
26	### `Spec/` — the specifications every rung refines against
27	- [`Spec/Hex0/Spec.lean`](../lean/Spec/Hex0/Spec.lean) — `Hex0.decode`/`coreSpec` (mirrors `coq/Spec.v`); [`Grammar.lean`](../lean/Spec/Hex0/Grammar.lean) — concrete-syntax grammar + error taxonomy.
28	- [`Spec/Hex1/Spec.lean`](../lean/Spec/Hex1/Spec.lean) — `Hex1.coreSpec1`; [`Grammar.lean`](../lean/Spec/Hex1/Grammar.lean).
29	- No `Spec/Strlen/`: strlen's spec is the one-liner `firstNulAt` in [`LowIR/Core.lean`](../lean/LowIR/Core.lean), and strlen has no grammar (not a parser).
30	
31	### `RawAsm/` — bare RV64I, flat-PC proofs
32	- [`Rv64i.lean`](../lean/RawAsm/Rv64i.lean) — the 16-instruction RV64I machine model (the whole trusted ISA surface; shared by every rung above).
33	- `Hex0/` — [`Refine.lean`](../lean/RawAsm/Hex0/Refine.lean) (the ~3400-line PC-simulation proof), `Certify`, `Harness`, `Image` (auto-gen from the ELF), `Validate` (executable diff-test).
34	- `Hex1/` — same shape plus `RefineBase` and `DecodeFacts` (kernel-checked per-instruction decode facts, auto-gen).
35	
36	### `LowIR/` — the structured IL, its compiler, and its programs
37	The IL is three stacked layers, each additive over the last:
38	- [`Core.lean`](../lean/LowIR/Core.lean) — base three-address IL (1:1 with RV64I ops) + clocked big-step semantics.
39	- [`Ctrl.lean`](../lean/LowIR/Ctrl.lean) — structured non-local control: `block`/`while`/`brkB`/`contL`/`ret` threading an `Outcome`.
40	- [`Prog.lean`](../lean/LowIR/Prog.lean) — the D7/D8 IR: activation-local calls, per-function stack frames, `ld`/`sd`.
41	
42	Backend + library:
43	- [`Compile.lean`](../lean/LowIR/Compile.lean) — verified-target compiler `Prog → RV64I` (memory-locals, -O0); [`CompileTests.lean`](../lean/LowIR/CompileTests.lean) — differential tests (`Prog.exec` vs compiled bytes on `Rv64i.step`); [`Dump.lean`](../lean/LowIR/Dump.lean) — s-expression/asm renderer.
44	- [`Lib.lean`](../lean/LowIR/Lib.lean) — **the combined function library**: `strlen`, `strtoull`, `hex0`, `hex1` as real `FunDef`s (params/rets/frames) plus a `main` driver that stages each input in its own frame and calls all four. Every function is spec-validated at the IL level.
45	- [`CtrlFacts.lean`](../lean/LowIR/CtrlFacts.lean) — the shared **Ctrl proof foundation**: generic exec/outcome lemmas (`exec_block_*`/`while_*`/`ret`/`brk`/`cont`/`call`/`mono`). Reused by every Ctrl-level proof (the LowIR analog of [`LowSSA/ExecFacts.lean`](../lean/LowSSA/ExecFacts.lean)).
46	
47	Programs (one subdir each — see the naming rule below):
48	- `Hex0/` — `Ctrl`+`CtrlProof` (hex0 on the Ctrl IL) and `Prog`+`ProgProof` (hex0 on the Prog IR).
49	- `Strlen/` — `Ctrl` (the Ctrl-IL version) and `CoreProof` (correctness of the base-IL strlen from `Core.lean`).
50	- `Strtoull/` — `Wr

… [truncated 2454 chars]
````

### 🤖 **Assistant**  ·  06:03:54; model 1.1s

**🔧 tool → Bash**  ·  86ms
```json
{"command": "find /var/data/bootstrap/lean -name '*.lean' | xargs wc -l | sort -rn | head -50", "description": "List Lean files by line count"}
```

### 🛠️ **Tool result**  ·  06:03:54; tool 86ms

```
  26756 total
   8700 /var/data/bootstrap/lean/RawAsm/Hex1/Refine.lean
   3377 /var/data/bootstrap/lean/RawAsm/Hex0/Refine.lean
   1703 /var/data/bootstrap/lean/LowIR/Hex0/CtrlProof.lean
   1519 /var/data/bootstrap/lean/LowSSA/Hex0/Proof.lean
   1098 /var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean
    736 /var/data/bootstrap/lean/LowSSA/ExecFacts.lean
    648 /var/data/bootstrap/lean/Spec/Hex1/Grammar.lean
    605 /var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean
    594 /var/data/bootstrap/lean/LowIR/Prog.lean
    539 /var/data/bootstrap/lean/LowSSA/Core.lean
    503 /var/data/bootstrap/lean/LowIR/ProgSim/Defs.lean
    489 /var/data/bootstrap/lean/RawAsm/Hex1/RefineBase.lean
    419 /var/data/bootstrap/lean/LowIR/Lib.lean
    395 /var/data/bootstrap/lean/LowIR/Core.lean
    375 /var/data/bootstrap/lean/RawAsm/Hex1/DecodeFacts.lean
    343 /var/data/bootstrap/lean/Spec/Hex0/Grammar.lean
    333 /var/data/bootstrap/lean/LowIR/Compile.lean
    309 /var/data/bootstrap/lean/LowIR/CompileTests.lean
    292 /var/data/bootstrap/lean/LowIR/CtrlFacts.lean
    258 /var/data/bootstrap/lean/LowIR/ProgSim/MemFacts.lean
    246 /var/data/bootstrap/lean/LowIR/ProgSim/SlotFacts.lean
    216 /var/data/bootstrap/lean/LowIR/ProgSim/ExecFacts.lean
    215 /var/data/bootstrap/lean/LowIR/Strtoull/WrappingProof.lean
    214 /var/data/bootstrap/lean/LowSSA/Strlen/Proof.lean
    210 /var/data/bootstrap/lean/RawAsm/Rv64i.lean
    198 /var/data/bootstrap/lean/LowIR/Strlen/CoreProof.lean
    189 /var/data/bootstrap/lean/LowSSA/Lib.lean
    166 /var/data/bootstrap/lean/LowIR/Hex0/Prog.lean
    163 /var/data/bootstrap/lean/Spec/Hex1/Spec.lean
    145 /var/data/bootstrap/lean/LowIR/ProgSim/WordMem.lean
    133 /var/data/bootstrap/lean/LowIR/Ctrl.lean
    117 /var/data/bootstrap/lean/LowIR/Dump.lean
    116 /var/data/bootstrap/lean/LowIR/Hex0/Ctrl.lean
    105 /var/data/bootstrap/lean/LowIR/Strtoull/Conformant.lean
    103 /var/data/bootstrap/lean/RawAsm/Hex0/Validate.lean
    100 /var/data/bootstrap/lean/LowIR/Strlen/Ctrl.lean
     95 /var/data/bootstrap/lean/Spec/Hex0/Spec.lean
     93 /var/data/bootstrap/lean/LowIR/Strtoull/ConformantProof.lean
     85 /var/data/bootstrap/lean/LowIR/Strtoull/Wrapping.lean
     81 /var/data/bootstrap/lean/LowIR/Hex0/ProgProof.lean
     80 /var/data/bootstrap/lean/RawAsm/Hex1/Certify.lean
     79 /var/data/bootstrap/lean/LowSSA/Dump.lean
     77 /var/data/bootstrap/lean/LowIR/Examples/Call.lean
     51 /var/data/bootstrap/lean/DumpProgMain.lean
     49 /var/data/bootstrap/lean/RawAsm/Hex1/Validate.lean
     48 /var/data/bootstrap/lean/RawAsm/Hex0/Certify.lean
     45 /var/data/bootstrap/lean/RawAsm/Hex0/Harness.lean
     40 /var/data/bootstrap/lean/RawAsm/Hex1/Harness.lean
     11 /var/data/bootstrap/lean/LowIR.lean
```

### 🤖 **Assistant**  ·  06:03:55; model 693ms

**🔧 tool → Bash**  ·  73ms
```json
{"command": "grep -rn 'sorry' /var/data/bootstrap/lean --include='*.lean' | grep -v '^\\s*--' | grep -v 'sorry-free' | head -40", "description": "Find sorry occurrences in Lean code"}
```

### 🛠️ **Tool result**  ·  06:03:55; tool 73ms

```
/var/data/bootstrap/lean/LowIR/Core.lean:318:    deferred (`sorry`) — what matters first is that the *design* admits the theorem.
/var/data/bootstrap/lean/LowIR/Core.lean:381:  sorry
/var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean:603:    all_goals sorry
/var/data/bootstrap/lean/LowIR/Hex0/ProgProof.lean:4:  (`sorry`, sanctioned like `compile_sim`). The executable certification
/var/data/bootstrap/lean/LowIR/Hex0/ProgProof.lean:79:  sorry
/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean:6:  `sorry`'d (Phase 4.3). It is the go/no-go for the `StInv`-based relation design — if
/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean:58:    their `lower_sim` cases are `sorry` (they use the real `Emitted`, not `emit`). -/
/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean:563:    non-memory and (still-`sorry`) control-flow cases. `lbu`/`sb` touch one byte,
/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean:670:    label target) and the `cref/clen` const-data cases are the `sorry`'d remainder
/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean:1003:    all_goals sorry
/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean:1054:  -- so this corollary is genuinely `sorry`-free — the real go/no-go).
/var/data/bootstrap/lean/LowIR/ProgSim/Defs.lean:6:  top-level theorems. Everything here is a real, `sorry`-free DEFINITION plus a
/var/data/bootstrap/lean/LowIR/ProgSim/Defs.lean:7:  handful of `sorry`'d equivalence theorems whose statements are the deliverable
/var/data/bootstrap/lean/LowIR/ProgSim/Defs.lean:432:  sorry
/var/data/bootstrap/lean/RawAsm/Hex0/Refine.lean:8:  STATUS: COMPLETE. `core_refines` is fully proved, `sorry`-free. The per-step
```

### 🤖 **Assistant**  ·  06:04:19; model 30.8s

> 💭 (thinking)
> 

The docs give me the map. Now I'll fan out read-only agents to assess redundancy in the big proof files while I read the measured-comparison docs myself.

**🔧 tool → Agent**  ·  1.6s
```json
{"description": "Assess RawAsm proof redundancy", "subagent_type": "Explore", "prompt": "In /var/data/bootstrap/lean, analyze proof complexity and redundancy between the two flat-PC refinement proofs: RawAsm/Hex0/Refine.lean (~3400 lines) and RawAsm/Hex1/Refine.lean (~8700 lines), plus RawAsm/Hex1/RefineBase.lean and DecodeFacts.lean. Be thorough. Report:\n1. The structural anatomy of each proof: what fraction of lines goes to (a) fetch/decode facts, (b) per-instruction step lemmas, (c) frame/memory-preservation plumbing, (d) the loop-simulation induction, (e) program-specific spec reasoning.\n2. Concretely which lemma families are duplicated (or near-duplicated) between hex0 and hex1's proofs — name lemma names and approximate line ranges. Quantify: how many lines of hex1's Refine.lean are re-derivations of machinery hex0 already paid for?\n3. Which of that duplicated machinery is generic (program-independent: fetch, decode, step, code-region preservation, PC arithmetic) vs genuinely program-specific.\n4. Why hex1 is 2.5x hex0's size — what scaled?\nReturn a structured report with lemma names, line counts, and percentages. Do not dump file contents; give conclusions with evidence pointers (file:line)."}
```

### 🛠️ **Tool result**  ·  06:04:27; tool 1.6s

```
Async agent launched successfully. (This tool result is internal metadata — never quote or paste any part of it, including the agentId below, into a user-facing reply.)
agentId: a6d2a4658e0523ade (internal ID - do not mention to user. Use SendMessage with to: 'a6d2a4658e0523ade', summary: '<5-10 word recap>' to continue this agent.)
The agent is working in the background. You will be notified automatically when it completes.
Do not duplicate this agent's work — avoid working with the same files or topics it is using.
output_file: /tmp/claude-1000/-var-data-bootstrap/35ad0295-329c-4cf2-910b-11585f90ee90/tasks/a6d2a4658e0523ade.output
Do NOT Read or tail this file via the shell tool — it is the full subagent JSONL transcript and reading it will overflow your context. If the user asks for progress, say the agent is still running; you'll get a completion notification.
```

### 🤖 **Assistant**  ·  06:04:37; model 9.3s

**🔧 tool → Agent**  ·  4.1s
```json
{"description": "Assess IL proof stack redundancy", "subagent_type": "Explore", "prompt": "In /var/data/bootstrap/lean, analyze proof complexity and redundancy across the structured-IL proof corpus. Be thorough. Files:\n- LowIR/CtrlFacts.lean (shared Ctrl foundation), LowIR/Hex0/CtrlProof.lean (~1700 lines), LowIR/Strlen/CoreProof.lean, LowIR/Strlen/Ctrl.lean, LowIR/Strtoull/WrappingProof.lean, LowIR/Strtoull/ConformantProof.lean, LowIR/Hex0/ProgProof.lean\n- LowSSA/ExecFacts.lean, LowSSA/Hex0/Proof.lean (~1500 lines), LowSSA/Strlen/Proof.lean\n- LowIR/ProgSim/*.lean (Defs, ExecFacts, WordMem, SlotFacts, MemFacts, StmtSim, CtrlSim) — the in-progress compile_sim campaign.\n\nReport:\n1. Per-program proof anatomy at the IL altitude: what fraction is generic exec/outcome plumbing vs actual spec reasoning (loop invariants, arithmetic)? How well do CtrlFacts.lean / LowSSA/ExecFacts.lean factor the plumbing out — what plumbing still gets re-proved inside each program proof (name lemma families, e.g. frame/Regs/Pres lemmas, mono lemmas, per-loop step lemmas)?\n2. Redundancy across rungs for the SAME program: hex0 has proofs at RawAsm (Refine), LowIR-Ctrl (CtrlProof), LowIR-Prog (ProgProof, sorry'd on compile_sim), LowSSA (Proof). How much genuine duplication is that — what does each rung's hex0 proof re-establish that another rung already proved? Which of these become redundant once ProgSim's compile_sim lands?\n3. The ProgSim campaign's current state: what's proved, what's sorry (c

… [truncated 414 chars]
```

**🔧 tool → Read**  ·  146ms
```json
{"file_path": "/var/data/bootstrap/docs/RESUME-SSA-HEX0.md"}
```

### 🛠️ **Tool result**  ·  06:04:37; tool 146ms

```
1	# RESUME — proving hex0 correct on LowIRSSA
2	
3	Plan written 2026-07-02, right after the strlen/hex0 ports landed. Read with
4	[LOWIR-SSA-EXPERIMENT.md](LOWIR-SSA-EXPERIMENT.md) (the IR's design record —
5	scopes, `defaultBody`, never/thru typing) and the baseline this campaign is
6	measured against: `lean/LowIR/Hex0/CtrlProof.lean` (1703 lines, sorry-free,
7	the Ctrl-IL hex0 proof).
8	
9	## STATUS (updated 2026-07-03) — ✅ CAMPAIGN COMPLETE, all 6 phases DONE
10	
11	`LowIR.SSA.hex0S_correct` is proved sorry-free; `#print axioms` =
12	`[propext, Classical.choice, Quot.sound]` — **no `native_decide`, no `sorry`**
13	in the chain (same axiom base as the Ctrl baseline). The whole SSA `hex0S`,
14	`run` on `memIn (asBytes inp)`, returns `Hex0.coreSpec`'s status and output
15	length as its two `.ret` values, with the output region holding `coreSpec`'s
16	bytes. Builds under `lake build LowIRSSA`. Both deliverables landed: (a) the
17	theorem, (b) the per-section comparison table (§6).
18	
19	- **Phase 2 DONE** — commits `f567a00`, `a1e3095`, Phase-2-tail. `pnibS_eff`,
20	  `exec_lit`, the `ceqS`/`weqS`/`geu_wwS` char-class dispatch lemmas, the
21	  `RegsS` register-context struct + `RegsS.of_agree`/`.frame` (the frame
22	  theorem in action — P2), `prefSt` read-char prefix + `hexPrefix_exec`.
23	- **Phase 3 DONE** — `skipCommentS_eq` (defeq to the inner `scWhile`),
24	  `skip_loopS` (induction on the `commentSkip` distance, exit via `cont 1`).
25	- **Phase 4 DONE** — `hexPathS_eff` (6-arm disjunction), `hex0DispatchS` +
26	  `body_lift`, `body_space`/`body_comment`/`body_hex_lift`.
27	- **Phase 5 DONE** — `main_loop` (strong induction on `|inp|−idx`, args-tuple
28	  invariant, `RegsS.rset56`/`.frame` carry the const/param regs; dispatch on
29	  char class; connected to `boundedRun`/`decodeS`).
30	- **Phase 6 DONE** — `hex0S_correct` (frameEnter + 11-lit prelude peel +
31	  `boundedRun_nil_coreSpec`).
32	
33	All landed code is sorry-free, builds under `lake build LowIRSSA`, and its
34	axioms track the Ctrl baseline `[propext, Classical.choice, Quot.sound]`
35	(no `native_decide` anywhere in the chain).
36	
37	- **Phase 0 DONE** — `lean/LowSSA/ExecFacts.lean` (~640 lines). All
38	  one-layer unfolders (leaf ops, `seq`, `catch0`, `block`, `ife`, the
39	  block-parameter `while` via `exec_while_unfold` + resolved
40	  `cont0`/`brk0`/`ret`/`contS`/`F_*` lemmas, `call`), `exec_mono`/`_le` (P4,
41	  guard-agnostic while handling), `exec_frame` + `exec_frame_rget` (P2 — the
42	  syntactic frame theorem, `catch0_frame` factors the break-scope analysis),
43	  `rget`/`rset`/`bindOuts`/`storeByte` helpers (P3 support; the `Slice`/`Wf`
44	  borrow layer is reused from `CtrlHex0Proof`). Commit `cb04884`.
45	- **Phase 1 DONE — GO** — `lean/LowSSA/Strlen/Proof.lean` (214 lines).
46	  `strlen_loop` (args-tuple invariant, induction on distance-to-NUL, `inits`
47	  parametrized by `inits.map (evalOpnd s) = [cur, mem cur]` so it survives the
48	  const-rebuilt back-edge; existential fuel + `exec_mono_le`) and the run-level
49	  `strlenS_correct` vs `IsLen`. **The GO/NO-GO passed**: the args-tuple
50	  invariant carries NO register-file clauses, exactly the experiment's claim.
51	  Commit `42de9b0`.
52	- **Phase 2 UNDERWAY** — `lean/LowSSA/Hex0/Proof.lean`. Landed:
53	  `pnibS_eff` (the value-producing nibble `ife` → `pnibR c` in `dst`, all 5
54	  leaves threaded through the nested `catch0` scopes; frame NOT baked in — it
55	  comes from `exec_frame`), `signExtend_ofNat_small` + `exec_lit`. Commit
56	  `f567a00`.
57	
58	### Frontier — proof done; §8 loop-arg redesign DONE (2026-07-03)
59	
60	All phases (0–6) landed and sorry-free. The proof lives in
61	`lean/LowIR/SSAProof/{ExecFacts,StrlenProof,Hex0Proof}.lean`; the headline
62	theorem is `LowIR.SSA.hex0S_correct`. See §6 for the size comparison.
63	
64	**The §8 loop-arg redesign is implemented** (commit `ef17bbd`): the `while`
65	back-edge now *rebinds in the environment* (`iterWhile`) instead of reb

… [truncated 23141 chars]
```

### 🛠️ **Tool result**  ·  06:04:41; tool 3.9s

```
Async agent launched successfully. (This tool result is internal metadata — never quote or paste any part of it, including the agentId below, into a user-facing reply.)
agentId: a15c56281848e138c (internal ID - do not mention to user. Use SendMessage with to: 'a15c56281848e138c', summary: '<5-10 word recap>' to continue this agent.)
The agent is working in the background. You will be notified automatically when it completes.
Do not duplicate this agent's work — avoid working with the same files or topics it is using.
output_file: /tmp/claude-1000/-var-data-bootstrap/35ad0295-329c-4cf2-910b-11585f90ee90/tasks/a15c56281848e138c.output
Do NOT Read or tail this file via the shell tool — it is the full subagent JSONL transcript and reading it will overflow your context. If the user asks for progress, say the agent is still running; you'll get a completion notification.
```

### 🤖 **Assistant**  ·  06:04:47; model 6.5s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Read**  ·  10ms
```json
{"file_path": "/var/data/bootstrap/docs/LOWIR-SSA-EXPERIMENT.md"}
```

### 🛠️ **Tool result**  ·  06:04:47; tool 10ms

```
1	# LowIRSSA — SSA/value-flavored variant of the Prog IR (experiment)
2	
3	Status: **design experiment**, user-directed, 2026-07-02. Code:
4	`lean/LowSSA/Core.lean` (lib `LowIRSSA`, in `defaultTargets`; executable `#guard`
5	battery, no proofs). Companions: [LOWIR-DESIGN.md](LOWIR-DESIGN.md) (D7/D8 and
6	the explicit not-SSA decision this experiment probes),
7	[RESUME-PROGSIM.md](RESUME-PROGSIM.md) (the campaign this must not disturb).
8	
9	## What it is
10	
11	`LowIR.SSA` is `LowIR.Prog` (D7 calls + D8 frames) re-shaped so that control
12	constructs *produce values* and registers are single-assignment:
13	
14	| Piece | Prog (D7/D8) | SSA experiment |
15	|---|---|---|
16	| registers | mutable locals | defined once per function (params, frameReg, op dests, `outs`, loop `args`); enforced by a decidable checker |
17	| outcomes | `brk k / cont k / ret` bare | **carry `List Word`** — jumps transport values |
18	| function results | `rets : Vector Reg rvc` read at the boundary | **arity only** (`rvc`); `ret [operands]` carries the values; `run` returns them directly |
19	| `block` | pure break scope | `block (outs) body` — `brk 0 [vs]` binds `outs`; body must not fall through (except `outs = []`) |
20	| `ife` | transparent to brk indices | **its own break scope** (Wasm-style): arms deliver `outs` via `brk 0 [vs]`, or are `.never` (e.g. `ret`) — then `outs` need no initialization on that path |
21	| `while` | cond over mutable regs, body falls through and re-loops | **block-parameter loop**: `while (outs) (inits) (args) c a b body dflt` — `args` are the φ-nodes, bound to `inits` on entry and rebound by `cont 0 [vs]` ("iteration is a tail call"); exits deliver `outs` |
22	| operands | registers only (+ x0) | `Opnd = .reg r \| .const v` at value-flow sites (brk/cont/ret args, inits, call args) |
23	| statement type | — | `.never` (fall-through unreachable) vs `.thru`, computed by the checker |
24	
25	Omitted as orthogonal (would port verbatim from Prog): const data
26	(`cref`/`clen`/`Program.data`), the P1 `pad` oracle, `execT` footprints.
27	
28	Tooling: `lean/LowSSA/Dump.lean` (the `LowIR.Dump` sibling, reusing its
29	rendering helpers) pretty-prints SSA functions/envs as WAST-flavoured
30	s-expressions — `brk`/`ret` as multi-value `(br k v…)`/`(return v…)`,
31	`block`/`if` results as named `(outs $rN…)` binders, `while` with its
32	`(init …) (args …)` block-parameter plumbing and `(default …)` body. The
33	SSA IR sits noticeably closer to the Wasm text idiom than Prog does.
34	
35	## Decisions made where the sketch was ambiguous
36	
37	1. **`defaultBody` runs on EVERY guard-false, with the current `args` in
38	   scope** — not only when the loop is never entered. The user's first sketch
39	   had `defaultOut : List Opnd` (outer-scope values); that design *discards
40	   loop-carried values on a guard exit* — a sum loop exiting via `i > n` would
41	   return the outer default, not the accumulated sum, so every value-carrying
42	   loop would be forced onto explicit `brk` exits and the guard would be dead
43	   weight. The mid-flight switch to a `defaultBody` *statement*, given `args`
44	   in scope, fixes this: the guard exit runs `dflt`, which can
45	   `brk 0 [.reg acc]`. This is exactly MLIR `scf.while`'s before-region /
46	   Cranelift's block-param shape. The zero-trip case falls out: `dflt` then
47	   sees `args = inits`. (`sumTo`/`sumCap` in the battery exercise both.)
48	   `dflt` shares the loop's scopes, so it may even `cont 0` to restart.
49	2. **Loop bodies are `.never`-typed**: `body` and `dflt` must end in
50	   `cont`/`brk`/`ret`. There is no implicit fall-through-and-loop (it couldn't
51	   rebind `args` anyway). `ife`/`block` arms may fall through only when
52	   `outs = []` (the user's "`.value 0` doesn't need destructuring").
53	3. **Scope shape**: `ife` shifts brk indices (it catches `brk 0`), unlike
54	   Prog; `while` is now *both* a break scope (`brk 0` exits with `outs`) and a
55	   continue scope (`cont 0` re

… [truncated 8508 chars]
```

### 🤖 **Assistant**  ·  06:04:48; model 567ms

**🔧 tool → Read**  ·  18ms
```json
{"file_path": "/var/data/bootstrap/docs/DESIGN-THESES.md"}
```

### 🛠️ **Tool result**  ·  06:04:48; tool 18ms

```
1	# Design theses: toward a verified systems language
2	
3	Distilled 2026-07 from the [third-party design-review series](third-party/README.md)
4	(Radix, Verus, CompCert, CakeML+Pancake, bedrock2, rustc, Creusot, Prusti,
5	RefinedC, Frama-C, lean-mlir, seL4+l4v) and the discussion around it. Ten
6	theses about how verified programs, compilers, and languages should be
7	built, ending with the design target they point at. Each thesis cites the
8	evidence; several carry explicit refinements or counterpoints found during
9	the survey.
10	
11	## The theses
12	
13	### 1. Proof difficulty is a property of how the program was constructed
14	
15	We do not fight the halting problem or incompleteness; we construct
16	programs *together with* their proofs, so that program and proof share
17	structure. The undecidability folklore intrudes in exactly one place —
18	termination of spec-level functions — and the universal dodge is
19	fuel/clocks plus partial correctness, which this repo already uses.
20	
21	*Evidence:* every verified artifact in the survey is co-designed —
22	seL4's preemption points turn concurrency into bounded sequential
23	reasoning; bedrock2 bans recursion so stack bounds are a static
24	computation; Radix's `scope` makes inlining a rule swap; CakeML's clock
25	makes divergence a fuel limit instead of coinduction
26	([SEL4-DESIGN](third-party/SEL4-DESIGN.md),
27	[BEDROCK2-DESIGN](third-party/BEDROCK2-DESIGN.md),
28	[RADIX-DESIGN](third-party/RADIX-DESIGN.md),
29	[CAKEML-DESIGN](third-party/CAKEML-DESIGN.md)).
30	
31	*Refinements:* (a) "constructed together" is a spectrum — derive the
32	program from the proof (LiveVerif, CakeML's translator), co-annotate
33	(RefinedC), post-hoc (Alive2 over LLVM) — and post-hoc verification of an
34	artifact not designed for it is where proofs go to die: rustc's borrow
35	checker can only ever get a validator, never a proof
36	([RUSTC-BORROWCK-DESIGN](third-party/RUSTC-BORROWCK-DESIGN.md)).
37	(b) The thesis extends to lifecycle: proof *maintenance* dominates proof
38	construction (see thesis 10).
39	
40	### 2. Factor compilation into (verified engine) × (cheap per-instance obligations)
41	
42	Many small transformations beat few big ones — but only under a uniform
43	composition framework, and "small passes" is just one instance of the real
44	principle. lean-mlir demonstrates the dual factorization: *one* verified
45	mega-engine (peephole rewriting, `denote_rewritePeepholeAt`) plus hundreds
46	of tiny per-rule obligations discharged by automation.
47	
48	*Evidence:* CompCert's ~20 passes under `mkpass`/simulation-diagram
49	uniformity; CakeML's 12 ILs under one `state_rel`+clock recipe; bedrock2's
50	`phase_correct` records; lean-mlir's rule library
51	([COMPCERT-DESIGN](third-party/COMPCERT-DESIGN.md),
52	[CAKEML-DESIGN](third-party/CAKEML-DESIGN.md),
53	[LEAN-MLIR-DESIGN](third-party/LEAN-MLIR-DESIGN.md)).
54	
55	*Caveats:* each intermediate language is itself a spec someone must
56	maintain — N passes = N semantics = N liabilities; the framework is what
57	keeps that affordable. Corollary: once verified, re-running a pass is free
58	(composition), which is how industrial pipelines actually behave (LLVM
59	runs InstCombine ~8× per -O2). Calibration: LLVM has ~500 registered
60	passes and ~55k lines in InstCombine alone; verified compilers sit at
61	1–5% of the pass count and ~0.1% of the pattern count — parity is not the
62	goal; the factorization is.
63	
64	### 3. For heuristic algorithms, verify the output checker, not the implementation
65	
66	Translation validation — an untrusted implementation plus a small verified
67	per-run checker — is the cheapest proof there is, *when it applies*.
68	
69	*Evidence:* CompCert's register allocation (untrusted IRC + Rideau–Leroy
70	checker) and linearization ("any enumeration is fine if every reachable
71	node appears once"); bedrock2's regalloc checker; seL4's binary
72	verification (graph-refine); Alive2 as per-run validation for LLVM
73	([CO

… [truncated 9492 chars]
```

### 🤖 **Assistant**  ·  06:04:55; model 7.6s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  79ms
```json
{"command": "grep -n 'Ext\\.\\|^##\\|^###' /var/data/bootstrap/docs/LOWIR-DESIGN.md | head -60", "description": "Skim LOWIR-DESIGN.md section/extension headings"}
```

### 🛠️ **Tool result**  ·  06:04:55; tool 79ms

```
8:## Purpose
21:  outcome-threaded `exec`. Reuses `St`/`Cond`/`evalCond`. **Has no compiler yet** (see Ext. 6).
26:## 1. Decisions made
28:### D1. A structured IL, not direct bare-RISC-V proofs
41:### D2. State = unbounded register file + flat byte memory
60:### D3. Non-local control flow (`ret`/`block`/`brkB`/`contL`) in `Ctrl`
78:### D4. Clocked big-step semantics (explicit fuel)
91:### D5. Separation via a borrow discipline on flat memory (not block memory)
109:### D6. Single-threaded; `errno` is a plain global
117:### D7. Activation-local calls: `rets := call f args` over an env-threaded `exec` — not SSA
141:  higher-IR pointer types lower to it, LowIR proofs consume it (cf. Ext. 5). **Registers are function-local; memory stays shared** (the `Slice`/`Wf` borrow
169:### D8. Per-function frames: `frameSize` in `FunDef`, semantic `sp` in `St` — no alloca statement
177:  loads/stores (Ext. 1's `ld/sd` included) off the frame-base register, by plain address
189:    heap, caller buffers, and sibling frames — Ext. 4's goal, without block memory and without
200:- **Alternatives rejected.** (a) SP-as-register convention (Ext. 2's original sketch) — dead
212:## 2. Current non-features (deliberate gaps)
230:  (no borrow checker yet — see Ext. 5).
236:## 2b. Non-goals — and where each one lives instead
247:| N3 | Separation / aliasing **enforcement** | *Above* the IL: borrow-typed higher IRs / future borrow checker (Ext. 5) *produce* `Wf`/`Disjoint`; LowIR proofs only *consume* them as hypotheses | Never into the memory model (D5). The checker is a pure gate whose output is hypotheses — rustc's architecture. |
251:| N7 | Heap allocation, GC | No IR primitive. `malloc`/`free` are *libc functions verified on top of* the IL; freshness bookkeeping via the Ext. 4 `alloca`-style fresh-`Slice` primitive if manual disjointness dominates | Pancake's lesson: no GC anywhere in the pipeline. Allocator verification is a program proof, not a language feature. |
262:## 2c. Low-level boundary catalog — where machine magic is still needed
277:| B4 | **Context capture** — `setjmp`/`longjmp`, coroutines, OS context switch: reifies exactly what D7/D8 made fictional (activation-local registers, LIFO frames) | refuse at the IL. Legal-`longjmp` error unwinding and generators get **structured, correct-by-construction replacements** — Ext. 11. A scheduler's context switch, if ever, is an (a) stub with a save-exactly-these-registers spec | rustc coroutine transform; seL4 TCB switch | Ext. 11, later; variadics/stack-walking stay non-goals |
284:## 3. Likely extensions (roughly in order of when they'll be forced)
333:   Ext. 10's `set r (imm v)`, which also removes the 12-bit ceiling on literals).
358:    schedule together with Ext. 5.
408:**Priorities (agreed 2026-07):** Ext. 8 first (cheapest, pays in every proof *and* in pass 2);
410:decision — design the env once); then Ext. 1 (`ld`/`sd`, compiler-forced). Ext. 9 is the
411:"cheap now, expensive later" sleeper — do it alongside Ext. 8. Ext. 10 waits for the HIR.
412:Ext. 11 is explicitly *later* — the unwind-outcome half is cheap and may come early if error-path
417:## 4. Compiler-side architecture — decomposing `compile_sim` (CompCert/CakeML-informed)
451:(matching CakeML's `stackLang`), and that frame also holds the `alloca`-style address-taken locals (Ext. 1).
473:## 5. The architecture in one line
```

### 🤖 **Assistant**  ·  06:05:05; model 11.6s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  74ms
```json
{"command": "grep -c 'theorem\\|lemma' /var/data/bootstrap/lean/RawAsm/Hex0/Refine.lean /var/data/bootstrap/lean/RawAsm/Hex1/Refine.lean /var/data/bootstrap/lean/LowIR/Hex0/CtrlProof.lean /var/data/bootstrap/lean/LowSSA/Hex0/Proof.lean /var/data/bootstrap/lean/LowIR/CtrlFacts.lean 2>/dev/null", "description": "Count theorem/lemma density in major proof files"}
```

### 🛠️ **Tool result**  ·  06:05:07; tool 74ms

```
/var/data/bootstrap/lean/LowIR/CtrlFacts.lean:38
/var/data/bootstrap/lean/LowSSA/Hex0/Proof.lean:46
/var/data/bootstrap/lean/LowIR/Hex0/CtrlProof.lean:82
/var/data/bootstrap/lean/RawAsm/Hex0/Refine.lean:105
/var/data/bootstrap/lean/RawAsm/Hex1/Refine.lean:98
```

### 🤖 **Assistant**  ·  06:05:15; model 9.1s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → ScheduleWakeup**  ·  1.4s
```json
{"delaySeconds": 270, "reason": "Waiting for the two Explore agents analyzing proof redundancy to complete", "prompt": "Check whether the two Explore agents (RawAsm redundancy, IL-stack redundancy) have completed; if so, synthesize the proof-complexity/redundancy assessment and the next-rung recommendation the user asked for."}
```

### 🛠️ **Tool result**  ·  06:05:18; tool 1.4s

```
Next wakeup scheduled for 06:10:00 (in 282s). Nothing more to do this turn — the harness re-invokes you when the wakeup fires or a task-notification arrives.
```

### 👤 **User**  ·  06:08:53; waited 3m35s

<task-notification>
<task-id>a6d2a4658e0523ade</task-id>
<tool-use-id>toolu_01P6QPejtqYdsYguNJEkbvt2</tool-use-id>
<output-file>/tmp/claude-1000/-var-data-bootstrap/35ad0295-329c-4cf2-910b-11585f90ee90/tasks/a6d2a4658e0523ade.output</output-file>
<status>completed</status>
<summary>Agent "Assess RawAsm proof redundancy" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I have enough evidence. Here is the report.

---

# Proof-complexity &amp; redundancy analysis: Hex0 vs Hex1 flat-PC refinement proofs

## Files &amp; raw sizes

| File | Lines | Role |
|---|---|---|
| `RawAsm/Hex0/Refine.lean` | 3377 | Entire hex0 refinement (self-contained) |
| `RawAsm/Hex1/Refine.lean` | 8700 | hex1 refinement, main body |
| `RawAsm/Hex1/RefineBase.lean` | 489 | hex1 fetch/step/word-memory toolkit |
| `RawAsm/Hex1/DecodeFacts.lean` | 375 | 181 per-instruction decode lemmas |
| **hex1 proof total** | **9564** | (Refine + RefineBase + DecodeFacts) |

Program sizes (the root driver): hex0 `coreBytes` = **324 bytes = 81 instructions**; hex1 `coreBytes` = **724 bytes = 181 instructions** (`Image.lean:7` / `Image1.lean:8`). Code ratio **2.23x**; proof ratio **2.83x** (Refine-to-Refine) or **2.55x** (total-to-Refine, matching the "2.5x" framing).

Crucially, hex1 does **not** re-derive the generic arithmetic/spec toolkit: `RefineBase.lean:16` and `Refine.lean:16-20` `open Hex0.Refine (...)` and import 20+ lemmas directly (`addr_ofNat_succ`, `ult_ofNat`, `ofNat_ne`, `slt_ofNat`, `getD_drop`, `setWidth8_64`, `rset_rget`, `storeByte_mem`, `li_block_frame`, `runFuel_halt/one/add`, `nibble_*`, `skipComment_*`, `combine_nibbles`, `loadBytes_frame/get`). So the truly program-independent math is paid once, in hex0.

---

## 1. Structural anatomy

### hex0 `Refine.lean` (3377 lines, 89 theorems)

| Bucket | Lines (approx) | % | Evidence |
|---|---|---|---|
| (a) fetch/decode facts | ~40 | 1.2% | `fetch_code`+`wordAt`+`CodeLoaded`+`addr_ofNat_succ` `Refine.lean:45-77`; decode discharged inline via `by decide` at ~71 `step_*` call sites (no decode-table file) |
| (b) per-instruction step lemmas | ~85 | 2.5% | `step_li_t0` :34; `step_addi`…`step_jal` :103-175 (14 forms) |
| (c) frame/memory plumbing | ~70 | 2.1% | state-projection simps :79-99; `loadBytes_frame/get` :205-246; `li_block_frame` :793 |
| (d) loop-simulation induction | ~110 | 3.3% | `runFuel_*` :330-351; `loop_iteration` :3118; `eof_result` :3166; `loop_correct` :3185 |
| (e) program-specific block/token simulation + spec | **~2970** | **~88%** | the entire middle: `loop_prefix`:480, `spacing_tail*`:594/803, `high_parse`/`low_parse`:1497/1621, `store_epilogue`:1744, `halt_epilogue`:1894, `loop_byte`:2292, `comment_read`/`comment_loop`:2758/2848, all `loop_*` case theorems:2585-2755+2995, plus spec lemmas `decodeS_*`:442, `nibble_*`:1000, `coreSpec_props`:3326 |

### hex1 (9564 lines: Refine 8700 + RefineBase 489 + DecodeFacts 375; 92+30+182 theorems)

| Bucket | Lines (approx) | % | Evidence |
|---|---|---|---|
| (a) fetch/decode facts | ~405 | 4.2% | **entire `DecodeFacts.lean`** (181 `dec_N` + `coreBytes_len`); `fetch_code1`/`wordAt1`/`CodeLoaded1` `RefineBase.lean:22-49` |
| (b) per-instruction step lemmas | ~395 | 4.1% | `RefineBase.lean:52-148` (16 forms incl. new `sub`,`srli`,`ld`,`sd`); 6 `li_*` blocks `Refine.lean:22-320` |
| (c) frame/memory plumbing | ~505 | 5.3% | word-memory toolkit `RefineBase.lean:149-488` (`storeWord_frame`, `storeWord_get0..7`, `assemble_bytes`); region preservation `Refine.lean:464-529`; table access `slot_addr`/`loadWord_slot`/`storeWord_slot` :366-463 |
| (d) loop-simulation induction | ~445 | 4.6% | `init_loop`:774 + `init_iter`:568-773 (256-iter zeroing loop); `pass1_correct`:4837; `pass2_correct`:8194; `p1_eof`/`p2_eof`; `init_phase`:970 |
| (e) program-specific token simulation + spec | **~7800** | **~82%** | two full passes of per-token machinery (see §4) + label families + spec lemmas `offBytes_*`:6813, `scan1_pos_le`:2673, `decode1_m`:2736, `emit1_props`:8295, `coreSpec1_props`:8471 |

Both proofs are dominated (&gt;80%) by bucket (e): program-specific basic-block simulation that threads fetch+decode+step+frame together and matches it against the spec token-by-token. The generic buckets (a)-(d) are individually small.

---

## 2. Duplicated / near-duplicated lemma families (hex0 → hex1)

These are re-derived in hex1 (not shared) because they are parameterized over the concrete `CodeLoaded`/`wordAt`/`fetch` tied to a specific `Image`, so hex0's `Image.coreBytes` versions don't apply.

| Family | hex0 (lines) | hex1 (lines) | Note |
|---|---|---|---|
| `fetch_code` → `fetch_code1` | :63-77 (~15) | `RefineBase.lean:35-49` (~15) | identical modulo Image |
| 14/16 `step_*` lemmas | :103-175 (~73) | `RefineBase.lean:52-148` (~97) | line-for-line ports; hex1 adds `step_sub/srli/ld/sd` |
| 6 `li_beq/blt/bge_*` blocks | :699-790, :1081-1272 (~269) | `Refine.lean:22-320` (~298) | header comment states "ports of Hex0.Refine's, under CodeLoaded1" |
| `bgeu_eq_taken` | :1960-1975 (16) | `Refine.lean:1509-1528` (20) | port |
| `comment_read`+`comment_loop` | :2758-2990 (~228) | `comment_read1`/`loop1` :1529-1829 (~300) **and** `comment_read2`/`loop2` :5426-5732 (~306) | ported **twice** (pass1 + pass2) |
| `spacing_tail`(+`_nl`) | :594-695, :803-892 (~192) | `p1_spacing_tail` :1327-1450 **and** `p2_spacing_tail` :5236-5346 (~233) | ported per pass |
| `read_prefix` (input-read head) | :1384-1493 (~110) | `p1_low_read` :3562, `p2_read2` :6149 | re-expressed per pass |
| `WellFormed`/`code_initOn`/`in_initOn` | :250-282 (~33) | `WellFormed1`/`code_initOn1`/`in_initOn1` :326, :799-828 (~40) | port |
| `error_result`/`reach_error`/`short_result` | :2244-2286, :2569 | `error_result1` :2068, `short_result1` :2754 | port |

**Quantified re-derivation of hex0-established machinery:** roughly

- step lemmas + fetch_code1: ~112
- 6 `li_*` blocks: ~298
- `bgeu_eq_taken`: ~20
- comment loop ×2: ~606
- spacing tail ×2: ~233
- initOn/WellFormed/error/short ports: ~90

≈ **1350–1450 lines** of hex1's ~9564 (**~15%**) are re-derivations of machinery hex0 already proved — machinery that is generic in spirit but re-instantiated because it is bound to the concrete image predicate. A further ~600 of those lines are hex1 duplicating *itself* (comment + spacing lemmas exist once per pass).

---

## 3. Generic (program-independent) vs genuinely program-specific

**Generic — and correctly shared (paid once in hex0, imported by hex1):**
PC arithmetic (`addr_ofNat_succ`), branch-decision arithmetic (`ult_ofNat`, `ofNat_ne`, `slt_ofNat`), byte helpers (`getD_drop`, `setWidth8_64`), register/memory frame (`rset_rget`, `storeByte_mem`, `li_block_frame`), the fuel/loop-induction primitive (`runFuel_halt/one/add`), and the hex-nibble spec lemmas (`nibble_*`, `combine_nibbles`, `skipComment_*`), input loading (`loadBytes_frame/get`). See `RefineBase.lean:16`, `Refine.lean:16-20`. This is the ideal — hex1 does not re-pay for it.

**Generic-but-re-instantiated (the ~15% redundancy above):** `fetch_code`, the 16 `step_*` lemmas, the `li_*` branch blocks, `bgeu_eq_taken`, the comment scan loop, the spacing tail. These are program-independent in structure (they describe RV64I semantics + reusable instruction patterns) but are stated against a fixed `Image`, so they were copied. This is the harvestable duplication: abstracting `CodeLoaded`/`wordAt`/`fetch`/`step`/`li_*` over an arbitrary code image would let both proofs share them and would remove the second copy inside hex1 (pass1 vs pass2 comment/spacing).

**Genuinely program-specific (cannot be shared):**
- The 181 `dec_N` facts in `DecodeFacts.lean` (bytes differ).
- The label table: `encodeSlot`:342, `TableLoaded`:347, `slot_addr`/`loadWord_slot`/`storeWord_slot`:366-453, `storeWord_get0..7`+`assemble_bytes` (`RefineBase.lean:189-488`), 4-region preservation (`codeLoaded1_*`/`inputLoaded_*`/`tableLoaded_*`:472-529).
- Label-definition and label-reference token handling: `p1_labelDef`:2189, `p1_ref`:2881, `p2_labelDef`:6968, `p2_ref`:7233, `offBytes_*`:6813, `scan1_pos_le`:2673.
- Two-pass control flow: `init_iter`/`init_loop`, `pass1_correct`, `pass2_correct`.

---

## 4. Why hex1 is ~2.5x hex0 — what scaled

Four independent multipliers, in decreasing impact:

**(1) A second pass + init loop (structural doubling of the token engine).** hex0 is single-pass. hex1 runs an init loop (`init_iter`/`init_loop` :568-794, ~225 lines), a parse/label-building pass (`pass1_*`), and an emit pass (`pass2_*`). Every per-token family exists twice: `p1_spacing`/`p2_spacing`, `comment_loop1`/`comment_loop2`, `p1_byte`/`p2_byte`. This alone roughly doubles the per-token simulation cost.

**(2) Label machinery — entirely new, ~2500 lines with no hex0 analog.** Label references dominate: `p2_ref` `Refine.lean:7233-8152` is **~919 lines** — the single largest lemma in either file (relative-offset computation + `offBytes` patching). Add `p1_labelDef` (~470), `p1_ref` (~349), `p2_labelDef` (~150), the word-memory toolkit (`RefineBase.lean:149-488`, ~340), and the label-table access/preservation (~250). This ~2500-line block by itself exceeds the entire hex0-to-hex1 delta of ~5300 lines' worth of new work.

**(3) Program size 2.23x (81→181 instructions).** Externalized as the new 375-line `DecodeFacts.lean` (181 `dec_N`); `step_*` call sites 71→127; `dec_N` references in Refine = 402. Linear scaling of the fetch/step spine.

**(4) Richer memory model.** hex0 touches ~2 regions (code, output) via byte stores; hex1 adds a 64-bit-word label table and a distinct input region → 4 regions, each needing preservation across `setPc`/`rset`/`storeWord`/`storeByte` (`Refine.lean:472-529`) plus an 8-byte `storeWord_get0..7`/`assemble_bytes` toolkit absent from hex0.

**Bottom line:** the 2.5x is *not* mostly duplicated boilerplate. Re-instantiated generic machinery accounts for only ~15% (~1400 lines). The dominant cause is genuinely new program-specific verification — the second pass (2) and the label-resolution/word-memory subsystem (2,4) — roughly 2500-3000 lines that have no counterpart in hex0. The redundancy that *could* be eliminated (share `fetch`/`step`/`li_*`/comment/spacing across images and across the two passes) is real but is the minority of the growth.</result>
<usage><subagent_tokens>51917</subagent_tokens><tool_uses>9</tool_uses><duration_ms>263562</duration_ms></usage>
</task-notification>

### 🤖 **Assistant**  ·  06:09:00; model 8.7s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → ScheduleWakeup**  ·  5.5s
```json
{"delaySeconds": 270, "reason": "Waiting for the IL-stack redundancy agent to finish", "prompt": "Check whether the IL-stack redundancy Explore agent has completed; if so, synthesize the proof-complexity/redundancy assessment and next-rung recommendation the user asked for."}
```

### 🛠️ **Tool result**  ·  06:09:07; tool 5.5s

```
Next wakeup scheduled for 06:13:00 (in 233s). Nothing more to do this turn — the harness re-invokes you when the wakeup fires or a task-notification arrives.
```

### 👤 **User**  ·  06:10:48; waited 1m41s

<task-notification>
<task-id>a15c56281848e138c</task-id>
<tool-use-id>toolu_019ADJr4iBNBpGts27C9RBJc</tool-use-id>
<output-file>/tmp/claude-1000/-var-data-bootstrap/35ad0295-329c-4cf2-910b-11585f90ee90/tasks/a15c56281848e138c.output</output-file>
<status>completed</status>
<summary>Agent "Assess IL proof stack redundancy" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result># Structured-IL Proof Corpus: Complexity &amp; Redundancy Analysis

## Corpus map (verified line counts)

| Rung | File | Lines | Status |
|---|---|---|---|
| Shared Ctrl foundation | `LowIR/CtrlFacts.lean` | 292 | sorry-free |
| Shared SSA foundation | `LowSSA/ExecFacts.lean` | 736 | sorry-free |
| hex0 @ RawAsm | `RawAsm/Hex0/Refine.lean` | 3377 | sorry-free |
| hex0 @ LowIR-Ctrl | `LowIR/Hex0/CtrlProof.lean` | 1703 | sorry-free |
| hex0 @ LowIR-Prog | `LowIR/Hex0/ProgProof.lean` | 81 | **`sorry` @ 79** |
| hex0 @ LowSSA | `LowSSA/Hex0/Proof.lean` | 1519 | sorry-free |
| strlen @ Ctrl (flat) | `LowIR/Strlen/CoreProof.lean` | 198 | sorry-free |
| strlen @ Ctrl (outcome) | `LowIR/Strlen/Ctrl.lean` | 100 | sorry-free |
| strlen @ SSA | `LowSSA/Strlen/Proof.lean` | 214 | sorry-free |
| strtoull (wrapping) | `LowIR/Strtoull/WrappingProof.lean` | 215 | sorry-free |
| strtoull (conformant) | `LowIR/Strtoull/ConformantProof.lean` | 93 | sorry-free |
| ProgSim campaign | `LowIR/ProgSim/*.lean` | 3071 | 3 real `sorry` |

All apparent `sorry` hits in RawAsm/Strlen/Strtoull are the string "sorry-free" in headers — those rungs are genuinely complete. Real open goals live only in `ProgProof.lean` and `ProgSim/`.

---

## 1. Per-program anatomy at the IL altitude: plumbing vs spec

### How well the shared foundations factor plumbing out

**`CtrlFacts.lean` (292 L)** is pure generic plumbing, fully factored: 26 one-layer `exec_*` equations (the `normal` set `exec_addi/sub/lbu/slli/add/orr/seq_normal/while_step/while_done`, plus the full break/continue/return outcome-threading set through `seq`/`block`/`while`/`call`/`ife`), and the two load-bearing lemmas `exec_mono` (171) / `exec_mono_le` (284) that let every program lemma return an existential fuel. It is used heavily downstream: 92 call sites in `CtrlProof.lean`, 23 in `WrappingProof`, 13 in `Strlen/Ctrl`.

**`LowSSA/ExecFacts.lean` (736 L)** is the strictly more powerful analog. Beyond the same unfolder+`exec_mono` set, it adds two things Ctrl never factored:
- `exec_frame` (591, ~126 L incl. `catch0_frame`/`iterWhile_frame` support) + `exec_frame_rget` (717) — the **syntactic frame theorem** (P2): "`exec` only ever writes def-site registers." This is the generic replacement for hand-rolled register-preservation bookkeeping.
- `iterWhile_*` port lemmas (204-347) for the block-parameter loop form.

### The plumbing that still gets re-proved inside each program proof

Even with the foundations, three lemma families are re-established per program:

1. **Register-context struct + preservation** (the `frame`/`Regs`/`Pres` family). This is the clearest place the two rungs diverge:
   - Ctrl (`CtrlProof.lean:463-505`): `structure Regs` (16 fields) + `abbrev Pres` (10 register-inequality hypotheses) + `Regs.transfer` — a 45-line hand-rolled block where every field is discharged by ten `(by decide)` inequality args. This is verbose because Ctrl has **no** generic frame theorem.
   - SSA (`Proof.lean:197-241`): `structure RegsS` + `RegsS.of_agree` + `RegsS.frame`, ~45 L but the preservation is delegated to the generic `exec_frame_rget` (the header at 194 explicitly flags this as "the headline claim — NOT a bespoke `Pres`/`transfer`"). So SSA factored the *reasoning* but the per-program register-context *structure* is still duplicated.

2. **Guard/condition-evaluation lemmas**, re-proved near-identically in each program: Ctrl `geuL_true/false`, `slt_true/false`, `ceq_true/false`, `weq_true/false`, `geu_ww_true/false` (`CtrlProof.lean:75-108, 419-459`); SSA `geuR_true/false`, `ceqS_*`, `weqS_*`, `geu_wwS_*` (`Proof.lean:39-190`); strtoull `geu_true/false` (`ConformantProof.lean:24-40`). These are boilerplate `BitVec`→`Nat` guard bridges that could be a shared "condition-facts" module but currently are copy-pasted per program.

3. **Per-loop / per-op step lemmas** (genuinely program-specific but plumbing-flavored): `readAdv_eff` (196), `cgGuard_eff` (253), `skip_body`/`skip_loop` (328/353) in Ctrl; `hexPrefix_exec` (250), `scBody`/`skip_loopS` (277/311) in SSA. Plus **local exec unfolders that were never promoted into the shared file**: Ctrl re-defines `exec_skip/exec_lit/exec_sb/exec_err` and `ife_skip_false/ife_err_true` (`CtrlProof.lean:607-641`); SSA re-defines `exec_seqE`, `exec_errS`, `ifeErrS_true/false`, `exec_ifeE_then_pass/else_pass` (`Proof.lean:480-524, 812-825`).

### The fraction split (hex0, the two large proofs)

Grouping declarations by section boundaries:

**`CtrlProof.lean` (1703 L):** generic exec/frame/condition plumbing ≈ **20-25%** (borrow-separation + condition arith 22-149; local exec unfolders 607-641; `Regs`/`Pres`/`transfer` 461-505; dispatch lemmas 419-459 ≈ 380 L). Genuine spec reasoning ≈ **75-80%**, dominated by:
- `main_loop` — the central loop invariant relating IL `exec` to `Hex0.decodeS` — **1228-1604, ~377 L (22% of the file in one theorem)**;
- `hexPath_eff` (two-nibble `&lt;BYTE&gt;` path) 648-924, ~277 L;
- the `decodeS`/`nibble`/`skipComment` spec bridges 998-1226, ~230 L.

**`Proof.lean` (SSA, 1519 L):** similar ~20% plumbing / ~80% spec. Its `main_loop` is **1014-1429, ~416 L (27%)**; `hexPathS_eff` 525-800, ~276 L.

**strlen** is the counter-example that proves the point: it uses no break/continue/ret, so nearly all of its ~100-200 L *is* the loop invariant + arithmetic — almost no outcome plumbing. `Strlen/CoreProof.lean` (198 L, "flat" `St`, predates CtrlFacts and re-proves its own `exec_seq/addi/lbu/sub/while` at 52-71) vs `Strlen/Ctrl.lean` (100 L, outcome-carrying, reuses CtrlFacts unfolders + imports CoreProof's register/arith lemmas). The **2× shrink is a direct measurement of CtrlFacts' factoring payoff**.

---

## 2. Cross-rung redundancy for hex0 (same program, four altitudes)

Every rung re-proves the **same functional spec**, `Hex0.coreSpec` — the two-state (`High`/`Low`) nibble decoder with comment-skip, capacity truncation, and status codes. Each rung re-establishes that end-to-end correspondence from scratch against a different execution semantics:

- **RawAsm `Refine.lean` (3377 L):** the full correspondence at the flat program-counter / instruction-decode level. Re-does the loop invariant *plus* all the PC/decode/branch-offset bookkeeping the IL rungs are designed to avoid (the `Strlen/CoreProof` header explicitly contrasts "no PC, no decode, no offsets" against "the ~3400-line flat-PC `Hex0/Refine.lean`").
- **LowIR-Ctrl `CtrlProof.lean` (1703 L):** the same decoder invariant against `LowIR.Ctrl.exec` (structured, register-machine, outcome-carrying). The `main_loop`↔`decodeS` argument here is genuinely re-derived — it does not reuse RawAsm's.
- **LowIR-Prog `ProgProof.lean` (81 L, `sorry`):** the same theorem again, now against `LowIR.Prog.exec` (adds frames/calls). It is **entirely a statement + prose proof-plan** (lines 43-74) — and that plan (main-loop invariant on `in_idx`, `decodeS .High` prefix agreement, `skipComment`/body/cap sub-lemmas, induct on `inp.length - J`) is essentially a **re-transcription of the invariant already proved in `CtrlProof.main_loop`**. This is the largest pure duplication in the corpus: the Prog rung would re-derive, against a near-identical structured semantics, what Ctrl already has.
- **LowSSA `Proof.lean` (1519 L):** the same decoder invariant a fourth time against `LowIR.SSA.exec` (valued outcomes, `catch0` break-scopes, block-parameter `while`). `main_loop` (416 L), `hexPathS_eff`, `pnibS_eff`, the `RegsS` context — all are SSA re-castings of their Ctrl namesakes (`main_loop`, `hexPath_eff`, `pnib_correct`, `Regs`).

**Genuine duplication:** the *decoder correctness argument itself* (High/Low state machine ≡ `decodeS`, comment-skip, capacity) is proved 3× today (RawAsm, Ctrl, SSA) and would be a 4th time at Prog. What legitimately differs each time is only the semantics-specific glue (PC/decode at RawAsm; outcome threading at Ctrl; `catch0`/`iterWhile` at SSA; frames/calls at Prog). The spec-side machinery (`decodeS` unfolders, `nibble`/`pnibR` bridges, `boundedRun`/`commentSkip`) is near-verbatim between Ctrl and SSA.

**What `compile_sim` (ProgSim) collapses:** ProgSim proves *the compiler is correct*, i.e. `LowIR.Prog.exec` ⇒ compiled-RV64I behavior (`prog_sim`, `Defs.lean:412`). Once it lands:
- The **RawAsm rung becomes redundant as a correctness path**: `hex0_correct` at Prog level `∘ prog_sim` transports IL correctness to the trusted `Rv64i` machine mechanically (the ProgProof header calls this "`T1 ∘ hex0_correct`"), so the 3377-line hand-proof `Refine.lean` is no longer needed to certify the bytes — it becomes a legacy artifact.
- The **Ctrl vs Prog duplication resolves in one direction**: only *one* structured-IL functional proof needs to exist and feed `prog_sim`. `ProgProof.hex0_correct` (Prog) is the one that composes with `prog_sim`; `CtrlProof` (Ctrl) then becomes either the source that `ProgProof` should reuse or a redundant sibling.
- SSA remains a *separate* correctness path (different backend), not subsumed by `prog_sim` (which is Prog→RV64I, not SSA→Prog).

---

## 3. ProgSim campaign state

**Proved / sorry-free:**
- `Defs.lean` — all definitions (`Layout`, `layoutOf`, `Installed`+computable `codeInstalledB`/`dataInstalledB`, `StInv`, `MachPriv`/`machPrivB`, `SimPre`, `execT`/`runT` footprint instrumentation, `userPad`) with a full `#guard` oracle battery (445-500) validating them against frozen-compiler fixtures. Only `prog_sim` is open.
- Helper layers `ExecFacts.lean` (216), `WordMem.lean` (145), `SlotFacts.lean` (246), `MemFacts.lean` (258) — **all sorry-free**.
- `StmtSim.lean` — `emit`/`Emitted`, the `step_*` decode lemmas (119-196), `single_op_sim` (444), `two_op_sim` (494), `run_load` (361)/`run_store` (389), `StInv_*` congruence lemmas, `jump_sim`/`ret_sim`/`brkB_sim`/`contL_sim` (595-661) all proved. `lower_sim` proves the **straight-line + memory** cases (skip, annot, addi, slli, srli, add, sub, orr, seq, ld, lbu, sd, sb). The `sub3_body_sim` end-to-end corollary (1034) is sorry-free — the validated vertical slice / go-no-go against the differential oracle (`[30,12,2]↦40`).
- `CtrlSim.lean` — `emitCF`/`csize`/`emitCF_length`, `cond_taken`/`cond_not_taken`, and `lower_sim_cf` proves **skip, annot, block, ret, brkB, contL, seq, all straight-line/memory ops, and `ife` fully** (both branches × all four outcomes, 474-602).

**Open `sorry` (3 real):**
- `StmtSim.lean:1003` — `all_goals sorry` catching `lower_sim`'s control-flow tail (ife/while/block/brkB/contL/ret/call/cref/clen). **Dead/redundant** (see §4).
- `CtrlSim.lean:603` — `all_goals sorry` catching `lower_sim_cf`'s remaining cases: **`while`, `call`, `cref`, `clen`**.
- `Defs.lean:432` — `prog_sim`, the top-level program payoff (statement complete, proof deferred).

**Size of the remaining gap:**
- **`while`** — the one remaining *inductive* statement case. The block/ife/seq skeleton it needs (recursive `ih`, outcome case-split, `Emitted_append` slicing, `jump_sim` back-edge) is already demonstrated in the proved `block`/`ife` cases (256-310, 474-602), so this is "more of the same" rather than new infrastructure — likely the largest single remaining chunk but structurally understood.
- **`call`** — needs a `call_sim`/activation-record lemma that does not yet exist even as a statement (referenced only in `Defs.lean:398` prose). Requires prologue/epilogue frame reasoning; this is the Phase-3/§3.3 workhorse and genuinely new work.
- **`cref`/`clen`** — const-data address/length; `emit` currently *stubs them* (`csize`=0, `emit`=`[]`, noted at `StmtSim.lean:670` and the `csize` header), so both the lowering and its proof are unwritten.
- **`prog_sim`** (Defs 432) — Phases 5/6: wire `lower_sim_cf` over a whole function body, add prologue/epilogue (frame enter/exit, arg marshaling), compose `call_sim`, and connect the abstract `Emitted` predicate to real compiled bytes (**Phase 2 / AsmFacts** — currently `Emitted` is used axiomatically and only validated by `#guard`, not proved from `layoutOf`). Phase 1 (stack-budget/`SimPre` overflow) already appears discharged in the definitions.

Net: the *statement-level* induction is ~85% done (all straight-line + memory + block/ife/seq/ret/brk/cont proven; while is tractable, call/cref/clen need new infra), but the *top-level* `prog_sim` still needs the whole prologue/epilogue/call/Emitted-realization stack, which is the bulk of the remaining engineering.

---

## 4. Internal redundancy in ProgSim: `lower_sim` (StmtSim) vs `lower_sim_cf` (CtrlSim)

**Partially subsumed, not fully.** The relationship is a deliberate delegation, not pure duplication:

- `lower_sim_cf` **reuses** `lower_sim` for every straight-line/memory leaf: its addi/add/sub/orr/slli/srli/lbu/ld/sb/sd cases (`CtrlSim.lean:394-473`) each call `lower_sim (fuel+1) …` directly. This works because `emitCF` = `emit` (label-free) for those ops. So `lower_sim`'s **leaf cases are live** — they are the proven backend `lower_sim_cf` sits on top of.

- What **is redundant**:
  1. `lower_sim`'s own **`seq` case** (`StmtSim.lean:785-814`) — `lower_sim_cf` handles `seq` itself via its recursion (`ih`, 338-393) and never calls `lower_sim` on a `seq`. So `lower_sim`'s seq case has no consumer.
  2. `lower_sim`'s entire **control-flow tail** (`all_goals sorry` @ 1003, covering ife/while/block/brkB/contL/ret/call/cref/clen) is fully superseded by `lower_sim_cf`, which is `.normal`-only-vs-outcome-carrying strictly more general and already proves block/ife/ret/brk/cont.

**Conclusion:** `lower_sim` should be pruned down to just the straight-line + memory leaf cases (its useful role as `lower_sim_cf`'s delegated backend). Its `seq` case and its 9-constructor control-flow `sorry` tail are dead weight — the redundant open `sorry` at `StmtSim.lean:1003` can be removed outright, leaving `CtrlSim.lean:603` as the single real statement-level frontier. The `.normal`-only `lower_sim` was the Phase-4.1 prototype; `lower_sim_cf` is its outcome-carrying replacement and has structurally absorbed everything except the arithmetic leaves.</result>
<usage><subagent_tokens>90009</subagent_tokens><tool_uses>27</tool_uses><duration_ms>365177</duration_ms></usage>
</task-notification>

### 🤖 **Assistant**  ·  06:11:57; model 1m09s

> 💭 (thinking)
> 

Both analyses are in. Here's the full assessment.

# Proof complexity & redundancy — the current picture

The corpus is ~27k lines of Lean. Its cost structure, measured:

**The flat-PC rung is the complexity sink — ~48% of all proof lines for two programs.** `RawAsm/Hex0/Refine.lean` (3,377) + the hex1 trio (9,564) are per-program, per-byte simulation proofs. The hex1 analysis shows the method scales *super-linearly* with program sophistication: code grew 2.2× (81→181 instructions) but proof grew 2.8×, and the growth is not boilerplate — only ~15% (~1,400 lines) of hex1 is re-instantiation of machinery hex0 already proved (`step_*`, `li_*` blocks, comment/spacing loops, rebound to a new concrete `Image`). The dominant cost is genuinely new flat-PC work the methodology forces: the two-pass structure duplicates every per-token lemma family (`p1_*`/`p2_*`, comment loop ported *twice*), and the label subsystem alone is ~2,500 lines with `p2_ref` a single 919-line lemma. Average lemma size: hex1 ~89 lines vs hex0 ~32. This method should never be used again — which is exactly what ProgSim is for.

**The same spec↔program argument is proved three times, heading for four.** The hex0 decoder-correctness invariant (High/Low state machine ≡ `decodeS`, comment skip, capacity) exists at RawAsm (inside the 3,377), at Ctrl (`CtrlProof.lean`, 1,703), and at SSA (`Proof.lean`, 1,519); `Hex0/ProgProof.lean` is an 81-line statement whose prose plan is a re-transcription of `CtrlProof.main_loop`. This cross-rung repetition — not intra-file bloat — is the largest redundancy in the corpus. In each re-proof only ~20–25% is rung-specific glue; the other ~75–80% (the `main_loop` invariant at ~380–420 lines, `hexPath_eff` at ~280, the spec bridges) is the same argument re-cast against a different `exec`.

**The IL rungs themselves are well-factored, with three residual leaks.** `CtrlFacts.lean` (292 lines, 92 call sites in the hex0 proof alone) and `LowSSA/ExecFacts.lean` (736) successfully hoist the exec/outcome/mono plumbing — the strlen pair measures the payoff directly (198-line pre-CtrlFacts proof vs 100-line post). What still gets re-paid per program: (1) **guard-bridge lemmas** (`geuL/slt/ceq/weq_true/false` families) copy-pasted near-identically into Ctrl-hex0, SSA-hex0, and strtoull; (2) **register-context structs** — Ctrl has no frame theorem, so `Regs`/`Pres`/`transfer` with ten `by decide` side-goals is hand-rolled per proof, whereas SSA proved `exec_frame` once and its per-proof context carries zero preservation obligations (the SSA campaign's confirmed headline); (3) **local unfolders never promoted** (`exec_lit`, `exec_err`, `ife_*_pass` variants live inside program proofs instead of the Facts files).

**ProgSim has one piece of internal dead weight.** `StmtSim.lean`'s `lower_sim` is the Phase-4.1 prototype: `lower_sim_cf` (CtrlSim) still consumes its straight-line/memory leaf cases, but its `seq` case and its nine-constructor `all_goals sorry` tail at `StmtSim.lean:1003` have no consumer — `lower_sim_cf` supersedes them. Pruning that leaves `CtrlSim.lean:603` (while/call/cref/clen) as the single honest statement-level frontier, plus `prog_sim` at `Defs.lean:432`.

# What reduces spec↔program complexity

The lever hierarchy, in order of payoff:

1. **Finish `compile_sim` — everything else is downstream of it.** It converts the 3.4k/9.6k-per-program flat-PC tax into a one-time cost; afterwards a program's machine-code correctness costs exactly its IL proof (~1.5–1.7k for hex0-class, ~100–200 for strlen-class — a 5–6× reduction, and more for hex1-class programs, which would never pay their two-pass flat-PC premium). Remaining, per the agent's audit: the `while` case is "more of the same" (the block/ife skeleton it needs is already proved); **`call` is the genuinely new work** (prologue/epilogue/`CallChain`, Phase 5 — the plan's summit); `cref`/`clen` need both the `emitCF` extension and proofs; then Phase 1 (encode/decode), Phase 2 (connecting `Emitted` to the real `layoutOf` pipeline — currently validated only by `#guard`), and Phase 6 assembly.

2. **Prove each program once, at one altitude.** Once `compile_sim` lands, the policy should be: the Prog-level IL proof is *the* proof; RawAsm `Refine.lean` files become legacy certification artifacts, and Ctrl/SSA hex0 proofs are retained as experiment records, not maintained rungs. Concretely: write `ProgProof.hex0_correct` by porting `CtrlProof.main_loop` (its plan already is that port), compose with `prog_sim`, and the tower closes without a fourth hand-derivation being *added to* rather than *replacing* the other three.

3. **Give Prog the SSA frame theorem before writing the Prog-level hex0 proof.** The measured SSA result says the win wasn't line count (1,519 vs 1,703 — flat) but *statement shape*: `exec_frame` proved once kills the per-body `Regs`/`Pres`/`transfer` tax and empties the loop invariant of register clauses. A syntactic `defs`-based frame theorem holds for Prog's mutable registers too (exec only writes textually-assigned registers). Proving it in `ProgSim/ExecFacts.lean` (~120 lines by the SSA precedent) before starting `ProgProof` is the cheapest way to make the fourth hex0 proof the smallest one yet.

4. **Hygiene, small but permanent:** a shared condition-facts module for the guard bridges (third copy-paste client already exists); promote the local unfolders into `CtrlFacts`/`ExecFacts`; do the P3 borrow-layer restatement over a bare memory update (also on its third client); prune `lower_sim`'s dead tail.

# The next rung

**Immediately: not a new rung — close this one.** Finish `lower_sim_cf` (while → cref/clen), then Phase 5 `call_sim`, Phases 1/2/6, then the Prog-level hex0/hex1 proofs + transport corollaries. That's what retires the flat-PC method and realizes the amortization the whole campaign is for.

**The next rung proper: graduate the SSA experiment — the SSA→Prog lowering with its simulation proof.** All three of your own documents converge on this: the SSA assessment's "keeper" (§1: lower SSA→Prog, don't fork the compiler — the ProgSim campaign is reused unchanged), the §8 `iterWhile` rework that made both sides' loop terms fixed so the lowering's `while` case is a per-head-entry correspondence with `iterWhile_mono`/`iterWhile_frame` as ready-made interface, and RESUME-PROGSIM §7.6, which explicitly kept `lower_sim`'s statement shape composition-friendly for it. The payoff: programs get *written and proved* at the SSA altitude — where the measured evidence says invariants are args-tuples with no register bookkeeping — and transported SSA→Prog→RV64I without re-litigating either pass. Out-of-SSA is nearly syntactic on unbounded registers (parallel copies through fresh temporaries), so this is a cheap rung relative to what it buys.

**The rung after that — the genuinely new component: the borrow checker over the SSA IR (Ext. 5 / theses 6).** It's the natural carrier (borrow signatures attach to single-assignment names without reborrow-vs-overwrite ambiguity), and it's what turns ProgSim's assumed side conditions into discharged ones: the footprint hypotheses (`MemAccOff`, `execT`'s `ws.all (¬ MachPriv ·)`) and the `Wf`/`Disjoint` hypotheses the IL program proofs currently take as inputs would be *produced* by a checker gate instead of assumed per program. That's the "empty cell" composition from DESIGN-THESES — borrow gate → verified backend — and per open question §7.4 the hooks for it (footprint as a def) are already in place.

So the ladder from here: **while/call → `prog_sim` → hex0F/hex1F at Prog (frame theorem first) → SSA→Prog lowering sim → borrow checker over SSA.** Each step strictly shrinks what the next program proof has to say.

### 👤 **User**  ·  06:13:00; waited 1m04s

Check whether the IL-stack redundancy Explore agent has completed; if so, synthesize the proof-complexity/redundancy assessment and next-rung recommendation the user asked for.

### 🤖 **Assistant**  ·  06:13:06; model 8.8s

> 💭 (thinking)
> 

Both agents completed and the synthesis was already delivered in my previous message — the full assessment (flat-PC rung as the complexity sink, the 3×-going-on-4× duplication of the hex0 decoder argument, the IL factoring leaks, ProgSim's dead `lower_sim` tail) and the next-rung recommendation (close `compile_sim` first, then Prog-altitude program proofs with a frame theorem, then graduate SSA→Prog lowering, then the borrow checker). No further work is pending; nothing to reschedule.

### 👤 **User**  ·  06:17:07; waited 3m58s

steer the docs to indicate the updated priorities/plan.

### 🤖 **Assistant**  ·  06:17:46; model 1m22s

> 💭 (thinking)
> 

I'll capture the assessment as a new analysis doc in `docs/`, re-point the active handoff's NEXT section at the re-assessed priorities, add the graduation note to the SSA experiment doc, index it, validate, and commit.

**🔧 tool → Write**  ·  58ms
```json
{"file_path": "/var/data/bootstrap/docs/PROOF-COMPLEXITY.md", "content": "# Proof complexity & redundancy — measured assessment and the priority ladder\n\nAssessed 2026-07-04 over the full `lean/` corpus (~27k lines), mid-ProgSim\ncampaign. Read with [LEAN-LAYOUT.md](LEAN-LAYOUT.md) (what the files are),\n[RESUME-PROGSIM.md](RESUME-PROGSIM.md) (the active campaign this re-prioritizes),\n[RESUME-SSA-HEX0.md](RESUME-SSA-HEX0.md) §6 (the measured SSA-vs-Ctrl table this\nextends), and [DESIGN-THESES.md](DESIGN-THESES.md) (the target the ladder climbs\ntoward). Purpose: record where the proof lines actually go, which of them are\nredundant, and the resulting plan — so the next campaign choice is made from\nmeasurement, not vibes.\n\n## 1. Where the lines go\n\n| Rung | Files | Lines | Share |\n|---|---|---|---|\n| RawAsm flat-PC proofs | `Hex0/Refine` 3377; `Hex1/{Refine,RefineBase,DecodeFacts}` 9564 | ~12.9k | **~48%** |\n| LowIR Ctrl/Prog program proofs | `Hex0/CtrlProof` 1703, strlen 298, strtoull 308, `Hex0/ProgProof` 81 (sorry) | ~2.4k | ~9% |\n| LowSSA program proofs | `Hex0/Proof` 1519, `Strlen/Proof` 214 | ~1.7k | ~6% |\n| Shared proof foundations | `CtrlFacts` 292, `LowSSA/ExecFacts` 736 | ~1.0k | ~4% |\n| ProgSim campaign (in flight) | `ProgSim/*` | ~3.1k | ~11% |\n| Definitions/compiler/libs/specs/harness | everything else | ~5.9k | ~22% |\n\nTwo programs at the flat-PC altitude cost as much as everything else combined.\n\n## 2. Findings\n\n### F1. The flat-PC method sc

… [truncated 6712 chars]
```

### 🛠️ **Tool result**  ·  06:18:28; tool 58ms

```
File created successfully at: /var/data/bootstrap/docs/PROOF-COMPLEXITY.md (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  06:18:33; model 8.3s

Now the README index entry and the RESUME-PROGSIM NEXT-section rewrite:

**🔧 tool → Edit**  ·  23ms
```json
{"replace_all": false, "file_path": "/var/data/bootstrap/docs/README.md", "old_string": "## Analyses\n\n- [LEAN-VS-COQ.md](LEAN-VS-COQ.md) — implementation-difficulty comparison of the Lean vs Coq proofs, measured from the recorded [agent sessions](../sessions/README.md).", "new_string": "## Analyses\n\n- [PROOF-COMPLEXITY.md](PROOF-COMPLEXITY.md) — measured complexity/redundancy assessment of the Lean proof corpus (2026-07-04) and the resulting priority ladder: close `compile_sim` → one proof per program at the Prog altitude (frame theorem first) → SSA→Prog lowering → borrow checker.\n- [LEAN-VS-COQ.md](LEAN-VS-COQ.md) — implementation-difficulty comparison of the Lean vs Coq proofs, measured from the recorded [agent sessions](../sessions/README.md)."}
```

### 🛠️ **Tool result**  ·  06:18:37; tool 23ms

```
The file /var/data/bootstrap/docs/README.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  06:18:47; model 10.4s

**🔧 tool → Edit**  ·  48ms
```json
{"replace_all": false, "file_path": "/var/data/bootstrap/docs/RESUME-PROGSIM.md", "old_string": "- **NEXT:** Phase 4.3 control flow (ife/while/block/ret/brk/cont — the outcome\n  selects a label; needs the `Emitted` label-offset algebra + a `.brk/.cont/.ret`\n  outcome-carrying `lower_sim` generalization), cref/clen (`synthConst`), Phase 2\n  (AsmFacts: `Emitted L pos (emit stmt)` from the real pipeline), 5 (call),\n  6 (prog_sim). A sorry-free frameLocal corollary (mem end-to-end vs the oracle)\n  can be done now by driving sd/ld through the simulators directly (as sub3 did).", "new_string": "- **NEXT (priorities re-assessed 2026-07-04 — [PROOF-COMPLEXITY.md](PROOF-COMPLEXITY.md)):**\n  1. **Prune `lower_sim`'s dead tail** (`StmtSim.lean:1003` + its `seq` case):\n     `lower_sim_cf` supersedes both and already proves block/ife/seq/ret/brk/\n     cont; keep only the straight-line/mem leaf cases it delegates to. After\n     the prune, `CtrlSim.lean:603` is the single statement-level `sorry`.\n  2. `lower_sim_cf` **while** (fuel-IH back-edge — design confirmed in the\n     Phase-4.3 status entry above), then **cref/clen** (`synthConst`; needs the\n     `emitCF` extension first — both are currently stubbed in `emit`/`csize`).\n  3. Phase 5 (**call**), then Phases 1/2 (encode/decode; AsmFacts —\n     `Emitted L pos (emit stmt)` from the real pipeline, today only `#guard`-\n     validated), then 6 (**prog_sim**).\n  4. **Before starting `ProgProof`** (post-campaign): prove the Pro

… [truncated 602 chars]
```

### 🛠️ **Tool result**  ·  06:18:47; tool 48ms

```
The file /var/data/bootstrap/docs/RESUME-PROGSIM.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  06:18:54; model 14.9s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Edit**  ·  28ms
```json
{"replace_all": false, "file_path": "/var/data/bootstrap/docs/RESUME-PROGSIM.md", "old_string": "- [ ] Finish `lower_sim`: the COMPOUND control-flow ops (ife/while/block) via the\n  outcome-carrying generalization + label-position map (design in the Phase-4.3\n  status entry above), then cref/clen, then call.\n- [ ] Then Phases 1/2 (encode/decode, assembler) in parallel-friendly chunks,\n  the rest of 4, 5, 6 in order.", "new_string": "- [x] `lower_sim_cf` (outcome-carrying, `CtrlSim.lean`): skip/annot/block/seq/\n  arith/mem/ret/brkB/contL/**ife** proven; needs `BranchOk` + ife `MemAccOff`.\n- [ ] Prune `lower_sim`'s superseded tail (`StmtSim.lean:1003` + its `seq`\n  case) — see the re-assessed NEXT list above ([PROOF-COMPLEXITY.md](PROOF-COMPLEXITY.md)).\n- [ ] Finish `lower_sim_cf`: while, then cref/clen (unstub `emitCF`/`csize`),\n  then call (Phase 5).\n- [ ] Then Phases 1/2 (encode/decode, assembler) in parallel-friendly chunks,\n  then 6 (`prog_sim`).\n- [ ] Post-campaign: Prog frame theorem, then `hex0F_correct` at the Prog\n  altitude (the ONE maintained hex0 proof), then the SSA→Prog lowering sim —\n  the ladder in [PROOF-COMPLEXITY.md](PROOF-COMPLEXITY.md) §3."}
```

### 🛠️ **Tool result**  ·  06:19:02; tool 28ms

```
The file /var/data/bootstrap/docs/RESUME-PROGSIM.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  06:19:07; model 5.2s

**🔧 tool → Edit**  ·  25ms
```json
{"replace_all": false, "file_path": "/var/data/bootstrap/docs/LOWIR-SSA-EXPERIMENT.md", "old_string": "   Until something needs (a)–(c), this stays a frozen experiment;\n   D7's \"explicitly not SSA\" decision for *Prog itself* stands unrevised.\n   The proof half of the graduation test now has a written plan:\n   [RESUME-SSA-HEX0.md](RESUME-SSA-HEX0.md) — prove `hex0S` ≡ `Hex0.coreSpec`\n   and measure the proof section-by-section against `CtrlHex0Proof.lean`.", "new_string": "   Until something needs (a)–(c), this stays a frozen experiment;\n   D7's \"explicitly not SSA\" decision for *Prog itself* stands unrevised.\n   The proof half of the graduation test now has a written plan:\n   [RESUME-SSA-HEX0.md](RESUME-SSA-HEX0.md) — prove `hex0S` ≡ `Hex0.coreSpec`\n   and measure the proof section-by-section against `CtrlHex0Proof.lean`.\n   **Update 2026-07-04**: (a) is done (the campaign landed, frame theorem +\n   §8 `iterWhile` included), and graduation — (b) the SSA→Prog lowering +\n   simulation, then (c) — is now scheduled as the rung after `compile_sim`\n   closes and the Prog-altitude program proofs land: the priority ladder in\n   [PROOF-COMPLEXITY.md](PROOF-COMPLEXITY.md) §3."}
```

### 🛠️ **Tool result**  ·  06:19:07; tool 25ms

⚠️ error

```
<tool_use_error>String to replace not found in file.
String:    Until something needs (a)–(c), this stays a frozen experiment;
   D7's "explicitly not SSA" decision for *Prog itself* stands unrevised.
   The proof half of the graduation test now has a written plan:
   [RESUME-SSA-HEX0.md](RESUME-SSA-HEX0.md) — prove `hex0S` ≡ `Hex0.coreSpec`
   and measure the proof section-by-section against `CtrlHex0Proof.lean`.
(note: Edit also tried swapping \uXXXX escapes and their characters; neither form matched, so the mismatch is likely elsewhere in old_string. Re-read the file and copy the exact surrounding text.)</tool_use_error>
```

### 🤖 **Assistant**  ·  06:19:16; model 11.3s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  83ms
```json
{"command": "grep -n 'frozen experiment' /var/data/bootstrap/docs/LOWIR-SSA-EXPERIMENT.md; sed -n 166,177p /var/data/bootstrap/docs/LOWIR-SSA-EXPERIMENT.md", "description": "Locate exact graduation paragraph text"}
```

### 🛠️ **Tool result**  ·  06:19:19; tool 83ms

```
172:   pilot. Until something needs (a)–(c), this stays a frozen experiment;
5. **Where it earns its keep / trigger to graduate**: if the borrow-checker
   layer (Ext. 5) lands, its natural carrier is exactly this IR — borrow
   signatures attach to single-assignment names far more cleanly than to
   mutable registers (no reborrow-vs-overwrite ambiguity). Graduation =
   (a) the SSA frame theorem + checker soundness lemma, (b) the SSA→Prog
   lowering + its simulation proof, (c) porting one ProgLib function as the
   pilot. Until something needs (a)–(c), this stays a frozen experiment;
   D7's "explicitly not SSA" decision for *Prog itself* stands unrevised.
   The proof half of the graduation test now has a written plan:
   [RESUME-SSA-HEX0.md](RESUME-SSA-HEX0.md) — prove `hex0S` ≡ `Hex0.coreSpec`
   and measure the proof section-by-section against `CtrlHex0Proof.lean`.
```

### 🤖 **Assistant**  ·  06:19:25; model 9.7s

> 💭 (thinking)
> 

**🔧 tool → Edit**  ·  53ms
```json
{"replace_all": false, "file_path": "/var/data/bootstrap/docs/LOWIR-SSA-EXPERIMENT.md", "old_string": "   and measure the proof section-by-section against `CtrlHex0Proof.lean`.", "new_string": "   and measure the proof section-by-section against `CtrlHex0Proof.lean`.\n   **Update 2026-07-04**: (a) is done (the campaign landed, frame theorem +\n   §8 `iterWhile` included), and graduation — (b) the SSA→Prog lowering +\n   simulation, then (c) — is now scheduled as the rung after `compile_sim`\n   closes and the Prog-altitude program proofs land: the priority ladder in\n   [PROOF-COMPLEXITY.md](PROOF-COMPLEXITY.md) §3."}
```

### 🛠️ **Tool result**  ·  06:19:28; tool 53ms

```
The file /var/data/bootstrap/docs/LOWIR-SSA-EXPERIMENT.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  06:19:36; model 8.4s

Now validate the doc corpus and commit:

**🔧 tool → Bash**  ·  2.0s
```json
{"command": "./docs/check_docs.py", "description": "Validate docs corpus (links, index reachability)"}
```

### 🛠️ **Tool result**  ·  06:19:39; tool 2.0s

```
OK — 42 markdown files checked, all constraints hold.
```

### 🤖 **Assistant**  ·  06:19:49; model 10.2s

**🔧 tool → Bash**  ·  1.5s
```json
{"command": "git -C /var/data/bootstrap add docs/PROOF-COMPLEXITY.md docs/README.md docs/RESUME-PROGSIM.md docs/LOWIR-SSA-EXPERIMENT.md && git -C /var/data/bootstrap commit -m \"$(cat <<'EOF'\ndocs: measured proof-complexity assessment + re-prioritized plan\n\nNew docs/PROOF-COMPLEXITY.md: where the 27k Lean lines go (flat-PC rung\n= 48% for two programs, hex1 scaling super-linearly), the redundancy\nfindings (hex0 decoder argument proved 3x heading for 4x; guard-bridge/\nframe-theorem/unfolder leaks in the IL rungs; lower_sim's superseded\ntail), and the resulting priority ladder: close compile_sim -> one proof\nper program at the Prog altitude (frame theorem first) -> SSA->Prog\nlowering -> borrow checker.\n\nRESUME-PROGSIM.md: NEXT section rewritten to the re-assessed order\n(prune StmtSim:1003 dead tail first; while -> cref/clen -> call ->\n1/2/6; post-campaign Prog frame theorem before ProgProof); cold-start\nlist updated. LOWIR-SSA-EXPERIMENT.md: graduation trigger updated —\n(a) done, (b)/(c) scheduled per the ladder. README.md indexes the new\nanalysis.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>\nEOF\n)\"", "description": "Commit the docs re-prioritization"}
```

### 🛠️ **Tool result**  ·  06:19:50; tool 1.5s

```
[master 553a490] docs: measured proof-complexity assessment + re-prioritized plan
 4 files changed, 168 insertions(+), 10 deletions(-)
 create mode 100644 docs/PROOF-COMPLEXITY.md
```

### 🤖 **Assistant**  ·  06:19:56; model 6.4s

> 💭 (thinking)
> 

Committed (`553a490`). Let me also update the campaign memory so future sessions pick up the re-prioritization:

**🔧 tool → Read**  ·  14ms
```json
{"file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md"}
```

### 🛠️ **Tool result**  ·  06:19:57; tool 14ms

```
1	---
2	name: progsim-campaign-frontier
3	description: compile_sim-for-Prog campaign — Phase 0/3 done; lower_sim_cf (outcome-carrying) has skip/annot/block/seq/arith/mem/leaves AND ife DONE; remaining sorry = while/cref/clen/call.
4	metadata: 
5	  node_type: memory
6	  type: project
7	  originSessionId: a41cd3ef-e057-451c-b2c8-26929e4cdbdc
8	---
9	
10	The `compile_sim`-for-Prog proof campaign (plan: `docs/RESUME-PROGSIM.md`; see
11	[[lowir-compiler-cut]] for the frozen compiler it verifies). 2026-07-03: the
12	plan doc gained (a) the full `lower_sim_cf` while-case skeleton — Prog's
13	back-edge re-enters the SAME term at fuel, so the fuel IH applies directly (the
14	fixed-term shape SSA bought via its §8 `iterWhile` rework, see
15	[[ssa-hex0-campaign-frontier]]); on `.cont 0` the body's own lowered jump
16	already lands at lTop (`contL_sim`), only `.normal` runs the emitted back-edge
17	jal — and (b) §7.6: keep `lower_sim`/`prog_sim` fuel-indexed, outcome-carrying,
18	plain-equality so the future SSA→Prog lowering sim composes (fixed loop terms
19	both sides; `iterWhile_mono`/`iterWhile_frame` are the SSA-side interface).
20	Progress as of 2026-07-02:
21	
22	**Phase 0.1 DONE** (commit cedb25c): decision **P1** — the frame-padding oracle
23	`pad : Name → Nat` added to `Prog.frameEnter`/`exec`/`run`. `frameReg` stays at
24	`spCaller−frameSize`; propagated `sp` drops an extra `pad`, so at `pad :=
25	userOff` IL `sp` ≡ machine `x2` at every call depth. Compiler/shim/QEMU FROZEN;
26	`pad = fun _ => 0` re-greens everything. Executable validation in
27	`CompileTests.lean` Stage 4c (`framesAgree`, `p1_chain_*`/`p1_rec_*`): pass at
28	userPad, diverge at pad=0.
29	
30	**ProgSim/Defs.lean DONE** (commits 2fa8faf + cdcbf0f): new lib target
31	`LowIRProgSim` (rooted at `LowIR.ProgSim.Defs`, in defaultTargets). Contains,
32	all elaborating: `Layout`/`layoutOf`, `Installed` (+ #guard'd computable
33	shadows), `execT` write-footprint + `runT`,
34	`memRange`/`MachPriv`/`machPrivB`/`MachStack`/`StInv`/`SimPre`/`userPad`, and
35	`prog_sim` (SORRY, the self-contained payoff). All defs #guard-validated on
36	real compiled blobs. `execT_erase` PROVED (commit after cdcbf0f, via
37	`execT_map_exec` — erasing the footprint by Option.map = exec, structural fuel
38	induction; axioms `[propext, Quot.sound]` only).
39	
40	**Phase 0.2 DONE** (commit a9afd33): `ProgSim/ExecFacts.lean` — 42 one-layer
41	`exec_*` unfolders in `namespace LowIR.Prog`, each a one-line `simp [exec, …]`.
42	
43	**Phase 3 (start) — WordMem DONE** (commit f513c27): `ProgSim/WordMem.lean` —
44	the 64-bit LE load/store algebra. `byte_bit` (bit `i` of shifted byte `c` = `v`
45	bit `i` iff `i∈[c,c+8)`), `loadWord_storeWord_same` (round-trip, no overflow
46	hyp — 8 addrs differ by distinct literals), `storeWord_mem_of_ne`/`_outside`,
47	`loadWord_storeWord_disjoint`. Reconstruction via `getLsbD` ext (bv_decide
48	can't see `>>>`); round-trip = OR of 8 windows tiling [0,64). Gotcha: `omega`
49	REJECTS a Bool-valued goal → `rw [Bool.eq_iff_iff]` to Prop first. **Retrofitted
50	(commit 6a4fbb2) to `[propext, Quot.sound]`** — see [[lean-omega-classical-choice]]
51	(omega on iff/∧/∨ goals + `by_cases` pull `Classical.choice`; window tiling is
52	now the explicit constructive `window_tiling`).
53	
54	**Phase 3 (finish) — SlotFacts DONE** (commit 6a4fbb2): `ProgSim/SlotFacts.lean`
55	— the StInv slot algebra, `[propext, Quot.sound]`. Slot arith
56	(`slotOff_add8_le_userOff`, `slotOff_disjoint`, `slotAddr_toNat`), slot rd/wr
57	lifted from WordMem (`loadWord_store_slot_same`/`_ne`), blob preservation
58	(`fetch32_pc_congr`, `blobAddr_toNat`, `mem_storeWord_off_blob`,
59	`Installed_storeWord_off_blob`), and the payoff **`StInv_store_slot`**: a machine
60	slot store mirrors IL `rset r v`, re-establishing all six `StInv` conjuncts.
61	Frame-/blob-placement side conditions (`hnw`/`hseg`/`hblob`/`hbd`) are explicit
62	hypotheses — Phases 4/5 discharge from `SimPre`. Reg binders typed `Nat` (a `≤`
63

… [truncated 8671 chars]
```

### 🤖 **Assistant**  ·  06:20:06; model 8.9s

**🔧 tool → Edit**  ·  74ms
```json
{"replace_all": false, "file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md", "old_string": "description: compile_sim-for-Prog campaign — Phase 0/3 done; lower_sim_cf (outcome-carrying) has skip/annot/block/seq/arith/mem/leaves AND ife DONE; remaining sorry = while/cref/clen/call.", "new_string": "description: compile_sim-for-Prog campaign — Phase 0/3 done; lower_sim_cf (outcome-carrying) has skip/annot/block/seq/arith/mem/leaves AND ife DONE; remaining sorry = while/cref/clen/call. Priorities re-assessed 2026-07-04 in docs/PROOF-COMPLEXITY.md."}
```

### 🛠️ **Tool result**  ·  06:20:06; tool 74ms

```
The file /home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  06:20:12; model 6.8s

**🔧 tool → Edit**  ·  72ms
```json
{"replace_all": false, "file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md", "old_string": "The `compile_sim`-for-Prog proof campaign (plan: `docs/RESUME-PROGSIM.md`; see\n[[lowir-compiler-cut]] for the frozen compiler it verifies).", "new_string": "The `compile_sim`-for-Prog proof campaign (plan: `docs/RESUME-PROGSIM.md`; see\n[[lowir-compiler-cut]] for the frozen compiler it verifies).\n\n**2026-07-04 re-prioritization** (measured assessment in\n`docs/PROOF-COMPLEXITY.md`, committed 553a490): (1) FIRST prune `lower_sim`'s\nsuperseded tail — `StmtSim.lean:1003` `all_goals sorry` + its `seq` case have\nno consumer, `lower_sim_cf` subsumes them; keep only the straight-line/mem\nleaves it delegates to. (2) while → cref/clen (unstub emitCF/csize) → call →\nPhases 1/2/6. (3) Post-campaign ladder: Prog syntactic frame theorem\n(defs-based `exec_frame` analog, ~120 lines by SSA precedent) BEFORE\n`ProgProof`; then hex0F_correct ported from `CtrlProof.main_loop` as the ONE\nmaintained hex0 proof (the decoder argument is currently proved 3×, RawAsm/\nCtrl/SSA — don't add a 4th sibling); then SSA→Prog lowering sim; then the\nborrow checker over SSA (discharges MemAccOff/execT/Wf hypotheses)."}
```

### 🛠️ **Tool result**  ·  06:20:12; tool 72ms

```
The file /home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  06:20:19; model 10.6s

> 💭 (thinking)
> 

**🔧 tool → Edit**  ·  9ms
```json
{"replace_all": false, "file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/MEMORY.md", "old_string": "- [ProgSim campaign frontier](progsim-campaign-frontier.md) — compile_sim-for-Prog: Phases 0/3 + straight-line/mem/leaf/ife `lower_sim_cf` DONE; remaining sorry = while/cref/clen/call. While-case skeleton + SSA→Prog composability (§7.6, iterWhile) recorded in RESUME-PROGSIM 2026-07-03.", "new_string": "- [ProgSim campaign frontier](progsim-campaign-frontier.md) — compile_sim-for-Prog: Phases 0/3 + straight-line/mem/leaf/ife `lower_sim_cf` DONE; remaining sorry = while/cref/clen/call. 2026-07-04 re-prioritization in docs/PROOF-COMPLEXITY.md: prune StmtSim:1003 dead tail first; post-campaign = Prog frame theorem → hex0F at Prog (the ONE hex0 proof) → SSA→Prog lowering → borrow checker."}
```

### 🛠️ **Tool result**  ·  06:20:23; tool 9ms

⚠️ error

```
<tool_use_error>File has not been read yet. Read it first before writing to it.</tool_use_error>
```

### 🤖 **Assistant**  ·  06:20:29; model 5.7s

**🔧 tool → Bash**  ·  80ms
```json
{"command": "grep -n 'progsim-campaign-frontier' /home/<user>/.claude/projects/-var-data-bootstrap/memory/MEMORY.md", "description": "Find the progsim index line in MEMORY.md"}
```

### 🛠️ **Tool result**  ·  06:20:29; tool 80ms

```
15:- [LowIR compiler cut](lowir-compiler-cut.md) — DONE 2026-07-02: D7/D8 Prog IR + -O0 compiler to RV64I + 16 differential theorems, in default build (LowIRCompile). Verification campaign now UNDERWAY — see [[progsim-campaign-frontier]] (P1 is the frame-padding ORACLE, not two-stack; Phase 0.1 + Defs.lean done). ProgLib.lean (8ba9a8f): strlen/strtoull/hex0/hex1 as Prog functions + main driver, spec-validated (coreSpec/coreSpec1/conf) AND differential — the pre-verification baseline; QEMU smoke test green: IL ≡ Lean-model ≡ QEMU. Ext. 12 DONE: Program{env,data}, cref/clen const slices, cref = jal-pc-read (NO auipc); cmain drives the library off rodata.
16:- [ProgSim campaign frontier](progsim-campaign-frontier.md) — compile_sim-for-Prog: Phases 0/3 + straight-line/mem/leaf/ife `lower_sim_cf` DONE; remaining sorry = while/cref/clen/call. While-case skeleton + SSA→Prog composability (§7.6, iterWhile) recorded in RESUME-PROGSIM 2026-07-03.
```

### 🤖 **Assistant**  ·  06:20:34; model 4.9s

**🔧 tool → Read**  ·  12ms
```json
{"file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/MEMORY.md", "offset": 14, "limit": 5}
```

### 🛠️ **Tool result**  ·  06:20:34; tool 12ms

```
14	- [hex1 campaign frontier](hex1-campaign-frontier.md) — CAMPAIGN COMPLETE: core1_refines proved in BOTH Lean and Coq (func-ext only); docs/TCB.md updated. docs/archive/RESUME-HEX1.md has the wrap-up.
15	- [LowIR compiler cut](lowir-compiler-cut.md) — DONE 2026-07-02: D7/D8 Prog IR + -O0 compiler to RV64I + 16 differential theorems, in default build (LowIRCompile). Verification campaign now UNDERWAY — see [[progsim-campaign-frontier]] (P1 is the frame-padding ORACLE, not two-stack; Phase 0.1 + Defs.lean done). ProgLib.lean (8ba9a8f): strlen/strtoull/hex0/hex1 as Prog functions + main driver, spec-validated (coreSpec/coreSpec1/conf) AND differential — the pre-verification baseline; QEMU smoke test green: IL ≡ Lean-model ≡ QEMU. Ext. 12 DONE: Program{env,data}, cref/clen const slices, cref = jal-pc-read (NO auipc); cmain drives the library off rodata.
16	- [ProgSim campaign frontier](progsim-campaign-frontier.md) — compile_sim-for-Prog: Phases 0/3 + straight-line/mem/leaf/ife `lower_sim_cf` DONE; remaining sorry = while/cref/clen/call. While-case skeleton + SSA→Prog composability (§7.6, iterWhile) recorded in RESUME-PROGSIM 2026-07-03.
17	- [SSA hex0 campaign frontier](ssa-hex0-campaign-frontier.md) — hex0-on-LowIRSSA: COMPLETE incl. §8 rebind-in-env `while` rework (`iterWhile`, 2026-07-03); `hex0S_correct` axiom-clean no-native_decide; P2 frame-theorem win confirmed; reusable SSA-proof recipe + Mathlib-free gotchas.
18	
```

### 🤖 **Assistant**  ·  06:20:41; model 6.9s

**🔧 tool → Edit**  ·  66ms
```json
{"replace_all": false, "file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/MEMORY.md", "old_string": "- [ProgSim campaign frontier](progsim-campaign-frontier.md) — compile_sim-for-Prog: Phases 0/3 + straight-line/mem/leaf/ife `lower_sim_cf` DONE; remaining sorry = while/cref/clen/call. While-case skeleton + SSA→Prog composability (§7.6, iterWhile) recorded in RESUME-PROGSIM 2026-07-03.", "new_string": "- [ProgSim campaign frontier](progsim-campaign-frontier.md) — compile_sim-for-Prog: Phases 0/3 + straight-line/mem/leaf/ife `lower_sim_cf` DONE; remaining sorry = while/cref/clen/call. 2026-07-04 re-prioritization in docs/PROOF-COMPLEXITY.md: prune StmtSim:1003 dead tail first; post-campaign = Prog frame theorem → hex0F at Prog (the ONE hex0 proof) → SSA→Prog lowering → borrow checker."}
```

### 🛠️ **Tool result**  ·  06:20:41; tool 66ms

```
The file /home/<user>/.claude/projects/-var-data-bootstrap/memory/MEMORY.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  06:20:52; model 10.8s

Done. The docs now carry the re-assessed plan, committed as `553a490`:

**New: `docs/PROOF-COMPLEXITY.md`** (indexed under Analyses in `docs/README.md`) — the measured assessment: where the 27k lines go (flat-PC = 48% for two programs), the four findings (F1 flat-PC scales super-linearly and is retired by `compile_sim`; F2 the hex0 decoder argument is proved 3× heading for 4× — hence one-proof-per-program policy; F3 the three IL factoring leaks including Ctrl's missing frame theorem; F4 `lower_sim`'s superseded tail at `StmtSim.lean:1003`), and the §3 priority ladder: close `compile_sim` → Prog frame theorem + Prog-altitude program proofs → SSA→Prog lowering sim → borrow checker over SSA, plus the hygiene backlog.

**`docs/RESUME-PROGSIM.md`** — the NEXT section is rewritten to the re-assessed order (prune the dead tail first, then while → cref/clen → call → Phases 1/2/6, with the frame-theorem-before-ProgProof instruction), and the cold-start checklist at the bottom updated to match (ife marked done, the post-campaign ladder appended).

**`docs/LOWIR-SSA-EXPERIMENT.md`** — the graduation trigger now records that (a) is done and (b)/(c) are scheduled as the post-campaign rung, pointing at the ladder.

`./docs/check_docs.py` passes (42 files, links/index integrity green). I also updated the ProgSim memory file and its index line so future sessions start from the new priorities rather than the 07-03 ordering.
