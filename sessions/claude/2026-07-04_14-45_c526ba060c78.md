# Claude session `c526ba06-0c78-435e-a640-68b61e931643`

- **Agent:** claude
- **Model(s):** claude-fable-5
- **Started:** 2026-07-04 14:45:07Z  |  **Last activity:** 2026-07-04 15:03:11Z
- **Turns:** 5 human / 26 assistant  |  **Tool calls:** 35
- **Source:** `/home/<user>/.claude/projects/-var-data-bootstrap/c526ba06-0c78-435e-a640-68b61e931643.jsonl`

## Token cost

- output: **60,219**  |  fresh input: 7,659  |  cache write: 134,015  |  cache read: 2,489,637
- **cost: **$7.25**** (priced as `anthropic/claude-fable-5` — $10/$50 in/out per Mtok, $1 cache-read, $12.5 cache-write)

## Timing

- wall-clock: **18m03s**  ·  active (model+tool): **16m00s**  ·  waiting for user: 2m03s
- model generation: 15m51s  ·  tool execution: 9.1s
- tool time by name: `Bash` 5.3s/15, `Write` 3.2s/1, `Read` 357ms/14, `Edit` 253ms/5
- slowest calls: `Write` 3.2s, `Bash` 1.4s, `Bash` 1.4s

## Summary

**First request:**

> read context from @docs/README.md I have a question: to formally verify "call", can we do it inductively? can we use the theorem for the evaluator itself to be used for the call?

**Final response:**

> Done — the detailed Phase-5 plan is written, indexed, and committed as `9d997f4`.
> 
> **What landed:**
> - **`docs/RESUME-CALL.md`** — the full handoff for the `call` case, grounded in the code (exact `exec` call equation, compiler prologue/epilogue/marshalling, `resolveOne`'s `callf`, the current theorem shape). It opens with the answer to your question: yes, the proof is inductive — `exec` runs the callee body at `fuel` from `fuel+1`, so the fuel IH *is* the simulation theorem for the callee body, and recursion comes free.
> - `docs/README.md` gained the index line; `RESUME-PROGSIM.md`'s NEXT item 
> 
> … [truncated 1640 chars]

---

## Transcript

### 👤 **User**  ·  14:45:07

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### 👤 **User**  ·  14:45:07

<command-name>/model</command-name>
            <command-message>model</command-message>
            <command-args></command-args>

### 👤 **User**  ·  14:45:07

<local-command-stdout>Set model to [1mFable 5[22m and saved as your default for new sessions</local-command-stdout>

### 👤 **User**  ·  14:45:08; waited 1.1s

read context from @docs/README.md I have a question: to formally verify "call", can we do it inductively? can we use the theorem for the evaluator itself to be used for the call?

### 🤖 **Assistant**  ·  14:45:16; model 8.1s

> 💭 (thinking)
> 

I'll read the active handoff and the relevant Lean code to answer this properly.

**🔧 tool → Read**  ·  145ms
```json
{"file_path": "/var/data/bootstrap/docs/RESUME-PROGSIM.md"}
```

### 🛠️ **Tool result**  ·  14:45:17; tool 145ms

```
1	# RESUME — proving `compile_sim` for Prog (D7/D8 compiler correctness)
2	
3	Plan written 2026-07-02, immediately after the executable cut landed. Read with
4	[LOWIR-DESIGN.md](LOWIR-DESIGN.md) (D7/D8, Ext. 12, §4 pass decomposition),
5	[PROGRESS.md](PROGRESS.md) (what exists), [archive/RESUME-LOWIR.md](archive/RESUME-LOWIR.md)
6	(the hex0-era proof toolbox and gotchas — much of it ports).
7	
8	**Status (updated 2026-07-04).** Phase 0 done, relation validated, and the
9	statement-level simulation `lower_sim_cf` now covers EVERY control-flow
10	construct AND both const-data ops — skip/annot/seq/block/ife/**while**, all six
11	arith + four mem ops, the three leaf jumps ret/brkB/contL, and **cref/clen**.
12	The ONLY remaining statement-level `sorry` is the **`call`** case (Phase 5).
13	`lower_sim` was pruned to leaf-only and is axiom-clean (`[propext, Quot.sound]`).
14	The const-data machinery (all `[propext, Quot.sound]`):
15	- **`run_synth`** (StmtSim) — the 3-instr `synthConst t v` sequence materialises
16	  `ofInt 64 v` into any scratch `t` (≠0,2), IL state preserved, plus an
17	  other-registers-preserved clause; `signExtend_ofInt_12` + `synth_val`
18	  (`(ofInt hi)<<<12 + ofInt lo = ofInt (hi*4096+lo)`) are the reusable atoms.
19	- **`run_cref`** (StmtSim) — the 5-instr cref sequence (`jal T0,+4` pc-read →
20	  synth δ into T1 via `run_synth`, T0 preserved → `add T0,T0,T1`) loads
21	  `codeBase + dpos(d)` into T0; `dp`/`here < 2²⁰` (blob fits) give the synth range.
22	- **`emitCF`/`csize`/`emitCF_length`** gained a `(dat : Data) (dpos : Name → Nat)`
23	  context + real clen/cref arms; `matchesReal` validates BOTH against the real
24	  `Compile.lower ▸ resolveOne` pipeline (8 new #guards: clen lengths/rd=0/seq,
25	  cref offsets/rd=0/seq-of-two).
26	- **`lower_sim_cf`** gained flat hypotheses `hdat` (data lengths synth-safe),
27	  `hdbase` (IL `dbase d` = the machine layout address `codeBase + dpos d`) and
28	  `hdpos` (`dpos d < 2²⁰`). clen = `run_synth`→`run_store`; cref = `run_cref`→
29	  `run_store`. NOTE: `hdbase`/`hdpos`/`hdat` are the Phase-2 obligations the real
30	  layout must discharge (`dpos := segStart + dataOffsetsFrom`; `dbaseOf` = it).
31	Concretely (older entries retained below):
32	- **Phase 0.1 (P1 oracle) — DONE.** `pad : Name → Nat` in `frameEnter`/`exec`/
33	  `run` (default `fun _ => 0`, everything re-greens); `CompileTests.lean` has
34	  the `pad = userOff` frame-memory differential tests validating P1. Committed.
35	- **Phase 0.2 (unfolders) — DONE.** `ProgSim/ExecFacts.lean`: 42 one-layer
36	  `exec_*` lemmas in `namespace LowIR.Prog`. Committed.
37	- **Phase 0.3 (execT + erasure) — DONE.** `execT`/`execT_map_exec`/`execT_erase`
38	  in `ProgSim/Defs.lean`, axioms `[propext, Quot.sound]`. Committed.
39	- **Defs (§3 statements) — DRAFTED.** `Layout`/`Installed`/`StInv`/`SimPre`/
40	  `userPad` real, `#guard`'d; `prog_sim` is the standing `sorry` (Phase 4/5/6
41	  target). `lean_lib LowIRProgSim` (root `LowIR.ProgSim.Defs`) in
42	  `defaultTargets`, builds in ~2.6 s.
43	- **Phase 3 (start) — WordMem DONE.** `ProgSim/WordMem.lean`: the 64-bit LE
44	  load/store algebra (`byte_bit`, `loadWord_storeWord_same` round-trip,
45	  `storeWord_mem_of_ne`/`_outside`, `loadWord_storeWord_disjoint`), sub-second.
46	  Committed `f513c27`; **axiom hygiene retrofit** since — `byte_bit`/round-trip
47	  now `[propext, Quot.sound]` (were pulling `Classical.choice`): the omega-on-
48	  iff/∧/∨ goals are split to atoms first (`constructor`/`repeat' apply And.intro`
49	  / `exfalso`), the window tiling is the new explicit constructive `window_tiling`
50	  (never `omega` on the disjunction), and `by_cases`→`cases Nat.decLe`.
51	- **Phase 3 (finish) — SlotFacts DONE.** `ProgSim/SlotFacts.lean`: the StInv
52	  slot algebra, `[propext, Quot.sound]`. Slot arithmetic (`slotOff_add8_le_userOff`,
53	  `slotOff_disjoint`, `slotAddr_toNat`), slot read/write lifted from WordMem
54	  (`loadWord_store_slot_same`/`_ne`), blob preservat

… [truncated 39912 chars]
```

### 🤖 **Assistant**  ·  14:45:40; model 24.3s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  164ms
```json
{"command": "grep -n \"call\" /var/data/bootstrap/lean/LowIR/Prog.lean | head -40", "description": "Find call semantics in Prog.lean"}
```

### 🛠️ **Tool result**  ·  14:45:41; tool 164ms

```
5:  D7 (calls): a program is an `Env : Name → FunDef`. A call runs the callee's
7:  to the caller's argument values; on return, only the declared return registers
8:  are copied back into the caller's file. Call sites carry their own arities
16:  restores the caller's `sp` structurally. No `alloca`.
35:/-- Structured statements. Ctrl's constructs minus its toy `call (g : Stmt)`,
36:    plus `ld`/`sd`/`annot` and the D7 named call. -/
57:  | call   (argc rvc : Nat) (f : Name)
63:/-- A function: declared parameter/return registers (callee-side names), a
87:/-- Data-free view (keeps pre-data call sites unchanged). -/
91:    semantic stack pointer — no instruction writes `sp`; only call/return move it. -/
129:/-- Build the callee's entry state (D7/D8): check stack overflow against
132:    callee `sp` dropped by `frameSize` PLUS the padding oracle `pad`, caller's
139:    `compile_sim`, IL `sp` coincides with the machine `x2` at every call depth
157:    shared constructs; every recursive call is at `fuel` from `fuel+1`.
159:    `call`: lookup + arity check (`wf` guarantees them; dynamic `none` keeps the
163:    the callee's memory kept. -/
222:    | .call argc rvc f args rets =>
229:            | some callee =>
230:              match exec P dbase pad stackLo fuel fd.body callee with
235:                          { s with mem := s1.mem },              -- caller regs+sp, callee mem
241:/-- Well-formedness (Ext. 8): brk/cont indices in scope, shifts `< 64`, call
252:  | _,  _,  .call argc rvc f _ _ =>
407:/-- Top-level entry: install the data objects at `dataBase`, then call `f`
409:    Returns the callee's final state (read results from `f`'s `rets`). -/
454:/-- `caller (v) = frameLocal(v+1) + 1`, i.e. v+2 — a two-function call. -/
455:def caller : FunDef :=
459:            .seq (.call 1 1 "frameLocal" #v[11] #v[12]) <|   -- x12 := frameLocal(x11)
476:   ("caller", caller), ("early", early), ("hog", hog),
479:                   body := .call 0 0 "hog" #v[] #v[] })]
500:-- two-function call: caller(5) = 7; callee's regs must NOT leak (x11 stays caller's v+1)
501:#guard (testRun "caller" [5]).map (fun s => ((s.rget 10).toNat, (s.rget 11).toNat))
504:-- callee frame is BELOW caller's sp; caller's sp is restored after the call:
505:-- run "caller" and check final sp = entry sp (sp0 - caller.frameSize = sp0)
506:#guard (testRun "caller" [5]).map (·.sp) = some SP0
516:-- stack overflow: hog's frame doesn't fit → none (directly and through a call)
520:-- unknown function / bad arity at a call site → none
522:#guard (run [("bad", { caller with body := .call 2 1 "frameLocal" #v[10, 11] #v[12] }),
526:/-! Nested calls, 3 deep: f3(v) = f2(v)+1 = (f1(v)+1)+1 = v+3, each with a
528:def chainFn (callee : Option Name) : FunDef :=
532:      match callee with
```

### 🤖 **Assistant**  ·  14:45:42; model 908ms

**🔧 tool → Bash**  ·  79ms
```json
{"command": "grep -n \"theorem lower_sim_cf\\|induction fuel\\|call\\|sorry\" /var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean | head -40", "description": "Locate lower_sim_cf statement, induction, call case"}
```

### 🛠️ **Tool result**  ·  14:45:42; tool 79ms

```
252:theorem lower_sim_cf
279:  induction fuel generalizing stmt s s' oc m here brkPos contPos with
886:    case call argc rvc f args rets => sorry
```

### 🤖 **Assistant**  ·  14:45:47; model 5.1s

**🔧 tool → Read**  ·  14ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/Prog.lean", "offset": 150, "limit": 95}
```

### 🛠️ **Tool result**  ·  14:45:47; tool 14ms

```
150	      (fd.params.toList.zip argVals).foldl
151	        (fun rf pv => fun r => if r = pv.1 then pv.2 else rf r) (fun _ => 0)
152	    some { regs := fun r => if r = fd.frameReg then frameBase else withParams r
153	           mem  := mem
154	           sp   := newSp }
155	
156	/-- Clocked big-step semantics with outcomes. Ctrl's equations verbatim for the
157	    shared constructs; every recursive call is at `fuel` from `fuel+1`.
158	
159	    `call`: lookup + arity check (`wf` guarantees them; dynamic `none` keeps the
160	    semantics total), enter the frame, run the body accepting `normal`/`ret`
161	    (an escaping brk/cont is `none` — `wf` bans it), then return to the CALLER's
162	    registers and `sp` (structural restore), with only `rets` copied back and
163	    the callee's memory kept. -/
164	def exec (P : Program) (dbase : Name → Option Word) (pad : Name → Nat)
165	    (stackLo : Word) :
166	    Nat → Stmt → St → Option (St × Outcome)
167	  | 0,      _,    _ => none
168	  | fuel+1, stmt, s =>
169	    match stmt with
170	    | .skip            => some (s, .normal)
171	    | .annot _         => some (s, .normal)
172	    | .cref rd d       =>
173	        match dbase d with
174	        | some a => some (s.rset rd a, .normal)
175	        | none   => none
176	    | .clen rd d       =>
177	        match List.lookup d P.data with
178	        | some bs => some (s.rset rd (BitVec.ofNat 64 bs.length), .normal)
179	        | none    => none
180	    | .addi rd rs imm  => some (s.rset rd (s.rget rs + imm.signExtend 64), .normal)
181	    | .add  rd r1 r2   => some (s.rset rd (s.rget r1 + s.rget r2), .normal)
182	    | .sub  rd r1 r2   => some (s.rset rd (s.rget r1 - s.rget r2), .normal)
183	    | .orr  rd r1 r2   => some (s.rset rd (s.rget r1 ||| s.rget r2), .normal)
184	    | .slli rd rs sh   => some (s.rset rd (s.rget rs <<< sh), .normal)
185	    | .srli rd rs sh   => some (s.rset rd (s.rget rs >>> sh), .normal)
186	    | .lbu  rd rs imm  => let a := s.rget rs + imm.signExtend 64
187	                          some (s.rset rd ((s.loadByte a).setWidth 64), .normal)
188	    | .sb   rb rv imm  => let a := s.rget rb + imm.signExtend 64
189	                          some (s.storeByte a ((s.rget rv).setWidth 8), .normal)
190	    | .ld   rd rs imm  => let a := s.rget rs + imm.signExtend 64
191	                          some (s.rset rd (s.loadWord a), .normal)
192	    | .sd   rb rv imm  => let a := s.rget rb + imm.signExtend 64
193	                          some (s.storeWord a (s.rget rv), .normal)
194	    | .ife c a b t e   =>
195	        if evalCond c (s.rget a) (s.rget b)
196	        then exec P dbase pad stackLo fuel t s else exec P dbase pad stackLo fuel e s
197	    | .seq a b         =>
198	        match exec P dbase pad stackLo fuel a s with
199	        | some (s', .normal) => exec P dbase pad stackLo fuel b s'
200	        | other              => other
201	    | .block body      =>
202	        match exec P dbase pad stackLo fuel body s with
203	        | some (s', .normal)     => some (s', .normal)
204	        | some (s', .brk 0)      => some (s', .normal)
205	        | some (s', .brk (k+1))  => some (s', .brk k)
206	        | some (s', .cont k)     => some (s', .cont k)
207	        | some (s', .ret)        => some (s', .ret)
208	        | none                   => none
209	    | .while c a b body =>
210	        if evalCond c (s.rget a) (s.rget b) then
211	          match exec P dbase pad stackLo fuel body s with
212	          | some (s', .normal)     => exec P dbase pad stackLo fuel (.while c a b body) s'
213	          | some (s', .cont 0)     => exec P dbase pad stackLo fuel (.while c a b body) s'
214	          | some (s', .cont (k+1)) => some (s', .cont k)
215	          | some (s', .brk k)      => some (s', .brk k)
216	          | some (s', .ret)        => some (s', .ret)
217	          | none                   => none
218	        else some (s, .normal)
219	    | .brkB k          => some (s, .brk k)
220	    | .contL k         =>

… [truncated 1362 chars]
```

### 🤖 **Assistant**  ·  14:45:48; model 658ms

**🔧 tool → Read**  ·  19ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean", "offset": 200, "limit": 90}
```

### 🛠️ **Tool result**  ·  14:45:48; tool 19ms

```
200	    `+8`, always fine. A structural side condition, discharged per-program by the
201	    real layout (branches that don't fit make `Compile` return `none`). -/
202	def BranchOk : PStmt → Prop
203	  | .seq a b          => BranchOk a ∧ BranchOk b
204	  | .block body       => BranchOk body
205	  | .ife _ _ _ t e    => 8 + 4 * csize e < 2 ^ 12 ∧ BranchOk t ∧ BranchOk e
206	  | .while _ _ _ body => BranchOk body
207	  | _                 => True
208	
209	/-- A conditional branch `condInstr c T0 T1 δ` whose registers hold `x`, `y`: when
210	    `evalCond c x y` holds the branch is taken (`pc += δ`), matching the compiler's
211	    positive-form encoding (`.ge`/`.geu` take the *else* arm of `bge`/`bgeu`). -/
212	theorem cond_taken (m : State) (c : Cond) (δ : Int) (x y : Word)
213	    (hd : decode (fetch32 m) = condInstr c T0 T1 δ)
214	    (hx : m.rget T0 = x) (hy : m.rget T1 = y) (hev : evalCond c x y = true) :
215	    step m = m.setPc (m.pc + (BitVec.ofInt 13 δ).signExtend 64) := by
216	  cases c <;> simp only [condInstr] at hd
217	  · have h : x = y := by simpa [evalCond] using hev
218	    rw [step_beq m T0 T1 _ hd, hx, hy, if_pos h]
219	  · have h : x.slt y = true := by simpa [evalCond] using hev
220	    rw [step_blt m T0 T1 _ hd, hx, hy, if_pos h]
221	  · have h : x.slt y = false := by simpa [evalCond] using hev
222	    rw [step_bge m T0 T1 _ hd, hx, hy, if_neg (by simp [h])]
223	  · have h : x.ult y = false := by simpa [evalCond] using hev
224	    rw [step_bgeu m T0 T1 _ hd, hx, hy, if_neg (by simp [h])]
225	
226	/-- …and when `evalCond c x y` fails the branch falls through (`pc += 4`). -/
227	theorem cond_not_taken (m : State) (c : Cond) (δ : Int) (x y : Word)
228	    (hd : decode (fetch32 m) = condInstr c T0 T1 δ)
229	    (hx : m.rget T0 = x) (hy : m.rget T1 = y) (hev : evalCond c x y = false) :
230	    step m = m.setPc (m.pc + 4) := by
231	  cases c <;> simp only [condInstr] at hd
232	  · have h : ¬ x = y := by simpa [evalCond] using hev
233	    rw [step_beq m T0 T1 _ hd, hx, hy, if_neg h]
234	  · have h : x.slt y = false := by simpa [evalCond] using hev
235	    rw [step_blt m T0 T1 _ hd, hx, hy, if_neg (by simp [h])]
236	  · have h : x.slt y = true := by simpa [evalCond] using hev
237	    rw [step_bge m T0 T1 _ hd, hx, hy, if_pos h]
238	  · have h : x.ult y = true := by simpa [evalCond] using hev
239	    rw [step_bgeu m T0 T1 _ hd, hx, hy, if_pos h]
240	
241	/-- A `getD` into an all-bounded list is bounded (the default `0` is too). Used to
242	    keep `brkB`/`contL`'s jump target inside the offset window. -/
243	theorem getD_lt (l : List Nat) (k b : Nat) (hb : 0 < b) (h : ∀ p ∈ l, p < b) :
244	    l.getD k 0 < b := by
245	  induction l generalizing k with
246	  | nil => simpa using hb
247	  | cons x t ih =>
248	    cases k with
249	    | zero => simpa using h x (by simp)
250	    | succ k => simpa using ih k (fun p hp => h p (by simp [hp]))
251	
252	theorem lower_sim_cf
253	    {P : Program} {dbase : Name → Option Word} {pad : Name → Nat} {stackLo : Word}
254	    {L : Layout} {fd : FunDef} {holes : List Hole} {epiPos : Nat} {dpos : Name → Nat}
255	    (fuel : Nat) (stmt : PStmt) (s s' : St) (oc : Outcome) (m : State)
256	    (here : Nat) (brkPos contPos : List Nat)
257	    (hexec : LowIR.Prog.exec P dbase pad stackLo fuel stmt s = some (s', oc))
258	    (hinv  : StInv L fd holes s m)
259	    (hpc   : m.pc = L.codeBase + BitVec.ofNat 64 here)
260	    (hem   : Emitted L here (emitCF P.data dpos brkPos contPos epiPos here stmt))
261	    (hreg  : maxRegS stmt ≤ maxRegF fd)
262	    (hnw   : s.sp.toNat + userOff fd ≤ 2 ^ 64)
263	    (hbd   : L.codeBase.toNat + L.blobLen ≤ s.sp.toNat
264	               ∨ s.sp.toNat + userOff fd ≤ L.codeBase.toNat)
265	    (haccess : MemAccOff L holes P dbase pad stackLo fuel stmt s)
266	    (hlbl  : LabelsOk brkPos contPos epiPos)
267	    (hbnd  : here + 4 * csize stmt < 2 ^ 20)
268	    (hbr   : BranchOk stmt)
269	    (hframe : userOff fd ≤ 2000)
270	    (hseg  : 4 * L.instrs.length ≤ L.segSta

… [truncated 1137 chars]
```

### 🤖 **Assistant**  ·  14:46:18; model 32.0s

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  99ms
```json
{"command": "grep -n \"Hole\\|holes\" /var/data/bootstrap/lean/LowIR/ProgSim/Defs.lean | head -30; echo ---; grep -n \"def StInv\\|structure Hole\\|MachPriv\" /var/data/bootstrap/lean/LowIR/ProgSim/Defs.lean", "description": "Check Hole/StInv/MachPriv definitions"}
```

### 🛠️ **Tool result**  ·  14:46:20; tool 99ms

```
109:    pad holes). `execT_erase` says the instrumentation is observationally
347:abbrev Hole := Word × Nat
352:/-- `MachPriv L holes a` — `a` is machine-private: inside the code+data blob, or
355:def MachPriv (L : Layout) (holes : List Hole) (a : Word) : Prop :=
356:  memRange a L.codeBase L.blobLen ∨ ∃ h ∈ holes, memRange a h.1 h.2
357:def machPrivB (L : Layout) (holes : List Hole) (a : Word) : Bool :=
358:  memRangeB a L.codeBase L.blobLen || holes.any (fun h => memRangeB a h.1 h.2)
367:/-- **`StInv L fd holes s m`** — the per-statement simulation invariant
373:def StInv (L : Layout) (fd : FunDef) (holes : List Hole) (s : St) (m : State) : Prop :=
378:  ∧ (∀ a, ¬ MachPriv L holes a → s.mem a = m.mem a)
379:  ∧ holes.head? = some (s.sp, userOff fd)
487:private def f3hole : Hole := (f3sp, userOff f3fd)                        -- [ra][slots]
489:-- a byte inside the code blob is machine-private (holes irrelevant)
---
19:  relation `StInv`/`MachPriv`/`SimPre` and the `lower_sim`/`call_sim`/`prog_sim`
107:    `lower_sim` footprint side condition is then `ws.all (¬ MachPriv ·)` — no
330:/-! ## §3.1 — the simulation relation: `MachPriv`, `StInv`, `SimPre`.
352:/-- `MachPriv L holes a` — `a` is machine-private: inside the code+data blob, or
355:def MachPriv (L : Layout) (holes : List Hole) (a : Word) : Prop :=
363:    per-hole `MachPriv` is the finer invariant `StInv` carries mid-run). -/
373:def StInv (L : Layout) (fd : FunDef) (holes : List Hole) (s : St) (m : State) : Prop :=
378:  ∧ (∀ a, ¬ MachPriv L holes a → s.mem a = m.mem a)
422:    (hmem   : ∀ a, ¬ MachPriv L [] a →
475:/-! ### `MachPriv` classification arithmetic (validated on the chain layout). -/
```

### 🤖 **Assistant**  ·  14:47:09; model 49.0s

> 💭 (thinking)
> 

Yes — and that's precisely why `lower_sim_cf` inducts on **fuel** rather than on statement structure. The design already anticipates this; what's left is making the induction hypothesis actually applicable at the call boundary. Let me lay it out.

## Why the fuel IH is the theorem for the evaluator

`exec`'s call equation (`Prog.lean:222-238`) runs the callee's body at `fuel` from `fuel+1`:

```lean
match exec P dbase pad stackLo fuel fd.body callee with
```

The induction in `lower_sim_cf` (`CtrlSim.lean:279`) is `induction fuel`, so in the `call` case the IH *is* the full simulation theorem at clock `fuel` — for **any** statement, including the callee's entire body. You can't invoke the closed theorem inside its own proof, but the fuel IH is exactly "the theorem for the evaluator itself," and the fuel decrement at the call is what makes that well-founded. This is the same move the `while` case already made (back-edge → fuel IH on the same term), and it handles recursive functions for free — a recursive call is just another body entry at a smaller clock, no separate treatment (that's the note in the plan closing policy C5).

## The catch: the current `generalizing` clause is too narrow

Line 279 generalizes `stmt s s' oc m here brkPos contPos` — but **`fd`, `holes`, and `epiPos` are fixed** across the induction, and all three change at a call boundary:

- the callee has its own `FunDef` (different `maxRegF`, `userOff`),
- its own epilogue position (and empty `brkPos`/`contPos` — `wf` bans escaping brk/cont, and `exec` maps the body's `.normal`/`.ret` both to a return),
- the holes list grows: `StInv` pins `holes.head? = some (s.sp, userOff fd)`, so the callee's invariant is `StInv L fd' ((calleeSp, userOff fd') :: holes) …` with the caller's hole pushed underneath. That growing list *is* the CallChain from the plan.

So step one of Phase 5 is mechanical: add `fd holes epiPos` to the `generalizing` list and re-derive the fd/epiPos-dependent hypotheses (`hreg`, `hnw`, `hframe`, `hlbl`, `hbnd`) for the callee instance. Everything global (`L`, `P`, `dbase`, `pad`, `dpos`, `hdat`/`hdbase`/`hdpos`) stays fixed, which is fine.

## What the IH does *not* give you — the actual Phase-5 content

The IH covers exactly the body-execution middle. Around it you still need:

1. **A function-table hypothesis** (the analogue of `hdat`/`hdbase`/`hdpos` you just added for cref/clen): `∀ f fd, List.lookup f P.env = some fd →` the callee's prologue/body/epilogue are `Emitted` at a known position, together with the callee's own side conditions (`maxRegS fd.body ≤ maxRegF fd`, `BranchOk`, the 2²⁰ bound, `userOff fd ≤ 2000`, `wf`). It must quantify over **all** functions in the env — the callee can itself call — and it becomes a Phase-2 obligation discharged from `layout`/`fnTab`, exactly like the data hypotheses.
2. **`prologue_sim`**: marshalling + `jal` + prologue establishes `StInv L fd' (newHole :: holes)` against `frameEnter`'s state — zero-init ↔ the `sd x0` zeroing, param parking ↔ the `withParams` fold (both last-wins).
3. **`epilogue_sim` + caller restore**: the body IH lands you at the callee's epilogue (`.normal` falls through to it, `.ret` jumps to it — they converge, which is why `exec` accepts both). Then rets→a-regs, ra/sp restore, and the caller's `StInv` must be *re-established*: the caller's slots live inside the caller's hole, which was inside the callee's `holes` list, so the callee's slot stores (its own hole) and user stores (constrained by `MemAccOff`) provably didn't touch them. This is the stack-discipline sub-proof and where the plan expects the unknown unknowns.
4. **A `MemAccOff` call arm** — same lesson as `while`: the current stub can't feed the body IH. The call arm must recurse into the body against the *extended* holes list, plus cover the marshalling.

One practical suggestion from the plan worth following: state `call_sim` standalone with the body simulation as an explicit *hypothesis* (a "body simulator" parameter), prove `prologue_sim`/`epilogue_sim` against the differential oracle (`caller`, `chainFn` states in `Prog.lean`) first, then instantiate that hypothesis with the fuel IH inside the induction. That de-risks the summit before touching the big proof term.

So: the inductive structure you're asking about is sound and already built in — the remaining work is the generalization fix plus the prologue/epilogue/stack-discipline plumbing around the IH, not any new induction principle.

### 👤 **User**  ·  14:49:11; waited 2m02s

write down the detailed plan for the proof and handoff

### 🤖 **Assistant**  ·  14:49:26; model 17.6s

> 💭 (thinking)
> 

I'll ground the plan in the actual code first — the compiler's call lowering, `emitCF`'s call arm status, `MemAccOff`, and the current state around the `sorry`.

**🔧 tool → Bash**  ·  78ms
```json
{"command": "grep -n \"call\\|prologue\\|epilogue\\|callf\\|fnTab\" /var/data/bootstrap/lean/LowIR/Compile.lean | head -50", "description": "Find call lowering in Compile.lean"}
```

### 🛠️ **Tool result**  ·  14:49:28; tool 78ms

```
9:  Machine frame (grows down; all offsets sp-relative, sp = x2 after prologue):
19:  across an IL statement except the slots, so calls need no caller-save logic.
21:  IL zero-init is matched EXACTLY: the prologue `sd x0` into every non-param,
25:  Pipeline: lower to `SymInstr` (labels symbolic, calls by name) → lay out the
26:  whole program (all instructions are 4 bytes) → resolve branches/jumps/calls
61:  | callf (f : Name)                           -- jal ra, function
97:    `epi` is this function's epilogue label. `wf` guarantees index ranges and
98:    call arities; out-of-range lookups fall back to label 0 (never emitted for
136:  | .call _ _ f args rets => do
139:      pure <| loads ++ [.callf f] ++ stores
164:  | .call _ _ _ args rets =>
189:def prologue (fd : FunDef) : List SymInstr :=
203:def epilogue (fd : FunDef) : List SymInstr :=
213:  pure <| prologue fd ++ body ++ [.label epi] ++ epilogue fd
277:  | (pos, .callf f)      => do
315:      layout (("", [.callf entry, .ins (jal0 0)]) :: segs) 0
```

### 🤖 **Assistant**  ·  14:49:30; model 1.2s

**🔧 tool → Bash**  ·  75ms
```json
{"command": "grep -n \"call\\|MemAccOff\\|def emitCF\\|def csize\\|def landPos\\|def LabelsOk\" /var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean | head -60", "description": "Locate call-related defs in CtrlSim.lean"}
```

### 🛠️ **Tool result**  ·  14:49:30; tool 75ms

```
38:def csize : PStmt → Nat
54:def emitCF (dat : Data) (dpos : Name → Nat) (brkPos contPos : List Nat) (epiPos : Nat) :
187:def landPos (brkPos contPos : List Nat) (epiPos ft : Nat) : Outcome → Nat
195:def LabelsOk (brkPos contPos : List Nat) (epiPos : Nat) : Prop :=
265:    (haccess : MemAccOff L holes P dbase pad stackLo fuel stmt s)
294:      -- csize/emitCF/MemAccOff for `.block body` reduce to `body` with `lEnd` pushed.
303:      -- `csize`/`emitCF`/`MemAccOff`/`maxRegS` on `.block body` are defeq to the
308:      have hacc' : MemAccOff L holes P dbase pad stackLo fuel body s := by
309:        simpa only [MemAccOff] using haccess
886:    case call argc rvc f args rets => sorry
```

### 🤖 **Assistant**  ·  14:49:36; model 6.0s

**🔧 tool → Read**  ·  16ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/Compile.lean", "offset": 120, "limit": 110}
```

### 🛠️ **Tool result**  ·  14:49:36; tool 16ms

```
120	      let ee ← lower dat brks conts epi e
121	      pure <| loadSlot a T0 ++ loadSlot b T1
122	           ++ [.br c T0 T1 lT] ++ ee ++ [.jmp lEnd, .label lT] ++ et ++ [.label lEnd]
123	  | .while c a b body => do
124	      let lTop ← fresh; let lBody ← fresh; let lEnd ← fresh
125	      let eb ← lower dat brks (lTop :: conts) epi body
126	      pure <| [.label lTop] ++ loadSlot a T0 ++ loadSlot b T1
127	           ++ [.br c T0 T1 lBody, .jmp lEnd, .label lBody]
128	           ++ eb ++ [.jmp lTop, .label lEnd]
129	  | .block body     => do
130	      let lEnd ← fresh
131	      let eb ← lower dat (lEnd :: brks) conts epi body
132	      pure <| eb ++ [.label lEnd]
133	  | .brkB k         => pure [.jmp (brks.getD k 0)]
134	  | .contL k        => pure [.jmp (conts.getD k 0)]
135	  | .ret            => pure [.jmp epi]
136	  | .call _ _ f args rets => do
137	      let loads  := args.toList.zipIdx.flatMap fun ri => loadSlot ri.1 (A ri.2)
138	      let stores := rets.toList.zipIdx.flatMap fun ri => storeSlot ri.1 (A ri.2)
139	      pure <| loads ++ [.callf f] ++ stores
140	  | .cref rd d      => pure <| [.cref d] ++ storeSlot rd T0
141	  | .clen rd d      =>
142	      pure <| synthConst T0 (((List.lookup d dat).map (·.length)).getD 0)
143	           ++ storeSlot rd T0
144	
145	/-! ### Frame accounting -/
146	
147	/-- Largest IL register mentioned in a statement. -/
148	def maxRegS : PStmt → Nat
149	  | .skip | .annot _ | .ret | .brkB _ | .contL _ => 0
150	  | .seq a b          => max (maxRegS a) (maxRegS b)
151	  | .addi rd rs _     => max rd rs
152	  | .add  rd r1 r2    => max rd (max r1 r2)
153	  | .sub  rd r1 r2    => max rd (max r1 r2)
154	  | .orr  rd r1 r2    => max rd (max r1 r2)
155	  | .slli rd rs _     => max rd rs
156	  | .srli rd rs _     => max rd rs
157	  | .lbu  rd rs _     => max rd rs
158	  | .ld   rd rs _     => max rd rs
159	  | .sb   rb rv _     => max rb rv
160	  | .sd   rb rv _     => max rb rv
161	  | .ife _ a b t e    => max (max a b) (max (maxRegS t) (maxRegS e))
162	  | .while _ a b body => max (max a b) (maxRegS body)
163	  | .block body       => maxRegS body
164	  | .call _ _ _ args rets =>
165	      max (args.toList.foldl max 0) (rets.toList.foldl max 0)
166	  | .cref rd _        => rd
167	  | .clen rd _        => rd
168	
169	/-- Largest IL register a function touches (body + declared regs + frameReg). -/
170	def maxRegF (fd : FunDef) : Nat :=
171	  max (maxRegS fd.body) <|
172	  max fd.frameReg <|
173	  max (fd.params.toList.foldl max 0) (fd.rets.toList.foldl max 0)
174	
175	/-- Byte offset of the user frame within the machine frame. -/
176	def userOff (fd : FunDef) : Nat := 8 * (maxRegF fd + 2)
177	
178	/-- Total machine frame size. -/
179	def totalFrame (fd : FunDef) : Nat := userOff fd + fd.frameSize
180	
181	/-- Per-function compilability limits for this first cut: a0..a7 marshalling
182	    and all slot offsets / frame adjustments within imm12. -/
183	def fnOk (fd : FunDef) : Bool :=
184	  fd.argc ≤ 8 && fd.rvc ≤ 8 && totalFrame fd ≤ 2000
185	
186	/-- Prologue: drop sp, save ra, park params from a0.., `sd x0` every other
187	    slot (matches IL zero-init EXACTLY), materialize the user-frame base into
188	    frameReg's slot. -/
189	def prologue (fd : FunDef) : List SymInstr :=
190	  let tf := totalFrame fd
191	  let params := fd.params.toList
192	  [.ins (.addi SP SP (BitVec.ofInt 12 (-(tf : Int)))),
193	   .ins (.sd SP RA 0)]
194	  ++ params.zipIdx.flatMap (fun pi => storeSlot pi.1 (A pi.2))
195	  ++ ((List.range (maxRegF fd + 1)).filter
196	        (fun r => r != 0 && !params.contains r && r != fd.frameReg)).map
197	       (fun r => .ins (.sd SP 0 (BitVec.ofNat 12 (slotOff r))))
198	  ++ (if fd.frameReg = 0 then [] else
199	        [.ins (.addi T0 SP (BitVec.ofNat 12 (userOff fd)))] ++ storeSlot fd.frameReg T0)
200	
201	/-- Epilogue (jumped to by `ret`, fallen into on `normal`): rets → a0..,
202	    restore ra + sp, return. -/
203	def epilogue (fd : FunDef) : List SymInstr :=
204	  fd.rets.toList

… [truncated 1061 chars]
```

### 🤖 **Assistant**  ·  14:49:37; model 876ms

**🔧 tool → Read**  ·  18ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean", "offset": 30, "limit": 170}
```

### 🛠️ **Tool result**  ·  14:49:37; tool 18ms

```
30	local notation "PStmt" => LowIR.Prog.Stmt
31	
32	/-- Instruction count of a statement's lowering — position/label-independent
33	    (every jump/branch is one 4-byte instruction, every label 0 bytes). The
34	    straight-line cases delegate to `emit` (already `#guard`'d faithful to
35	    `Compile.lower`); the control-flow overheads are read off the compiler's
36	    layout: `ife` = 2 slot-loads + branch + jmp (4), `while` = +1 back-edge (5).
37	    `cref`/`clen` are `0` here (placeholder — `emit` still stubs them). -/
38	def csize : PStmt → Nat
39	  | .seq a b        => csize a + csize b
40	  | .ret            => 1
41	  | .brkB _         => 1
42	  | .contL _        => 1
43	  | .block body     => csize body
44	  | .ife _ _ _ t e  => 4 + csize e + csize t
45	  | .while _ _ _ b  => 5 + csize b
46	  | .clen rd _      => 3 + (if rd = 0 then 0 else 1)   -- synthConst (3) + storeSlot
47	  | .cref rd _      => 5 + (if rd = 0 then 0 else 1)   -- pc-read + synth + add + store
48	  | s               => (emit s).length
49	
50	/-- The label-aware lowering. `brkPos`/`contPos` are the enclosing block-end /
51	    loop-top label byte positions (indexed like `brkB`/`contL`'s de-Bruijn
52	    indices); `epiPos` the epilogue; `here` the byte offset of this code.
53	    Straight-line cases reuse `emit` (`here` irrelevant). -/
54	def emitCF (dat : Data) (dpos : Name → Nat) (brkPos contPos : List Nat) (epiPos : Nat) :
55	    Nat → PStmt → List Instr
56	  | here, .seq a b =>
57	      emitCF dat dpos brkPos contPos epiPos here a ++
58	        emitCF dat dpos brkPos contPos epiPos (here + 4 * csize a) b
59	  | here, .ret     => [LowIR.jal0 ((epiPos : Int) - (here : Int))]
60	  | here, .brkB k  => [LowIR.jal0 ((brkPos.getD k 0 : Int) - (here : Int))]
61	  | here, .contL k => [LowIR.jal0 ((contPos.getD k 0 : Int) - (here : Int))]
62	  | here, .block body =>
63	      -- the break target `lEnd` sits just past the body (0-byte label).
64	      emitCF dat dpos ((here + 4 * csize body) :: brkPos) contPos epiPos here body
65	  | here, .ife c a b t e =>
66	      -- br→lT (then) skips over the else block; jmp→lEnd skips over then.
67	      -- offsets are size-relative: lT is 8 + 4|e| past the branch, lEnd 4 + 4|t|
68	      -- past the jmp. else code `e` runs on fall-through (at here+12), then `t`.
69	      loadSlotI a T0 ++ loadSlotI b T1
70	        ++ [LowIR.condInstr c T0 T1 (8 + 4 * csize e)]
71	        ++ emitCF dat dpos brkPos contPos epiPos (here + 12) e
72	        ++ [LowIR.jal0 (4 + 4 * csize t)]
73	        ++ emitCF dat dpos brkPos contPos epiPos (here + 16 + 4 * csize e) t
74	  | here, .while c a b body =>
75	      -- lTop = here (0-byte label); br→lBody (+8) enters, jmp→lEnd exits, the
76	      -- body runs a continue-scope (lTop::contPos), then the back-edge to lTop.
77	      loadSlotI a T0 ++ loadSlotI b T1
78	        ++ [LowIR.condInstr c T0 T1 8, LowIR.jal0 (8 + 4 * csize body)]
79	        ++ emitCF dat dpos brkPos (here :: contPos) epiPos (here + 16) body
80	        ++ [LowIR.jal0 (-(16 + 4 * csize body : Int))]
81	  | _here, .clen rd d =>
82	      -- position-independent: synth the data length into T0, park into rd's slot.
83	      synthI T0 (((List.lookup d dat).map (·.length)).getD 0 : Int) ++ storeSlotI rd T0
84	  | here, .cref rd d =>
85	      -- pc-read (`jal T0,+4`) + synth the delta to `d`'s absolute position into T1
86	      -- + `add T0,T0,T1` ⇒ T0 = data address; park into rd's slot.
87	      crefI T0 T1 ((dpos d : Int) - ((here : Int) + 4)) ++ storeSlotI rd T0
88	  | _, s => emit s
89	
90	/-- `emitCF`'s length is exactly `csize` — position/label-independent, as claimed.
91	    Needed by the outcome-carrying `lower_sim` to place `seq`'s second half and to
92	    read the fall-through position. -/
93	theorem emitCF_length (dat : Data) (dpos : Name → Nat) (bp cp : List Nat) (ep here : Nat)
94	    (stmt : PStmt) :
95	    (emitCF dat dpos bp cp ep here stmt).length = csize stmt := by
96	  induction stmt generalizing bp cp ep here with

… [truncated 5947 chars]
```

### 🤖 **Assistant**  ·  14:49:44; model 8.7s

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  75ms
```json
{"command": "grep -n \"def emit\\b\\|def emit \\|call\\|jalr\" /var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean | head; echo ---; grep -n \"def MemAccOff\" /var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean", "description": "Check emit's call arm and MemAccOff location"}
```

### 🛠️ **Tool result**  ·  14:49:45; tool 75ms

```
59:def emit : PStmt → List Instr
624:    the caller supplies `hC` (the one-instruction step lemma specialised to `C`),
878:    ife/while/block, brk/cont/ret, call, cref/clen) is the domain of the
---
```

### 🤖 **Assistant**  ·  14:49:46; model 906ms

**🔧 tool → Read**  ·  19ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/Compile.lean", "offset": 230, "limit": 110}
```

### 🛠️ **Tool result**  ·  14:49:46; tool 19ms

```
230	      let (flat, lbls, pos') := layoutItems rest pos
231	      ((pos, .label l) :: flat, (l, pos) :: lbls, pos')
232	  | si :: rest, pos =>
233	      let (flat, lbls, pos') := layoutItems rest (pos + symSize si)
234	      ((pos, si) :: flat, lbls, pos')
235	
236	/-- Pass A: walk the whole program, assigning byte positions. Returns the flat
237	    positioned stream, the label→addr map, the function→addr map, and the end
238	    position (total code bytes). -/
239	def layout : List (Name × List SymInstr) →
240	    (start : Nat) →
241	    List (Nat × SymInstr) × List (Nat × Nat) × List (Name × Nat) × Nat
242	  | [], pos => ([], [], [], pos)
243	  | (n, items) :: rest, pos =>
244	      let (flat1, lbls1, pos') := layoutItems items pos
245	      let (flat2, lbls2, fns, endP) := layout rest pos'
246	      (flat1 ++ flat2, lbls1 ++ lbls2, (n, pos) :: fns, endP)
247	
248	/-- Resolve one positioned symbolic instruction (range-checked). `cref d`:
249	    `jal T0, +4` reads the pc (T0 := address of the next instruction — pc-read
250	    with NO auipc, staying inside the 16-encoding surface and keeping the blob
251	    position-independent), then a fixed 3-instr synth of the delta to the data
252	    object into T1, then `add T0, T0, T1`. -/
253	def resolveOne (lbls : List (Nat × Nat)) (fns : List (Name × Nat))
254	    (dats : List (Name × Nat)) :
255	    Nat × SymInstr → Option (List Instr)
256	  | (_,   .label _)      => some []
257	  | (_,   .ins i)        => some [i]
258	  | (pos, .cref d)       => do
259	      let off ← List.lookup d dats
260	      let δ : Int := (off : Int) - ((pos : Int) + 4)
261	      let lo : Int := ((δ + 2048) % 4096) - 2048
262	      let hi : Int := (δ - lo) / 4096
263	      if -2048 ≤ hi && hi ≤ 2047 then
264	        some [.jal T0 (BitVec.ofInt 21 4),
265	              .addi T1 0 (BitVec.ofInt 12 hi), .slli T1 T1 12,
266	              .addi T1 T1 (BitVec.ofInt 12 lo),
267	              .add T0 T0 T1]
268	      else none
269	  | (pos, .br c a b l)   => do
270	      let tgt ← List.lookup l lbls
271	      let δ : Int := (tgt : Int) - (pos : Int)
272	      if -4096 ≤ δ && δ ≤ 4094 then some [condInstr c a b δ] else none
273	  | (pos, .jmp l)        => do
274	      let tgt ← List.lookup l lbls
275	      let δ : Int := (tgt : Int) - (pos : Int)
276	      if -(2^20 : Int) ≤ δ && δ ≤ (2^20 : Int) - 2 then some [jal0 δ] else none
277	  | (pos, .callf f)      => do
278	      let tgt ← List.lookup f fns
279	      let δ : Int := (tgt : Int) - (pos : Int)
280	      if -(2^20 : Int) ≤ δ && δ ≤ (2^20 : Int) - 2
281	      then some [.jal RA (BitVec.ofInt 21 δ)] else none
282	
283	def pad8 (n : Nat) : Nat := (n + 7) / 8 * 8
284	
285	/-- Byte offsets of the data objects, laid out 8-aligned from `start`
286	    (mirrors `Prog.layoutData`'s spacing). -/
287	def dataOffsets (start : Nat) : Data → List (Name × Nat)
288	  | [] => []
289	  | (n, bs) :: rest => (n, start) :: dataOffsets (start + pad8 bs.length) rest
290	
291	/-- The data segment bytes, each object zero-padded to 8. -/
292	def dataBytes : Data → List Byte
293	  | [] => []
294	  | (_, bs) :: rest =>
295	      bs ++ List.replicate (pad8 bs.length - bs.length) 0 ++ dataBytes rest
296	
297	/-- Compile a whole program. The stream is `jal ra, entry` (the 4-byte stub at
298	    offset 0), a `jal x0, 0` self-loop landing pad at offset 4 — the HALT
299	    address, occupied by a real instruction so no function entry can collide
300	    with it — then every function in env order, then (in `progBytes`) the
301	    data segment. Run the result with `runFuel (codeBase+4)`. `none` =
302	    ill-formed program, per-function limits exceeded, missing entry, data too
303	    large, or a branch/jump/cref out of range. The `T` variant also returns
304	    the function and data byte-offset tables. -/
305	def compileProgT (P : Program) (entry : Name) :
306	    Option (List Instr × List (Name × Nat) × List (Name × Nat)) :=
307	  if !(wfProgram P && P.env.all (fun nf => fnOk nf.2)
308	  

… [truncated 1366 chars]
```

### 🤖 **Assistant**  ·  14:49:57; model 11.3s

> 💭 (thinking)
> 

**🔧 tool → Read**  ·  10ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean", "offset": 55, "limit": 50}
```

### 🛠️ **Tool result**  ·  14:49:58; tool 10ms

```
55	/-- The resolved instruction stream for a straight-line statement — mirrors
56	    `Compile.lower` exactly for the label-free cases (validated by `#guard`
57	    against the real pipeline below). Control-flow/`cref`/`clen` are `[]` here;
58	    their `lower_sim` cases are `sorry` (they use the real `Emitted`, not `emit`). -/
59	def emit : PStmt → List Instr
60	  | .skip            => []
61	  | .annot _         => []
62	  | .addi rd rs imm  => loadSlotI rs T0 ++ [.addi T0 T0 imm] ++ storeSlotI rd T0
63	  | .add  rd r1 r2   => loadSlotI r1 T0 ++ loadSlotI r2 T1 ++ [.add T0 T0 T1] ++ storeSlotI rd T0
64	  | .sub  rd r1 r2   => loadSlotI r1 T0 ++ loadSlotI r2 T1 ++ [.sub T0 T0 T1] ++ storeSlotI rd T0
65	  | .orr  rd r1 r2   => loadSlotI r1 T0 ++ loadSlotI r2 T1 ++ [.or  T0 T0 T1] ++ storeSlotI rd T0
66	  | .slli rd rs sh   => loadSlotI rs T0 ++ [.slli T0 T0 sh] ++ storeSlotI rd T0
67	  | .srli rd rs sh   => loadSlotI rs T0 ++ [.srli T0 T0 sh] ++ storeSlotI rd T0
68	  | .lbu  rd rs imm  => loadSlotI rs T0 ++ [.lbu T0 T0 imm] ++ storeSlotI rd T0
69	  | .ld   rd rs imm  => loadSlotI rs T0 ++ [.ld  T0 T0 imm] ++ storeSlotI rd T0
70	  | .sb   rb rv imm  => loadSlotI rb T0 ++ loadSlotI rv T1 ++ [.sb T0 T1 imm]
71	  | .sd   rb rv imm  => loadSlotI rb T0 ++ loadSlotI rv T1 ++ [.sd T0 T1 imm]
72	  | .seq a b         => emit a ++ emit b
73	  | _                => []
74	
75	/-! ## `Emitted` — the static "code segment carries `is` at `pos`" fact. -/
76	
77	/-- `L.instrs` has `is` as its segment starting at byte offset `pos`
78	    (`pos` 4-aligned; each `is[j]` is `L.instrs[pos/4 + j]`). Static in `L`; the
79	    machine fetch is recovered from `Installed` (a `StInv` conjunct). -/
80	def Emitted (L : Layout) (pos : Nat) (is : List Instr) : Prop :=
81	  pos % 4 = 0 ∧ ∀ (j : Nat) (h : j < is.length),
82	    ∃ (h2 : pos / 4 + j < L.instrs.length), L.instrs[pos / 4 + j] = is[j]'h
83	
84	/-- `Emitted` of a concatenation splits: `is₁` at `pos`, `is₂` at `pos + 4|is₁|`. -/
85	theorem Emitted_append_left (L : Layout) (pos : Nat) (is₁ is₂ : List Instr)
86	    (h : Emitted L pos (is₁ ++ is₂)) : Emitted L pos is₁ := by
87	  obtain ⟨ha, hf⟩ := h
88	  refine ⟨ha, fun j hj => ?_⟩
89	  obtain ⟨h2, he⟩ := hf j (by simp only [List.length_append]; omega)
90	  exact ⟨h2, by rw [he, List.getElem_append_left hj]⟩
91	
92	theorem Emitted_append_right (L : Layout) (pos : Nat) (is₁ is₂ : List Instr)
93	    (h : Emitted L pos (is₁ ++ is₂)) : Emitted L (pos + 4 * is₁.length) is₂ := by
94	  obtain ⟨ha, hf⟩ := h
95	  refine ⟨by omega, fun j hj => ?_⟩
96	  obtain ⟨h2, he⟩ := hf (is₁.length + j) (by simp only [List.length_append]; omega)
97	  have hpos : (pos + 4 * is₁.length) / 4 + j = pos / 4 + (is₁.length + j) := by omega
98	  have hb : (pos + 4 * is₁.length) / 4 + j < L.instrs.length := by rw [hpos]; exact h2
99	  refine ⟨hb, ?_⟩
100	  simp only [hpos, he]
101	  simp
102	
103	/-- The fetch bridge: `Emitted` (static) + `Installed` (a `StInv` conjunct,
104	    preserved across steps) ⇒ the machine at `codeBase + (pos + 4j)` decodes to
```

### 🤖 **Assistant**  ·  14:49:59; model 1.8s

**🔧 tool → Bash**  ·  98ms
```json
{"command": "grep -rn \"MemAccOff\" /var/data/bootstrap/lean/LowIR/ProgSim/*.lean | grep \"def \\|where\\|^\\S*:.*MemAccOff :\" | head; echo ---; grep -rn \"step_jalr\\|jalr\" /var/data/bootstrap/lean/LowIR/ProgSim/*.lean | head", "description": "Find MemAccOff def and jalr lemmas"}
```

### 🛠️ **Tool result**  ·  14:50:00; tool 98ms

```
/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean:752:def MemAccOff (L : Layout) (holes : List Hole)
---
```

### 🤖 **Assistant**  ·  14:50:09; model 10.1s

> 💭 (thinking)
> 

**🔧 tool → Read**  ·  8ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean", "offset": 740, "limit": 75}
```

### 🛠️ **Tool result**  ·  14:50:10; tool 8ms

```
740	        loadSlotI_length, loadSlotI_length]
741	    exact pc_congr _ (by simp only [List.length_cons, List.length_nil]; omega)
742	
743	/-! ## `MemAccOff` — the memory-access side condition for `lower_sim`. -/
744	
745	/-- Every byte the statement's IL memory ops touch lies off `MachPriv` (the frame
746	    holes + code blob). This is the read+write generalisation of Defs' footprint
747	    note (`ws.all (¬ MachPriv ·)`) — loads need it for value agreement, stores for
748	    slot/`Installed` preservation. It mirrors `exec`'s recursion for `seq` (the
749	    second statement's addresses depend on the first's result); `True` for the
750	    non-memory and (still-`sorry`) control-flow cases. `lbu`/`sb` touch one byte,
751	    `ld`/`sd` eight; both `n`-byte ranges are pinned via `¬ MachPriv (base + i)`. -/
752	def MemAccOff (L : Layout) (holes : List Hole)
753	    (P : Program) (dbase : Name → Option Word) (pad : Name → Nat) (stackLo : Word) :
754	    Nat → PStmt → St → Prop
755	  | _, .ld _ rs imm, s =>
756	      ∀ i, i < 8 → ¬ MachPriv L holes (s.rget rs + imm.signExtend 64 + BitVec.ofNat 64 i)
757	  | _, .lbu _ rs imm, s => ¬ MachPriv L holes (s.rget rs + imm.signExtend 64)
758	  | _, .sd rb _ imm, s =>
759	      (s.rget rb + imm.signExtend 64).toNat + 8 ≤ 2 ^ 64 ∧
760	        ∀ i, i < 8 → ¬ MachPriv L holes (s.rget rb + imm.signExtend 64 + BitVec.ofNat 64 i)
761	  | _, .sb rb _ imm, s => ¬ MachPriv L holes (s.rget rb + imm.signExtend 64)
762	  | fuel + 1, .seq a b, s =>
763	      MemAccOff L holes P dbase pad stackLo fuel a s ∧
764	        (∀ s1, LowIR.Prog.exec P dbase pad stackLo fuel a s = some (s1, .normal) →
765	           MemAccOff L holes P dbase pad stackLo fuel b s1)
766	  | fuel + 1, .block body, s =>
767	      MemAccOff L holes P dbase pad stackLo fuel body s
768	  | fuel + 1, .ife _ _ _ t e, s =>
769	      MemAccOff L holes P dbase pad stackLo fuel t s ∧
770	        MemAccOff L holes P dbase pad stackLo fuel e s
771	  | fuel + 1, .while c a b body, s =>
772	      -- one unrolling: the body runs at `fuel`, and every iteration that loops
773	      -- back (`.normal`/`.cont 0`) must itself be access-safe — mirrors `exec`'s
774	      -- self-recursion, fuel-structural exactly like `seq`.
775	      MemAccOff L holes P dbase pad stackLo fuel body s ∧
776	        (∀ s', (LowIR.Prog.exec P dbase pad stackLo fuel body s = some (s', .normal) ∨
777	                LowIR.Prog.exec P dbase pad stackLo fuel body s = some (s', .cont 0)) →
778	           MemAccOff L holes P dbase pad stackLo fuel (.while c a b body) s')
779	  | _, _, _ => True
780	
781	/-! ## `jump_sim` — the leaf unconditional jump (the `ret`/`brkB`/`contL` atom).
782	
783	    An unconditional `jal x0, δ` at `codeBase + here` (the resolution of `.jmp l`,
784	    with `δ = target − here`) leaves the IL state untouched (jumps don't mutate
785	    state — `exec` gives `(s, .ret/.brk/.cont)`) and lands the machine at
786	    `codeBase + target`, preserving `StInv`. This is the whole content of the three
787	    leaf control-transfer ops; the outcome-carrying `lower_sim` will invoke it once
788	    the label environment resolves `target` (epilogue / break / continue label).
789	    `δ`'s range is exactly `resolveOne`'s `jmp` guard. -/
790	theorem jump_sim (L : Layout) (fd : FunDef) (holes : List Hole) (s : St) (m : State)
791	    (here tgt : Nat) (δ : Int)
792	    (hinv : StInv L fd holes s m)
793	    (hpc  : m.pc = L.codeBase + BitVec.ofNat 64 here)
794	    (hem  : Emitted L here [Rv64i.Instr.jal 0 (BitVec.ofInt 21 δ)])
795	    (hδ   : δ = (tgt : Int) - (here : Int))
796	    (hlo  : -(2 ^ 20) ≤ δ) (hhi : δ < 2 ^ 20) :
797	    StInv L fd holes s (step m) ∧ (step m).pc = L.codeBase + BitVec.ofNat 64 tgt := by
798	  have hinst : Installed L m := hinv.2.2.1
799	  have hd : decode (fetch32 m) = Rv64i.Instr.jal 0 (BitVec.ofInt 21 δ) := by
800	    have h := decode_at L m m here [Rv64i.Instr.jal 0 (BitVec.ofInt 21 δ)] hem hinst 0
801	                (by simp) (by rw [hp

… [truncated 810 chars]
```

### 🤖 **Assistant**  ·  14:50:11; model 1.4s

**🔧 tool → Read**  ·  28ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/Defs.lean", "offset": 330, "limit": 100}
```

### 🛠️ **Tool result**  ·  14:50:11; tool 28ms

```
330	/-! ## §3.1 — the simulation relation: `MachPriv`, `StInv`, `SimPre`.
331	
332	    P1 (the frame-padding oracle) makes the relation PLAIN EQUALITY: every
333	    IL-visible value is numerically identical on both sides, so `StInv` needs no
334	    injection — only that IL registers live in their machine slots, `sp ≡ x2`,
335	    the code is installed, and memory agrees off the machine-private bytes. -/
336	
337	/-- Half-open byte range `[base, base+len)` in `toNat` (no wrap — all our
338	    addresses sit far below 2⁶⁴). -/
339	def memRange (a base : Word) (len : Nat) : Prop :=
340	  base.toNat ≤ a.toNat ∧ a.toNat < base.toNat + len
341	def memRangeB (a base : Word) (len : Nat) : Bool :=
342	  base.toNat ≤ a.toNat && a.toNat < base.toNat + len
343	
344	/-- A machine-private hole: the `[ra][slots]` area an activation adds BELOW its
345	    user frame — `(base = that activation's sp, len = userOff)`. Listed per live
346	    activation by the `CallChain` ghost (Phase 5). -/
347	abbrev Hole := Word × Nat
348	
349	/-- Total loadable blob length (code + zero-pad + data segment). -/
350	def Layout.blobLen (L : Layout) : Nat := L.segStart + (dataSegment L.data).length
351	
352	/-- `MachPriv L holes a` — `a` is machine-private: inside the code+data blob, or
353	    inside some live activation's `[ra][slots]` hole. The IL never writes these;
354	    the footprint side condition forbids IL stores here. -/
355	def MachPriv (L : Layout) (holes : List Hole) (a : Word) : Prop :=
356	  memRange a L.codeBase L.blobLen ∨ ∃ h ∈ holes, memRange a h.1 h.2
357	def machPrivB (L : Layout) (holes : List Hole) (a : Word) : Bool :=
358	  memRangeB a L.codeBase L.blobLen || holes.any (fun h => memRangeB a h.1 h.2)
359	
360	/-- The whole shared stack region `[stackLo, sp0)` — used by `prog_sim`'s
361	    coarse top-level memory agreement (the harness's output buffers live OUTSIDE
362	    it, so "agree off blob and off stack" is the composable observable; the
363	    per-hole `MachPriv` is the finer invariant `StInv` carries mid-run). -/
364	def MachStack (stackLo sp0 : Word) (a : Word) : Prop :=
365	  memRange a stackLo (sp0.toNat - stackLo.toNat)
366	
367	/-- **`StInv L fd holes s m`** — the per-statement simulation invariant
368	    (RESUME-PROGSIM §3.1). At every IL statement boundary during `fd`'s body:
369	    `sp ≡ x2`; each live IL register sits in its 8-byte machine slot at
370	    `sp + slotOff r`; the code+data is installed; IL and machine memory agree
371	    off the machine-private bytes; and the current activation's hole is
372	    `[sp, sp + userOff fd)` with `sp` 8-aligned. -/
373	def StInv (L : Layout) (fd : FunDef) (holes : List Hole) (s : St) (m : State) : Prop :=
374	  m.rget 2 = s.sp
375	  ∧ (∀ r, 1 ≤ r → r ≤ maxRegF fd →
376	        s.rget r = m.loadWord (s.sp + BitVec.ofNat 64 (slotOff r)))
377	  ∧ Installed L m
378	  ∧ (∀ a, ¬ MachPriv L holes a → s.mem a = m.mem a)
379	  ∧ holes.head? = some (s.sp, userOff fd)
380	  ∧ s.sp.toNat % 8 = 0
381	
382	/-- The top-level separation bundle for `prog_sim`: the entry stack `[stackLo,
383	    sp0)` is well-formed and disjoint from the code+data blob, and `sp0` is
384	    8-aligned. (The per-function stack budget is SUBSUMED by P1's overflow
385	    check, so it is NOT a hypothesis here — that is the point of §2.) -/
386	structure SimPre (L : Layout) (stackLo sp0 : Word) : Prop where
387	  spAligned    : sp0.toNat % 8 = 0
388	  stackNonEmpty : stackLo.toNat ≤ sp0.toNat
389	  blobStackDisjoint : ∀ a, memRange a L.codeBase L.blobLen → ¬ MachStack stackLo sp0 a
390	
391	/-- The padding oracle `compile_sim` instantiates: `userOff` of each function.
392	    With this, IL `sp` ≡ machine `x2` at every depth (validated in
393	    `CompileTests.p1_*`). -/
394	def userPad (env : Env) : Name → Nat := fun f => (List.lookup f env).elim 0 userOff
395	
396	/-! ## §3.3 — the program-level payoff `prog_sim` (statement; proof deferred).
397	
398	    `lower_sim`/`call_sim` (§3.2/§3.3) are the induction's workh

… [truncated 1981 chars]
```

### 🤖 **Assistant**  ·  14:50:27; model 19.0s

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  91ms
```json
{"command": "grep -n \"def loadSlot\\|def storeSlot\\|def A \\|def slotOff\\|abbrev A\\|def RA\\|def SP\\|def T0\\|def T1\" /var/data/bootstrap/lean/LowIR/Compile.lean; echo ---; grep -n \"jalr\" /var/data/bootstrap/lean/LowIR.lean | head; echo ---; grep -n \"theorem step_jal\\b\\|theorem step_jal \\|theorem step_ld\\|theorem step_sd\\|theorem step_addi\" /var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean", "description": "Check helper defs and existing step lemmas"}
```

### 🛠️ **Tool result**  ·  14:50:30; tool 91ms

```
45:def SP : Reg := 2
46:def RA : Reg := 1
47:def T0 : Reg := 5
48:def T1 : Reg := 6
50:def A (i : Nat) : Reg := 10 + i
68:def slotOff (r : Reg) : Nat := 8 * (1 + r)
72:def loadSlot (r : Reg) (t : Reg) : List SymInstr :=
78:def storeSlot (r : Reg) (t : Reg) : List SymInstr :=
---
---
119:theorem step_addi (m : State) (rd rs : Nat) (imm : BitVec 12)
139:theorem step_ld (m : State) (rd rs : Nat) (imm : BitVec 12)
144:theorem step_sd (m : State) (rs1 rs2 : Nat) (imm : BitVec 12)
167:theorem step_jal (m : State) (rd : Nat) (imm : BitVec 21)
```

### 🤖 **Assistant**  ·  14:50:31; model 735ms

**🔧 tool → Read**  ·  10ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean", "offset": 830, "limit": 60}
```

### 🛠️ **Tool result**  ·  14:50:31; tool 10ms

```
830	            Emitted_append_left L here (synthI T0 (bs.length : Int)) (storeSlotI rd T0) hem
831	          have hemST : Emitted L (here + 12) (storeSlotI rd T0) := by
832	            have h := Emitted_append_right L here (synthI T0 (bs.length : Int)) (storeSlotI rd T0) hem
833	            rwa [synthI_length] at h
834	          have hrange : -2048 ≤ synthHi (bs.length : Int) ∧ synthHi (bs.length : Int) ≤ 2047 := by
835	            have h := hdat d; rwa [hlk, Option.map_some, Option.getD_some] at h
836	          obtain ⟨hinvS, hT0S, hpcS, -, -⟩ :=
837	            run_synth L fd holes s m T0 (by decide) (by decide) (bs.length : Int) here
838	              hinv hpc hemS hrange
839	          obtain ⟨ks, hinvF, hpcF⟩ :=
840	            run_store L fd holes s (stepN 3 m) rd (BitVec.ofInt 64 (bs.length : Int)) (here + 12)
841	              hinvS hT0S hpcS hemST hrd hfrd hnw hseg hblob hbd
842	          have hval : s.rset rd (BitVec.ofInt 64 (bs.length : Int))
843	              = s.rset rd (BitVec.ofNat 64 bs.length) := by rw [BitVec.ofInt_natCast]
844	          refine ⟨3 + ks, ?_, ?_⟩
845	          · rw [stepN_add 3 ks m]; rw [hval] at hinvF; exact hinvF
846	          · rw [stepN_add 3 ks m, hpcF]
847	            apply pc_congr
848	            simp only [landPos, csize, storeSlotI_length]
849	            by_cases hrd0 : rd = 0 <;> simp only [hrd0, if_true, if_false] <;> omega
850	    case cref rd d =>
851	      -- pc-read + delta-synth + add (`run_cref`) loads the data address into T0,
852	      -- then park it into rd's slot (`run_store`). `hdbase` links the IL `dbase`
853	      -- to the machine layout; `hdpos`/`here < 2²⁰` give the delta synth range.
854	      have hrd : rd ≤ maxRegF fd := hreg
855	      have hfrd : slotOff rd < 2 ^ 11 := by
856	        have := slotOff_add8_le_userOff fd rd hrd; omega
857	      have hhere : here < 2 ^ 20 := by
858	        have : here + 4 * csize (.cref rd d) < 2 ^ 20 := hbnd; omega
859	      cases hdb : dbase d with
860	      | none =>
861	          rw [LowIR.Prog.exec_cref_none P dbase pad stackLo fuel rd d s hdb] at hexec
862	          exact absurd hexec (by simp)
863	      | some a =>
864	          rw [LowIR.Prog.exec_cref P dbase pad stackLo fuel rd d s hdb, Option.some.injEq,
865	              Prod.mk.injEq] at hexec
866	          obtain ⟨rfl, rfl⟩ := hexec
867	          have ha : a = L.codeBase + BitVec.ofNat 64 (dpos d) := hdbase d a hdb
868	          simp only [emitCF] at hem
869	          have hemC : Emitted L here (crefI T0 T1 ((dpos d : Int) - ((here : Int) + 4))) :=
870	            Emitted_append_left L here _ (storeSlotI rd T0) hem
871	          have hemST : Emitted L (here + 20) (storeSlotI rd T0) := by
872	            have h := Emitted_append_right L here
873	              (crefI T0 T1 ((dpos d : Int) - ((here : Int) + 4))) (storeSlotI rd T0) hem
874	            rwa [crefI_length] at h
875	          obtain ⟨hinvC, hT0C, hpcC, -⟩ :=
876	            run_cref L fd holes s m (dpos d) here hinv hpc hemC hhere (hdpos d)
877	          obtain ⟨ks, hinvF, hpcF⟩ :=
878	            run_store L fd holes s (stepN 5 m) rd (L.codeBase + BitVec.ofNat 64 (dpos d))
879	              (here + 20) hinvC hT0C hpcC hemST hrd hfrd hnw hseg hblob hbd
880	          refine ⟨5 + ks, ?_, ?_⟩
881	          · rw [stepN_add 5 ks m]; rw [← ha] at hinvF; exact hinvF
882	          · rw [stepN_add 5 ks m, hpcF]
883	            apply pc_congr
884	            simp only [landPos, csize, storeSlotI_length]
885	            by_cases hrd0 : rd = 0 <;> simp only [hrd0, if_true, if_false] <;> omega
886	    case call argc rvc f args rets => sorry
887	
888	end LowIR.ProgSim
889	
```

### 🤖 **Assistant**  ·  14:50:40; model 11.6s

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  101ms
```json
{"command": "grep -n \"structure Layout\" -A 12 /var/data/bootstrap/lean/LowIR/ProgSim/Defs.lean; echo ---; grep -rn \"| jalr\" /var/data/bootstrap/lean/LowIR.lean /var/data/bootstrap/lean/RawAsm*.lean 2>/dev/null | head -3; grep -rn \"jalr\" /var/data/bootstrap/lean/LowIR.lean | head -8", "description": "Check Layout fields and jalr in machine model"}
```

### 🛠️ **Tool result**  ·  14:50:43; tool 101ms

```
48:structure Layout where
49-  codeBase : Word
50-  instrs   : List Instr
51-  fnTab    : List (Name × Nat)
52-  segStart : Nat
53-  data     : Data
54-deriving Repr
55-
56-/-- Build the `Layout` for `entry` at `codeBase` straight from the FROZEN
57-    compiler. `none` iff the program is uncompilable (same condition as
58-    `compileProgT`). `segStart` is recomputed here from the resolved stream
59-    and is DEFINITIONALLY the compiler's `pad8 codeEnd` (proved in Phase 2). -/
60-def layoutOf (P : Program) (entry : Name) (codeBase : Word) : Option Layout :=
---
```

### 🤖 **Assistant**  ·  14:50:43; model 804ms

**🔧 tool → Read**  ·  10ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/Compile.lean", "offset": 68, "limit": 20}
```

### 🛠️ **Tool result**  ·  14:50:44; tool 10ms

```
68	def slotOff (r : Reg) : Nat := 8 * (1 + r)
69	
70	/-- Load IL register `r`'s slot into physical register `t`
71	    (IL x0 is hardwired zero — never read its slot). -/
72	def loadSlot (r : Reg) (t : Reg) : List SymInstr :=
73	  if r = 0 then [.ins (.addi t 0 0)]
74	  else [.ins (.ld t SP (BitVec.ofNat 12 (slotOff r)))]
75	
76	/-- Store physical register `t` into IL register `r`'s slot
77	    (IL writes to x0 are discarded — emit nothing). -/
78	def storeSlot (r : Reg) (t : Reg) : List SymInstr :=
79	  if r = 0 then []
80	  else [.ins (.sd SP t (BitVec.ofNat 12 (slotOff r)))]
81	
82	/-- Fresh-label supply. -/
83	abbrev M := StateM Nat
84	def fresh : M Nat := modifyGet fun n => (n, n + 1)
85	
86	/-- Materialize the constant `v` (|v| < 2^23) into `t` — a FIXED 3-instruction
87	    sequence (`v = hi*4096 + lo`, both imm12): layout must not depend on the
```

### 🤖 **Assistant**  ·  14:50:51; model 9.0s

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  100ms
```json
{"command": "grep -rln \"inductive Instr\" /var/data/bootstrap/lean/ | head; echo ---; grep -rn \"jalr\" /var/data/bootstrap/lean/Spec 2>/dev/null | head -3; grep -rln \"namespace Rv64i\" /var/data/bootstrap/lean | head", "description": "Locate machine Instr type and Rv64i model"}
```

### 🛠️ **Tool result**  ·  14:50:53; tool 100ms

```
/var/data/bootstrap/lean/RawAsm/Rv64i.lean
---
/var/data/bootstrap/lean/LowIR/ProgSim/WordMem.lean
/var/data/bootstrap/lean/RawAsm/Hex1/Harness.lean
/var/data/bootstrap/lean/RawAsm/Rv64i.lean
/var/data/bootstrap/lean/RawAsm/Hex0/Harness.lean
/var/data/bootstrap/lean/RawAsm/Hex1/Image.lean
/var/data/bootstrap/lean/RawAsm/Hex0/Image.lean
```

### 🤖 **Assistant**  ·  14:50:58; model 5.5s

**🔧 tool → Bash**  ·  90ms
```json
{"command": "grep -n \"jalr\" /var/data/bootstrap/lean/RawAsm/Rv64i.lean | head -8", "description": "Check jalr semantics in Rv64i model"}
```

### 🛠️ **Tool result**  ·  14:50:58; tool 90ms

```
71:  | jalr (rd rs1 : Nat) (imm : BitVec 12)
130:  | 0x67 => if funct3 = 0 then .jalr rd rs1 immI else .unknown
189:  | .jalr rd rs1 imm =>
```

### 🤖 **Assistant**  ·  14:51:04; model 5.3s

**🔧 tool → Read**  ·  12ms
```json
{"file_path": "/var/data/bootstrap/lean/RawAsm/Rv64i.lean", "offset": 183, "limit": 15}
```

### 🛠️ **Tool result**  ·  14:51:04; tool 12ms

```
183	      s.setPc (if (s.rget rs1).slt (s.rget rs2) then s.pc + imm.signExtend 64 else next)
184	  | .bge  rs1 rs2 imm =>
185	      s.setPc (if (s.rget rs1).slt (s.rget rs2) then next else s.pc + imm.signExtend 64)
186	  | .bgeu rs1 rs2 imm =>
187	      s.setPc (if (s.rget rs1).ult (s.rget rs2) then next else s.pc + imm.signExtend 64)
188	  | .jal  rd imm => (s.rset rd next).setPc (s.pc + imm.signExtend 64)
189	  | .jalr rd rs1 imm =>
190	      let t := (s.rget rs1 + imm.signExtend 64) &&& (~~~ (1 : Word))
191	      (s.rset rd next).setPc t
192	  | .unknown => s
193	
194	/-- Run until pc = `halt`. `partial`: used only for executable validation,
195	    NOT in proofs (the proof side reasons about `step` directly). -/
196	partial def runUntil (halt : Word) (s : State) : State :=
197	  if s.pc = halt then s else runUntil halt (step s)
```

### 🤖 **Assistant**  ·  14:56:09; model 5m07s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Read**  ·  22ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/Prog.lean", "offset": 35, "limit": 115}
```

### 🛠️ **Tool result**  ·  14:56:10; tool 22ms

```
35	/-- Structured statements. Ctrl's constructs minus its toy `call (g : Stmt)`,
36	    plus `ld`/`sd`/`annot` and the D7 named call. -/
37	inductive Stmt where
38	  | skip
39	  | seq    (a b : Stmt)
40	  | addi   (rd rs : Reg) (imm : BitVec 12)
41	  | add    (rd rs1 rs2 : Reg)
42	  | sub    (rd rs1 rs2 : Reg)
43	  | orr    (rd rs1 rs2 : Reg)
44	  | slli   (rd rs : Reg) (sh : Nat)
45	  | srli   (rd rs : Reg) (sh : Nat)
46	  | lbu    (rd rs : Reg) (imm : BitVec 12)
47	  | sb     (rbase rval : Reg) (imm : BitVec 12)
48	  | ld     (rd rs : Reg) (imm : BitVec 12)          -- rd := mem64[rs + sext imm]
49	  | sd     (rbase rval : Reg) (imm : BitVec 12)     -- mem64[rbase + sext imm] := rval
50	  | annot  (a : String)                             -- semantic no-op (Ext. 9)
51	  | ife    (c : Cond) (a b : Reg) (t e : Stmt)
52	  | while  (c : Cond) (a b : Reg) (body : Stmt)     -- continue scope
53	  | block  (body : Stmt)                            -- break scope
54	  | brkB   (k : Nat)
55	  | contL  (k : Nat)
56	  | ret
57	  | call   (argc rvc : Nat) (f : Name)
58	           (args : Vector Reg argc) (rets : Vector Reg rvc)
59	  | cref   (rd : Reg) (d : Name)     -- rd := address of const data object d
60	  | clen   (rd : Reg) (d : Name)     -- rd := length of const data object d
61	deriving Repr
62	
63	/-- A function: declared parameter/return registers (callee-side names), a
64	    frame size in bytes, the register that receives the frame base, a body. -/
65	structure FunDef where
66	  argc      : Nat
67	  rvc       : Nat
68	  params    : Vector Reg argc
69	  rets      : Vector Reg rvc
70	  frameSize : Nat
71	  frameReg  : Reg
72	  body      : Stmt
73	
74	abbrev Env := List (Name × FunDef)
75	
76	/-- The program's read-only data segment: named constant byte objects,
77	    referenced from code as slices via `cref` (address) + `clen` (length). -/
78	abbrev Data := List (Name × List Byte)
79	
80	/-- A whole program: functions plus const data. Where the data objects LIVE
81	    is not part of the program — the semantics takes a base map (`dbase`),
82	    ∀-quantifiable like D8's `sp₀`, so programs stay address-independent. -/
83	structure Program where
84	  env  : Env
85	  data : Data
86	
87	/-- Data-free view (keeps pre-data call sites unchanged). -/
88	instance : Coe Env Program := ⟨fun env => ⟨env, []⟩⟩
89	
90	/-- IL machine state: register file (x0 hardwired 0), byte memory, and the
91	    semantic stack pointer — no instruction writes `sp`; only call/return move it. -/
92	structure St where
93	  regs : Reg → Word
94	  mem  : Word → Byte
95	  sp   : Word
96	
97	@[inline] def St.rget (s : St) (i : Reg) : Word := if i = 0 then 0 else s.regs i
98	
99	@[inline] def St.rset (s : St) (i : Reg) (v : Word) : St :=
100	  if i = 0 then s else { s with regs := fun j => if j = i then v else s.regs j }
101	
102	@[inline] def St.loadByte (s : St) (a : Word) : Byte := s.mem a
103	
104	@[inline] def St.storeByte (s : St) (a : Word) (b : Byte) : St :=
105	  { s with mem := fun x => if x = a then b else s.mem x }
106	
107	/-- Little-endian 64-bit load — byte order identical to `Rv64i.State.loadWord`. -/
108	def St.loadWord (s : St) (a : Word) : Word :=
109	  (s.mem a).setWidth 64 |||
110	  ((s.mem (a + 1)).setWidth 64) <<< 8 |||
111	  ((s.mem (a + 2)).setWidth 64) <<< 16 |||
112	  ((s.mem (a + 3)).setWidth 64) <<< 24 |||
113	  ((s.mem (a + 4)).setWidth 64) <<< 32 |||
114	  ((s.mem (a + 5)).setWidth 64) <<< 40 |||
115	  ((s.mem (a + 6)).setWidth 64) <<< 48 |||
116	  ((s.mem (a + 7)).setWidth 64) <<< 56
117	
118	/-- Little-endian 64-bit store — identical to `Rv64i.State.storeWord`. -/
119	def St.storeWord (s : St) (a : Word) (v : Word) : St :=
120	  (((((((s.storeByte a (v.setWidth 8)
121	    ).storeByte (a + 1) ((v >>> 8).setWidth 8)
122	    ).storeByte (a + 2) ((v >>> 16).setWidth 8)
123	    ).storeByte (a + 3) ((v >>> 24).setWidth 8)
124	    ).storeByte (a + 4) ((v >>> 32).setWidth 8)
125	    ).storeByte (a + 5) ((v >>> 40).setWidth 8)
126	    ).st

… [truncated 1481 chars]
```

### 🤖 **Assistant**  ·  14:56:19; model 11.4s

> 💭 (thinking)
> 

**🔧 tool → Bash**  ·  90ms
```json
{"command": "grep -n \"theorem run_load\\|theorem run_store\\|theorem run_synth\\|theorem run_cref\" /var/data/bootstrap/lean/LowIR/ProgSim/StmtSim.lean; echo ---; grep -n \"def wfProgram\" -A 8 /var/data/bootstrap/lean/LowIR/Prog.lean; echo ---; grep -n \"p1_\\|caller\\|chain\" /var/data/bootstrap/lean/LowIR/CompileTests.lean | head -20", "description": "Check run_* lemma names, wfProgram, differential test names"}
```

### 🛠️ **Tool result**  ·  14:56:22; tool 90ms

```
370:theorem run_load (L : Layout) (fd : FunDef) (holes : List Hole) (s : St) (m : State)
398:theorem run_store (L : Layout) (fd : FunDef) (holes : List Hole) (s : St) (m2 : State)
477:theorem run_synth (L : Layout) (fd : FunDef) (holes : List Hole) (s : St) (m : State)
544:theorem run_cref (L : Layout) (fd : FunDef) (holes : List Hole) (s : St) (m : State)
---
264:def wfProgram (P : Program) : Bool :=
265-  P.env.all (fun nf =>
266-    wf P 0 0 nf.2.body && !(nf.2.params.toList.contains nf.2.frameReg))
267-  && (P.data.map Prod.fst).eraseDups.length == P.data.length
268-
269-/-- Data-free convenience (pre-data name, used by the existing tests). -/
270-def wfEnv (env : Env) : Bool := wfProgram env
271-
272-/-! ### Data-segment layout — the SINGLE source of truth.
---
110:open LowIR.Prog (sub3 sumTo frameLocal caller early chainEnv recSum)
176:   ("caller", caller), ("early", early), ("bitops", bitops),
188:    ((compileProg denv "caller").getD [.unknown]).all
209:theorem diff_caller     : diffOk denv "caller" [5] = true := by native_decide
210:theorem diff_chain3     : diffOk chainEnv "f3" [40] = true := by native_decide
221:-- 3-deep chain, each activation parks its argument in its own user frame.
222:theorem p1_chain_userPad :
223:    framesAgree 512 chainEnv "f3" [40] (userPad chainEnv) = true := by native_decide
224:theorem p1_chain_pad0_diverges :
225:    framesAgree 512 chainEnv "f3" [40] (fun _ => 0) = false := by native_decide
228:theorem p1_rec_userPad :
230:theorem p1_rec_pad0_diverges :
```

### 🤖 **Assistant**  ·  14:58:51; model 4m53s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Write**  ·  3.2s
````json
{"file_path": "/var/data/bootstrap/docs/RESUME-CALL.md", "content": "# RESUME-CALL — Phase 5: the `call` case of `lower_sim_cf`\n\nPlan written 2026-07-04, when `call` became the ONLY remaining statement-level\n`sorry` (`lean/LowIR/ProgSim/CtrlSim.lean:886`). Read with\n[RESUME-PROGSIM.md](RESUME-PROGSIM.md) (the campaign handoff — §2 P1, §4\nPhase 5 sketch, §6 discipline); this doc supersedes that Phase-5 sketch with a\nworked design. Everything below is grounded in the code as of commit `5a88ef2`;\nline numbers refer to that state.\n\n## 0. Mission\n\nClose `lower_sim_cf`'s `call` case. After it: no statement-level `sorry`\nremains; what's left of the campaign is Phases 1/2 (encode/decode + AsmFacts\ndischarging the flat layout hypotheses) and Phase 6 (`prog_sim`).\n\n**The headline design answer** (asked 2026-07-04: *\"can we verify `call`\ninductively — using the theorem for the evaluator itself for the call?\"*):\n**yes, and the proof is already shaped for it.** `exec`'s call equation\n(`Prog.lean:222`) runs the callee's body at `fuel` from `fuel+1`:\n\n```lean\nmatch exec P dbase pad stackLo fuel fd.body callee with\n```\n\nand `lower_sim_cf` inducts on `fuel` (`CtrlSim.lean:279`), so in the `call`\ncase the induction hypothesis IS the full simulation theorem at clock `fuel` —\nfor ANY statement, including the callee's whole body. This is the same move the\n`while` case already made (back-edge → fuel IH on the same term), and it\nhandles **recursive functions for free**

… [truncated 24710 chars]
````

### 🛠️ **Tool result**  ·  15:01:18; tool 3.2s

```
File created successfully at: /var/data/bootstrap/docs/RESUME-CALL.md (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  15:01:25; model 14.0s

> 💭 (thinking)
> 

**🔧 tool → Edit**  ·  46ms
```json
{"replace_all": false, "file_path": "/var/data/bootstrap/docs/README.md", "old_string": "- [RESUME-PROGSIM.md](RESUME-PROGSIM.md) — **the ACTIVE handoff: proving `compile_sim` for Prog** (compiler correctness for the D7/D8 IR): the address-divergence obstacle and the P1 frame-padding-oracle decision, the simulation relation and theorem statements, the footprint side-condition design, six proof phases with risk/size estimates, file/build plan, the vertical-slice go/no-go checkpoint, and the SSA→Prog composability notes (§7.6, on the `iterWhile` semantics).", "new_string": "- [RESUME-PROGSIM.md](RESUME-PROGSIM.md) — **the ACTIVE handoff: proving `compile_sim` for Prog** (compiler correctness for the D7/D8 IR): the address-divergence obstacle and the P1 frame-padding-oracle decision, the simulation relation and theorem statements, the footprint side-condition design, six proof phases with risk/size estimates, file/build plan, the vertical-slice go/no-go checkpoint, and the SSA→Prog composability notes (§7.6, on the `iterWhile` semantics).\n- [RESUME-CALL.md](RESUME-CALL.md) — **Phase 5 detailed plan: the `call` case of `lower_sim_cf`** (the last statement-level `sorry`): why the fuel IH is the evaluator theorem applied to the callee body, the dead-hole `StInv` repair (free-stack privatization), the `FramesPres` conclusion conjunct, the new flat hypotheses (`hpad`/`hfn`/`halign`), the prologue/epilogue/marshalling lemma atoms, the six-segment call-case assembly, and the commit-or

… [truncated 18 chars]
```

### 🛠️ **Tool result**  ·  15:01:32; tool 46ms

```
The file /var/data/bootstrap/docs/README.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  15:01:34; model 2.2s

**🔧 tool → Edit**  ·  83ms
```json
{"replace_all": false, "file_path": "/var/data/bootstrap/docs/RESUME-PROGSIM.md", "old_string": "  3. **`lower_sim_cf` `call` (Phase 5) is the last statement-level `sorry`.** Then", "new_string": "  3. **`lower_sim_cf` `call` (Phase 5) is the last statement-level `sorry`.**\n     Detailed worked plan (2026-07-04): [RESUME-CALL.md](RESUME-CALL.md) — the\n     fuel-IH-on-the-callee-body design, the dead-hole `StInv` repair, and the\n     commit-ordered step list. Start there. Then"}
```

### 🛠️ **Tool result**  ·  15:01:34; tool 83ms

```
The file /var/data/bootstrap/docs/RESUME-PROGSIM.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  15:01:39; model 5.3s

**🔧 tool → Bash**  ·  1.4s
```json
{"command": "./docs/check_docs.py 2>&1 | tail -5", "description": "Validate docs corpus (links, index, reachability)"}
```

### 🛠️ **Tool result**  ·  15:01:41; tool 1.4s

```
FAIL — 1 problem(s):
  RESUME-CALL.md:4: broken file reference `lean/LowIR/ProgSim/CtrlSim.lean:886` (no such path 'lean/LowIR/ProgSim/CtrlSim.lean:886')
```

### 🤖 **Assistant**  ·  15:01:50; model 10.6s

> 💭 (thinking)
> 

The checker rejects the full path + line-number form; RESUME-PROGSIM uses the bare-filename style (`CtrlSim.lean:777`), so I'll match that.

**🔧 tool → Edit**  ·  38ms
```json
{"replace_all": false, "file_path": "/var/data/bootstrap/docs/RESUME-CALL.md", "old_string": "`sorry` (`lean/LowIR/ProgSim/CtrlSim.lean:886`). Read with", "new_string": "`sorry` (`CtrlSim.lean:886`, in `lean/LowIR/ProgSim/`). Read with"}
```

### 🛠️ **Tool result**  ·  15:01:51; tool 38ms

```
The file /var/data/bootstrap/docs/RESUME-CALL.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  15:01:59; model 7.7s

**🔧 tool → Bash**  ·  1.4s
```json
{"command": "./docs/check_docs.py 2>&1 | tail -3", "description": "Re-validate docs corpus"}
```

### 🛠️ **Tool result**  ·  15:02:01; tool 1.4s

```
OK — 43 markdown files checked, all constraints hold.
```

### 🤖 **Assistant**  ·  15:02:17; model 16.3s

> 💭 (thinking)
> 

> 💭 (thinking)
> 

**🔧 tool → Read**  ·  26ms
```json
{"file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md"}
```

### 🛠️ **Tool result**  ·  15:02:17; tool 26ms

```
1	---
2	name: progsim-campaign-frontier
3	description: compile_sim-for-Prog campaign — Phase 0/3 done; lower_sim_cf (outcome-carrying) has EVERYTHING except call — skip/annot/block/seq/arith/mem/leaves/ife/while/clen/cref all DONE (2026-07-04); lower_sim pruned to leaf-only (axiom-clean); ONLY remaining statement-level sorry = the call case (Phase 5). Priorities in docs/PROOF-COMPLEXITY.md + docs/RESUME-PROGSIM.md.
4	metadata: 
5	  node_type: memory
6	  type: project
7	  originSessionId: a41cd3ef-e057-451c-b2c8-26929e4cdbdc
8	---
9	
10	The `compile_sim`-for-Prog proof campaign (plan: `docs/RESUME-PROGSIM.md`; see
11	[[lowir-compiler-cut]] for the frozen compiler it verifies).
12	
13	**2026-07-04 — CLEN + CREF both DONE; `lower_sim_cf` now only lacks `call`.**
14	Const-data machinery (all `[propext, Quot.sound]`), commits after `0615725`:
15	`run_synth` (StmtSim, GENERALISED to any scratch target `t`≠0,2 + an
16	other-registers-preserved clause — the clause is what lets cref synth δ into T1
17	without clobbering the pc-read in T0; `9ec08e5`), built on `signExtend_ofInt_12`
18	+ `synth_val` (`(ofInt hi)<<<12 + ofInt lo = ofInt(hi*4096+lo)` via
19	`BitVec.shiftLeft_eq_mul_twoPow` + `twoPow`→`ofNat`→`ofInt`; `synthLo`/`synthHi`
20	arithmetic all by `omega` after `unfold`). `run_cref` (StmtSim): jal T0,+4
21	pc-read → `run_synth` δ into T1 → `add T0 T0 T1` = codeBase+dpos(d); `jump_lands`
22	handles both the +4 pc-read landing and the final add landing; range from
23	`dp`/`here < 2²⁰`. `emitCF`/`csize`/`emitCF_length` gained a `(dat : Data,
24	dpos : Name → Nat)` context + real clen (`synthI T0 len ++ storeSlotI`) / cref
25	(`crefI T0 T1 δ ++ storeSlotI`) arms; `matchesReal` generalised with a `dats`
26	table validates BOTH against `Compile.lower ▸ resolveOne` (8 new #guards). The
27	`05d5ac8` cref case + the clen case add flat theorem hyps `hdat` (lengths
28	synth-safe), `hdbase` (`dbase d = some (codeBase + ofNat (dpos d))`), `hdpos`
29	(`dpos d < 2²⁰`) — the Phase-2 layout obligations (`dpos := segStart +
30	dataOffsetsFrom`, `dbaseOf` IS it). GOTCHAS: `set` is Mathlib-only (unavailable)
31	— inline the value; conjunction-goal `omega` pulls `Classical.choice` (split
32	`hlo1`/`hlo2`, and `refine ⟨?_,?_⟩ <;> omega` for the δ-range); `here+4+12`≡
33	`here+16` and `stepN 5 m ≡ step(stepN 3 (step m))` hold by `rfl` (Nat-literal /
34	stepN defeq) so `apply pc_congr; omega` over-closes ("no goals") — just `rw`;
35	`exec_clen_none`/`exec_cref_none` added to ExecFacts to discharge the missing-data
36	branch WITHOUT `simp [exec]` (OOM with IH in scope). Next: the `call` case
37	(Phase 5) — the last statement-level sorry.
38	
39	**2026-07-04 re-prioritization** (measured assessment in
40	`docs/PROOF-COMPLEXITY.md`, committed 553a490): (1) FIRST prune `lower_sim`'s
41	superseded tail — `StmtSim.lean:1003` `all_goals sorry` + its `seq` case have
42	no consumer, `lower_sim_cf` subsumes them; keep only the straight-line/mem
43	leaves it delegates to. (2) while → cref/clen (unstub emitCF/csize) → call →
44	Phases 1/2/6. (3) Post-campaign ladder: Prog syntactic frame theorem
45	(defs-based `exec_frame` analog, ~120 lines by SSA precedent) BEFORE
46	`ProgProof`; then hex0F_correct ported from `CtrlProof.main_loop` as the ONE
47	maintained hex0 proof (the decoder argument is currently proved 3×, RawAsm/
48	Ctrl/SSA — don't add a 4th sibling); then SSA→Prog lowering sim; then the
49	borrow checker over SSA (discharges MemAccOff/execT/Wf hypotheses). 2026-07-03: the
50	plan doc gained (a) the full `lower_sim_cf` while-case skeleton — Prog's
51	back-edge re-enters the SAME term at fuel, so the fuel IH applies directly (the
52	fixed-term shape SSA bought via its §8 `iterWhile` rework, see
53	[[ssa-hex0-campaign-frontier]]); on `.cont 0` the body's own lowered jump
54	already lands at lTop (`contL_sim`), only `.normal` runs the emitted back-edge
55	jal — and (b) §7.6: keep `lower_sim`/`prog_sim` fuel-indexed, outcome-carrying,
56	plain-equality so the

… [truncated 14070 chars]
```

### 🤖 **Assistant**  ·  15:02:33; model 16.1s

**🔧 tool → Edit**  ·  41ms
```json
{"replace_all": false, "file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md", "old_string": "description: compile_sim-for-Prog campaign — Phase 0/3 done; lower_sim_cf (outcome-carrying) has EVERYTHING except call — skip/annot/block/seq/arith/mem/leaves/ife/while/clen/cref all DONE (2026-07-04); lower_sim pruned to leaf-only (axiom-clean); ONLY remaining statement-level sorry = the call case (Phase 5). Priorities in docs/PROOF-COMPLEXITY.md + docs/RESUME-PROGSIM.md.", "new_string": "description: compile_sim-for-Prog campaign — Phase 0/3 done; lower_sim_cf (outcome-carrying) has EVERYTHING except call — skip/annot/block/seq/arith/mem/leaves/ife/while/clen/cref all DONE (2026-07-04); lower_sim pruned to leaf-only (axiom-clean); ONLY remaining statement-level sorry = the call case (Phase 5) — detailed worked plan in docs/RESUME-CALL.md (fuel IH on callee body; dead-hole StInv repair required FIRST). Priorities in docs/PROOF-COMPLEXITY.md + docs/RESUME-PROGSIM.md."}
```

### 🛠️ **Tool result**  ·  15:02:33; tool 41ms

```
The file /home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  15:02:44; model 10.9s

**🔧 tool → Edit**  ·  45ms
```json
{"replace_all": false, "file_path": "/home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md", "old_string": "The `compile_sim`-for-Prog proof campaign (plan: `docs/RESUME-PROGSIM.md`; see\n[[lowir-compiler-cut]] for the frozen compiler it verifies).", "new_string": "The `compile_sim`-for-Prog proof campaign (plan: `docs/RESUME-PROGSIM.md`; see\n[[lowir-compiler-cut]] for the frozen compiler it verifies).\n\n**2026-07-04 — Phase-5 (call) detailed plan written: `docs/RESUME-CALL.md`.**\nKey findings baked into it: (1) the call case IS inductive — exec runs the\ncallee body at `fuel` from `fuel+1`, so the lower_sim_cf fuel IH applies to the\ncallee body directly (recursion free; same move as while's back-edge) — but the\n`generalizing` clause must gain `fd holes epiPos` (C1). (2) DISCOVERED: the\ncurrent StInv memory-agreement conjunct is UNRESTORABLE after a call returns —\nthe machine dirties the callee's dead [ra][slots] hole, which after return is\nfree stack outside `holes`; fix = free-stack-below-sp joins the private region\n(StInv gains stackLo; MemAccOff strengthened in lockstep) + two new conjuncts\n(holes-above-sp ordering, per-hole no-wrap) — do this W2 surgery FIRST, re-green\neverything. (3) A `FramesPres` conjunct must join the lower_sim_cf CONCLUSION\n(machine hole-bytes preserved off the own slot window) — the body IH otherwise\ncan't deliver the saved-ra/caller-slot preservation. (4) New flat hyps\n`hpad` (pad f = userOff fd' — P1 en

… [truncated 421 chars]
```

### 🛠️ **Tool result**  ·  15:02:44; tool 45ms

```
The file /home/<user>/.claude/projects/-var-data-bootstrap/memory/progsim-campaign-frontier.md has been updated successfully. (file state is current in your context — no need to Read it back)
```

### 🤖 **Assistant**  ·  15:02:52; model 8.1s

**🔧 tool → Bash**  ·  1.4s
```json
{"command": "git add docs/RESUME-CALL.md docs/README.md docs/RESUME-PROGSIM.md && git commit -m \"docs: RESUME-CALL — Phase-5 worked plan (call case of lower_sim_cf)\n\nThe fuel IH is the evaluator theorem applied to the callee body; blocking\ndesign decisions C1-C6 (induction generalization, the dead-hole StInv repair,\nFramesPres conclusion conjunct, hpad/hfn/halign flat hypotheses, emitCF call\narms, MemAccOff call arm), lemma atoms, the six-segment case assembly, and\nthe commit-ordered work plan W1-W8.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\" --quiet && git log --oneline -1", "description": "Commit the Phase-5 handoff doc"}
```

### 🛠️ **Tool result**  ·  15:02:53; tool 1.4s

```
9d997f4 docs: RESUME-CALL — Phase-5 worked plan (call case of lower_sim_cf)
```

### 🤖 **Assistant**  ·  15:03:11; model 17.2s

> 💭 (thinking)
> 

Done — the detailed Phase-5 plan is written, indexed, and committed as `9d997f4`.

**What landed:**
- **`docs/RESUME-CALL.md`** — the full handoff for the `call` case, grounded in the code (exact `exec` call equation, compiler prologue/epilogue/marshalling, `resolveOne`'s `callf`, the current theorem shape). It opens with the answer to your question: yes, the proof is inductive — `exec` runs the callee body at `fuel` from `fuel+1`, so the fuel IH *is* the simulation theorem for the callee body, and recursion comes free.
- `docs/README.md` gained the index line; `RESUME-PROGSIM.md`'s NEXT item now points at the new doc; `check_docs.py` passes; the campaign memory is updated.

**The most important thing in the doc** — a genuine invariant bug I found while working out the design, now written down as decision **C2**: the current `StInv` memory-agreement conjunct (`agree everywhere off MachPriv`) is *unrestorable* after any call returns. The machine dirties the callee's `[ra][slots]` hole; after return that region is dead free stack, outside the caller's `holes` list, so `StInv` demands agreement there and it's false. The adopted fix is to make the free stack `[stackLo, sp)` machine-private (which is exactly the coarse observable `prog_sim` already uses at top level), with `MemAccOff` strengthened in lockstep. This surgery must be done *first* (work step W2) and it's the phase's biggest risk — everything else is plumbing.

The other load-bearing decisions the doc works out: the `generalizing` clause must gain `fd holes epiPos` (C1); a `FramesPres` conjunct must join the theorem's *conclusion* so the body IH can deliver saved-ra and caller-slot preservation (C3 — the CallChain, concretely); three new flat hypotheses `hpad`/`hfn`/`halign` become Phase-2 layout obligations alongside `hdat`/`hdbase`/`hdpos` (C4); `csize`/`emitCF` need real call arms plus a whole-program `matchesRealProg` validation, since both currently fall to empty defaults (C5); and `MemAccOff` gets a call arm mirroring the `while` fix (C6). The work plan is eight commit-ordered steps, ~2000 lines, with `prologue_sim`/`epilogue_sim` proved standalone against the `diff_caller`/`diff_chain3` oracle states before the induction case is touched.
