# Claude session `8d8a5b9a-7768-4afa-9485-dbad93f5a5c3`

- **Agent:** claude
- **Model(s):** claude-opus-4-8
- **Started:** 2026-07-04 14:41:30Z  |  **Last activity:** 2026-07-04 14:43:14Z
- **Turns:** 2 human / 8 assistant  |  **Tool calls:** 11
- **Source:** `/home/<user>/.claude/projects/-var-data-bootstrap/8d8a5b9a-7768-4afa-9485-dbad93f5a5c3.jsonl`

## Token cost

- output: **4,097**  |  fresh input: 27,910  |  cache write: 49,313  |  cache read: 405,591
- **cost: **$0.75**** (priced as `anthropic/claude-opus-4.8` — $5/$25 in/out per Mtok, $0.5 cache-read, $6.25 cache-write)

## Timing

- wall-clock: **1m44s**  ·  active (model+tool): **1m05s**  ·  waiting for user: 38.3s
- model generation: 1m05s  ·  tool execution: 632ms
- tool time by name: `Bash` 535ms/6, `Read` 97ms/5

## Summary

**First request:**

> resume from @docs/README.md @docs/RESUME-PROGSIM.md

**Final response:**

> This is the summit (Phase 5). The structural crux: the `call` case needs the *callee's* body to simulate, which the current per-statement IH doesn't provide. Let me read the full `lower_sim_cf` signature and its induction setup to understand what IH is available.

---

## Transcript

### 👤 **User**  ·  14:41:30

resume from @docs/README.md @docs/RESUME-PROGSIM.md

### 🤖 **Assistant**  ·  14:41:33; model 4.7s

> 💭 (thinking)
> 

I'll get oriented on the current state of the code, then figure out what to tackle next.

**🔧 tool → Bash**  ·  165ms
```json
{"command": "git log --oneline -8 && echo \"---STATUS---\" && git status --short", "description": "Check recent commits and working tree"}
```

### 🛠️ **Tool result**  ·  14:41:35; tool 165ms

```
5a88ef2 docs: RESUME-PROGSIM — clen/cref DONE; only call remains in lower_sim_cf
05d5ac8 lean/progsim: prove lower_sim_cf cref case — only call remains
9ec08e5 lean/progsim: generalise run_synth to any scratch target + reg-preservation
bf32801 lean/progsim: prove lower_sim_cf clen case (synthConst → slot store)
a3fdb54 lean/progsim: synthConst_correct (run_synth) — the reusable cref/clen core
8f73960 docs: RESUME-PROGSIM — prune + while DONE, cref/clen/call remain
0615725 lean/progsim: prove lower_sim_cf `while` (fuel-IH back-edge)
f0cdeff lean/progsim: prune lower_sim to leaf statements (axiom-clean)
---STATUS---
?? .pi/
?? GROK_DOCS_REVIEW.md
?? hex0-proof-wall.svg
?? libc-explore.md
?? pitch-v2.md
?? pitch.md
?? runs_tmp/
?? third-party/
```

### 🤖 **Assistant**  ·  14:41:36; model 713ms

**🔧 tool → Bash**  ·  62ms
```json
{"command": "grep -rn \"sorry\" lean/LowIR/ProgSim/ 2>/dev/null", "description": "Find remaining sorry in ProgSim"}
```

### 🛠️ **Tool result**  ·  14:41:36; tool 62ms

```
lean/LowIR/ProgSim/Defs.lean:6:  top-level theorems. Everything here is a real, `sorry`-free DEFINITION plus a
lean/LowIR/ProgSim/Defs.lean:7:  handful of `sorry`'d equivalence theorems whose statements are the deliverable
lean/LowIR/ProgSim/Defs.lean:432:  sorry
lean/LowIR/ProgSim/CtrlSim.lean:886:    case call argc rvc f args rets => sorry
lean/LowIR/ProgSim/StmtSim.lean:6:  `sorry`'d (Phase 4.3). It is the go/no-go for the `StInv`-based relation design — if
lean/LowIR/ProgSim/StmtSim.lean:58:    their `lower_sim` cases are `sorry` (they use the real `Emitted`, not `emit`). -/
lean/LowIR/ProgSim/StmtSim.lean:750:    non-memory and (still-`sorry`) control-flow cases. `lbu`/`sb` touch one byte,
lean/LowIR/ProgSim/StmtSim.lean:862:    so restricting `lower_sim` to them keeps it total-and-`sorry`-free (a control-flow
lean/LowIR/ProgSim/StmtSim.lean:1235:  -- so this corollary is genuinely `sorry`-free — the real go/no-go).
```

### 🤖 **Assistant**  ·  14:41:40; model 6.0s

> 💭 (thinking)
> 

The only remaining statement-level `sorry` is the `call` case at `CtrlSim.lean:886` — Phase 5, the summit. Let me understand the current structure before diving in.

**🔧 tool → Read**  ·  25ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean", "offset": 780, "limit": 120}
```

### 🛠️ **Tool result**  ·  14:41:42; tool 25ms

```
780	              | brk k =>
781	                  rw [LowIR.Prog.exec_while_brk P dbase pad stackLo fuel c a b body s sb k hev hbody,
782	                      Option.some.injEq, Prod.mk.injEq] at hexec
783	                  obtain ⟨rfl, rfl⟩ := hexec
784	                  refine ⟨3 + kb, by rw [hsN kb]; exact hinvB, ?_⟩
785	                  rw [hsN kb, hpcB]; simp only [landPos]
786	              | cont k =>
787	                  cases k with
788	                  | zero =>
789	                      rw [LowIR.Prog.exec_while_cont0 P dbase pad stackLo fuel c a b body s sb hev
790	                          hbody] at hexec
791	                      have hpcB' : (stepN kb (step (step (step m)))).pc
792	                          = L.codeBase + BitVec.ofNat 64 here := by
793	                        rw [hpcB]; simp only [landPos, List.getD_cons_zero]
794	                      obtain ⟨kw, hinvW, hpcW⟩ :=
795	                        ih (.while c a b body) sb s' oc (stepN kb (step (step (step m))))
796	                          here brkPos contPos hexec hinvB hpcB' hem hreg
797	                          (by rw [hsp]; exact hnw) (by rw [hsp]; exact hbd)
798	                          (haccRec sb (Or.inr hbody)) hlbl hbnd hbr
799	                      refine ⟨3 + kb + kw, ?_, ?_⟩
800	                      · rw [stepN_add (3 + kb) kw, hsN kb]; exact hinvW
801	                      · rw [stepN_add (3 + kb) kw, hsN kb]; exact hpcW
802	                  | succ k' =>
803	                      rw [LowIR.Prog.exec_while_contS P dbase pad stackLo fuel c a b body s sb k' hev
804	                          hbody, Option.some.injEq, Prod.mk.injEq] at hexec
805	                      obtain ⟨rfl, rfl⟩ := hexec
806	                      refine ⟨3 + kb, by rw [hsN kb]; exact hinvB, ?_⟩
807	                      rw [hsN kb, hpcB]; simp only [landPos, List.getD_cons_succ]
808	              | ret =>
809	                  rw [LowIR.Prog.exec_while_ret P dbase pad stackLo fuel c a b body s sb hev hbody,
810	                      Option.some.injEq, Prod.mk.injEq] at hexec
811	                  obtain ⟨rfl, rfl⟩ := hexec
812	                  refine ⟨3 + kb, by rw [hsN kb]; exact hinvB, ?_⟩
813	                  rw [hsN kb, hpcB]; simp only [landPos]
814	    case clen rd d =>
815	      -- synthConst the length into T0 (`run_synth`), then park it into rd's slot
816	      -- (`run_store`). Position-independent; `hdat` supplies the synth range.
817	      have hrd : rd ≤ maxRegF fd := hreg
818	      have hfrd : slotOff rd < 2 ^ 11 := by
819	        have := slotOff_add8_le_userOff fd rd hrd; omega
820	      cases hlk : List.lookup d P.data with
821	      | none =>
822	          rw [LowIR.Prog.exec_clen_none P dbase pad stackLo fuel rd d s hlk] at hexec
823	          exact absurd hexec (by simp)
824	      | some bs =>
825	          rw [LowIR.Prog.exec_clen P dbase pad stackLo fuel rd d s hlk, Option.some.injEq,
826	              Prod.mk.injEq] at hexec
827	          obtain ⟨rfl, rfl⟩ := hexec
828	          simp only [emitCF, hlk, Option.map_some, Option.getD_some] at hem
829	          have hemS : Emitted L here (synthI T0 (bs.length : Int)) :=
830	            Emitted_append_left L here (synthI T0 (bs.length : Int)) (storeSlotI rd T0) hem
831	          have hemST : Emitted L (here + 12) (storeSlotI rd T0) := by
832	            have h := Emitted_append_right L here (synthI T0 (bs.length : Int)) (storeSlotI rd T0) hem
833	            rwa [synthI_length] at h
834	          have hrange : -2048 ≤ synthHi (bs.length : Int) ∧ synthHi (bs.length : Int) ≤ 2047 := by
835	            have h := hdat d; rwa [hlk, Option.map_some, Option.getD_some] at h
836	          obtain ⟨hinvS, hT0S, hpcS, -, -⟩ :=
837	            run_synth L fd holes s m T0 (by decide) (by decide) (bs.length : Int) here
838	              hinv hpc hemS hrange
839	          obtain ⟨ks, hinvF, hpcF⟩ :=
840	            run_store L fd holes s (stepN 3 m) rd (BitVec.ofInt 64 (bs.length : Int)) (here + 12)
841	    

… [truncated 2808 chars]
```

### 🤖 **Assistant**  ·  14:41:47; model 7.7s

> 💭 (thinking)
> 

The `call` case is Phase 5 — the summit, estimated HIGH-risk / ~1500 lines. Before committing to an approach, I need to understand the exact shapes. Let me gather the key pieces in parallel.

**🔧 tool → Bash**  ·  86ms
```json
{"command": "grep -n \"exec_call\\|def exec\\b\\|\\.call\\|call \" lean/LowIR/ProgSim/ExecFacts.lean | head -40", "description": "Find exec_call unfolders"}
```

### 🛠️ **Tool result**  ·  14:41:50; tool 86ms

```
209:theorem exec_call_ok (fuel argc rvc : Nat) (f : Name) (args : Vector Reg argc)
216:    exec P dbase pad stackLo (fuel+1) (.call argc rvc f args rets) s
221:theorem exec_call_lookup_none (fuel argc rvc : Nat) (f : Name) (args : Vector Reg argc)
223:    exec P dbase pad stackLo (fuel+1) (.call argc rvc f args rets) s = none := by simp [exec, hlk]
```

### 🤖 **Assistant**  ·  14:41:51; model 917ms

**🔧 tool → Bash**  ·  64ms
```json
{"command": "grep -n \"call\\|emitCF\\|csize\" lean/LowIR/ProgSim/CtrlSim.lean | head -60", "description": "Find emitCF/csize call arms in CtrlSim"}
```

### 🛠️ **Tool result**  ·  14:41:51; tool 64ms

```
2:  LowIR.ProgSim.CtrlSim — the label-aware lowering `emitCF` and its size `csize`,
8:  `here` where this statement's code begins, `emitCF` produces the concrete RV64I
12:  (they depend only on sub-block sizes, `csize`); only `ret`/`brkB`/`contL` targets
17:  tree; `emitCF`/`csize` annotate it with byte positions).
38:def csize : PStmt → Nat
39:  | .seq a b        => csize a + csize b
43:  | .block body     => csize body
44:  | .ife _ _ _ t e  => 4 + csize e + csize t
45:  | .while _ _ _ b  => 5 + csize b
54:def emitCF (dat : Data) (dpos : Name → Nat) (brkPos contPos : List Nat) (epiPos : Nat) :
57:      emitCF dat dpos brkPos contPos epiPos here a ++
58:        emitCF dat dpos brkPos contPos epiPos (here + 4 * csize a) b
64:      emitCF dat dpos ((here + 4 * csize body) :: brkPos) contPos epiPos here body
70:        ++ [LowIR.condInstr c T0 T1 (8 + 4 * csize e)]
71:        ++ emitCF dat dpos brkPos contPos epiPos (here + 12) e
72:        ++ [LowIR.jal0 (4 + 4 * csize t)]
73:        ++ emitCF dat dpos brkPos contPos epiPos (here + 16 + 4 * csize e) t
78:        ++ [LowIR.condInstr c T0 T1 8, LowIR.jal0 (8 + 4 * csize body)]
79:        ++ emitCF dat dpos brkPos (here :: contPos) epiPos (here + 16) body
80:        ++ [LowIR.jal0 (-(16 + 4 * csize body : Int))]
90:/-- `emitCF`'s length is exactly `csize` — position/label-independent, as claimed.
93:theorem emitCF_length (dat : Data) (dpos : Name → Nat) (bp cp : List Nat) (ep here : Nat)
95:    (emitCF dat dpos bp cp ep here stmt).length = csize stmt := by
98:      simp only [emitCF, csize, List.length_append, iha, ihb]
103:      simp only [emitCF, csize]; rw [ih]
105:      simp only [emitCF, csize, List.length_append, List.length_cons,
109:      simp only [emitCF, csize, List.length_append, List.length_cons,
113:      simp only [emitCF, csize, List.length_append, synthI_length, storeSlotI_length]
115:      simp only [emitCF, csize, List.length_append, crefI_length, storeSlotI_length]
118:/-- Sanity: on the straight-line fragment, `emitCF` is exactly `emit` (the
122:    emitCF dat dpos bp cp ep here (.addi 12 5 (BitVec.ofNat 12 3))
128:    `emitCF` must equal what the trusted `Compile.lower ▸ layoutItems ▸ resolveOne`
136:    position is `4·csize body` — and `fresh` is seeded from `1` so internal labels
137:    can't collide with it. So `emitCF` must be compared with `epiPos = 4·csize`. -/
143:/-- `emitCF` matched against the real pipeline, epilogue at the body's end.
147:    == some (emitCF dat (fun d => (dats.lookup d).getD 0) [] [] (4 * csize body) 0 body)
177:    `emitCF` with the label-position environment (`brkPos`/`contPos`/`epiPos`).
198:/-- Every `ife`'s else-skip branch fits the 13-bit branch window (`8 + 4·csize e`
205:  | .ife _ _ _ t e    => 8 + 4 * csize e < 2 ^ 12 ∧ BranchOk t ∧ BranchOk e
260:    (hem   : Emitted L here (emitCF P.data dpos brkPos contPos epiPos here stmt))
267:    (hbnd  : here + 4 * csize stmt < 2 ^ 20)
278:           + BitVec.ofNat 64 (landPos brkPos contPos epiPos (here + 4 * csize stmt) oc) := by
286:      exact ⟨0, hinv, by simp only [stepN_zero, csize, emit, List.length_nil, Nat.mul_zero,
291:      exact ⟨0, hinv, by simp only [stepN_zero, csize, emit, List.length_nil, Nat.mul_zero,
294:      -- csize/emitCF/MemAccOff for `.block body` reduce to `body` with `lEnd` pushed.
295:      have hbnd' : here + 4 * csize body < 2 ^ 20 := hbnd
296:      have hlEnd : here + 4 * csize body < 2 ^ 20 := hbnd
297:      have hlbl' : LabelsOk ((here + 4 * csize body) :: brkPos) contPos epiPos := by
303:      -- `csize`/`emitCF`/`MemAccOff`/`maxRegS` on `.block body` are defeq to the
305:      have hem' : Emitted L here (emitCF P.data dpos ((here + 4 * csize body) :: brkPos) contPos epiPos here body)
318:            ih body s s'' ocb m here ((here + 4 * csize body) :: brkPos) contPos
325:              exact ⟨k, hst, by rw [hpck]; simp only [csize, landPos]⟩
332:                  refine ⟨k, hst, by rw [hpck]; simp only [csize, landPos, List.getD_cons_zero]⟩
351:

… [truncated 488 chars]
```

### 🤖 **Assistant**  ·  14:41:53; model 4.5s

> 💭 (thinking)
> 

Notably `csize`/`emitCF` don't show a `call` arm in the grep. Let me look at the full definitions and the lower_sim_cf signature/hypotheses.

**🔧 tool → Read**  ·  18ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean", "offset": 36, "limit": 90}
```

### 🛠️ **Tool result**  ·  14:41:55; tool 18ms

```
36	    layout: `ife` = 2 slot-loads + branch + jmp (4), `while` = +1 back-edge (5).
37	    `cref`/`clen` are `0` here (placeholder — `emit` still stubs them). -/
38	def csize : PStmt → Nat
39	  | .seq a b        => csize a + csize b
40	  | .ret            => 1
41	  | .brkB _         => 1
42	  | .contL _        => 1
43	  | .block body     => csize body
44	  | .ife _ _ _ t e  => 4 + csize e + csize t
45	  | .while _ _ _ b  => 5 + csize b
46	  | .clen rd _      => 3 + (if rd = 0 then 0 else 1)   -- synthConst (3) + storeSlot
47	  | .cref rd _      => 5 + (if rd = 0 then 0 else 1)   -- pc-read + synth + add + store
48	  | s               => (emit s).length
49	
50	/-- The label-aware lowering. `brkPos`/`contPos` are the enclosing block-end /
51	    loop-top label byte positions (indexed like `brkB`/`contL`'s de-Bruijn
52	    indices); `epiPos` the epilogue; `here` the byte offset of this code.
53	    Straight-line cases reuse `emit` (`here` irrelevant). -/
54	def emitCF (dat : Data) (dpos : Name → Nat) (brkPos contPos : List Nat) (epiPos : Nat) :
55	    Nat → PStmt → List Instr
56	  | here, .seq a b =>
57	      emitCF dat dpos brkPos contPos epiPos here a ++
58	        emitCF dat dpos brkPos contPos epiPos (here + 4 * csize a) b
59	  | here, .ret     => [LowIR.jal0 ((epiPos : Int) - (here : Int))]
60	  | here, .brkB k  => [LowIR.jal0 ((brkPos.getD k 0 : Int) - (here : Int))]
61	  | here, .contL k => [LowIR.jal0 ((contPos.getD k 0 : Int) - (here : Int))]
62	  | here, .block body =>
63	      -- the break target `lEnd` sits just past the body (0-byte label).
64	      emitCF dat dpos ((here + 4 * csize body) :: brkPos) contPos epiPos here body
65	  | here, .ife c a b t e =>
66	      -- br→lT (then) skips over the else block; jmp→lEnd skips over then.
67	      -- offsets are size-relative: lT is 8 + 4|e| past the branch, lEnd 4 + 4|t|
68	      -- past the jmp. else code `e` runs on fall-through (at here+12), then `t`.
69	      loadSlotI a T0 ++ loadSlotI b T1
70	        ++ [LowIR.condInstr c T0 T1 (8 + 4 * csize e)]
71	        ++ emitCF dat dpos brkPos contPos epiPos (here + 12) e
72	        ++ [LowIR.jal0 (4 + 4 * csize t)]
73	        ++ emitCF dat dpos brkPos contPos epiPos (here + 16 + 4 * csize e) t
74	  | here, .while c a b body =>
75	      -- lTop = here (0-byte label); br→lBody (+8) enters, jmp→lEnd exits, the
76	      -- body runs a continue-scope (lTop::contPos), then the back-edge to lTop.
77	      loadSlotI a T0 ++ loadSlotI b T1
78	        ++ [LowIR.condInstr c T0 T1 8, LowIR.jal0 (8 + 4 * csize body)]
79	        ++ emitCF dat dpos brkPos (here :: contPos) epiPos (here + 16) body
80	        ++ [LowIR.jal0 (-(16 + 4 * csize body : Int))]
81	  | _here, .clen rd d =>
82	      -- position-independent: synth the data length into T0, park into rd's slot.
83	      synthI T0 (((List.lookup d dat).map (·.length)).getD 0 : Int) ++ storeSlotI rd T0
84	  | here, .cref rd d =>
85	      -- pc-read (`jal T0,+4`) + synth the delta to `d`'s absolute position into T1
86	      -- + `add T0,T0,T1` ⇒ T0 = data address; park into rd's slot.
87	      crefI T0 T1 ((dpos d : Int) - ((here : Int) + 4)) ++ storeSlotI rd T0
88	  | _, s => emit s
89	
90	/-- `emitCF`'s length is exactly `csize` — position/label-independent, as claimed.
91	    Needed by the outcome-carrying `lower_sim` to place `seq`'s second half and to
92	    read the fall-through position. -/
93	theorem emitCF_length (dat : Data) (dpos : Name → Nat) (bp cp : List Nat) (ep here : Nat)
94	    (stmt : PStmt) :
95	    (emitCF dat dpos bp cp ep here stmt).length = csize stmt := by
96	  induction stmt generalizing bp cp ep here with
97	  | seq a b iha ihb =>
98	      simp only [emitCF, csize, List.length_append, iha, ihb]
99	  | ret => rfl
100	  | brkB k => rfl
101	  | contL k => rfl
102	  | block body ih =>
103	      simp only [emitCF, csize]; rw [ih]
104	  | ife c a b t e iht ihe =>
105	      simp only [emitCF, csize, List.length_append, List.length_cons,
106	                 List.length_n

… [truncated 899 chars]
```

### 🤖 **Assistant**  ·  14:42:02; model 8.0s

> 💭 (thinking)
> 

Let me look at the real compiler's call lowering, prologue, and epilogue, plus what `emit` currently does for `.call`.

**🔧 tool → Bash**  ·  82ms
```json
{"command": "grep -n \"call\\|prologue\\|epilogue\\|withParams\\|marshal\\|def lower\\b\" lean/LowIR/Compile.lean | head -50", "description": "Find call lowering in Compile.lean"}
```

### 🛠️ **Tool result**  ·  14:42:03; tool 82ms

```
9:  Machine frame (grows down; all offsets sp-relative, sp = x2 after prologue):
18:  argument/return marshalling (so argc,rvc ≤ 8, checked). Nothing is live
19:  across an IL statement except the slots, so calls need no caller-save logic.
21:  IL zero-init is matched EXACTLY: the prologue `sd x0` into every non-param,
25:  Pipeline: lower to `SymInstr` (labels symbolic, calls by name) → lay out the
26:  whole program (all instructions are 4 bytes) → resolve branches/jumps/calls
61:  | callf (f : Name)                           -- jal ra, function
97:    `epi` is this function's epilogue label. `wf` guarantees index ranges and
98:    call arities; out-of-range lookups fall back to label 0 (never emitted for
100:def lower (dat : Data) (brks conts : List Nat) (epi : Nat) : PStmt → M (List SymInstr)
136:  | .call _ _ f args rets => do
139:      pure <| loads ++ [.callf f] ++ stores
164:  | .call _ _ _ args rets =>
181:/-- Per-function compilability limits for this first cut: a0..a7 marshalling
189:def prologue (fd : FunDef) : List SymInstr :=
203:def epilogue (fd : FunDef) : List SymInstr :=
213:  pure <| prologue fd ++ body ++ [.label epi] ++ epilogue fd
277:  | (pos, .callf f)      => do
315:      layout (("", [.callf entry, .ins (jal0 0)]) :: segs) 0
```

### 🤖 **Assistant**  ·  14:42:04; model 822ms

**🔧 tool → Bash**  ·  76ms
```json
{"command": "grep -n \"call\\|def emit\\b\" lean/LowIR/ProgSim/StmtSim.lean | head -20", "description": "Find emit call arm in StmtSim"}
```

### 🛠️ **Tool result**  ·  14:42:04; tool 76ms

```
59:def emit : PStmt → List Instr
624:    the caller supplies `hC` (the one-instruction step lemma specialised to `C`),
878:    ife/while/block, brk/cont/ret, call, cref/clen) is the domain of the
```

### 🤖 **Assistant**  ·  14:42:07; model 2.4s

**🔧 tool → Read**  ·  12ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/Compile.lean", "offset": 120, "limit": 100}
```

### 🛠️ **Tool result**  ·  14:42:07; tool 12ms

```
120	      let ee ← lower dat brks conts epi e
121	      pure <| loadSlot a T0 ++ loadSlot b T1
122	           ++ [.br c T0 T1 lT] ++ ee ++ [.jmp lEnd, .label lT] ++ et ++ [.label lEnd]
123	  | .while c a b body => do
124	      let lTop ← fresh; let lBody ← fresh; let lEnd ← fresh
125	      let eb ← lower dat brks (lTop :: conts) epi body
126	      pure <| [.label lTop] ++ loadSlot a T0 ++ loadSlot b T1
127	           ++ [.br c T0 T1 lBody, .jmp lEnd, .label lBody]
128	           ++ eb ++ [.jmp lTop, .label lEnd]
129	  | .block body     => do
130	      let lEnd ← fresh
131	      let eb ← lower dat (lEnd :: brks) conts epi body
132	      pure <| eb ++ [.label lEnd]
133	  | .brkB k         => pure [.jmp (brks.getD k 0)]
134	  | .contL k        => pure [.jmp (conts.getD k 0)]
135	  | .ret            => pure [.jmp epi]
136	  | .call _ _ f args rets => do
137	      let loads  := args.toList.zipIdx.flatMap fun ri => loadSlot ri.1 (A ri.2)
138	      let stores := rets.toList.zipIdx.flatMap fun ri => storeSlot ri.1 (A ri.2)
139	      pure <| loads ++ [.callf f] ++ stores
140	  | .cref rd d      => pure <| [.cref d] ++ storeSlot rd T0
141	  | .clen rd d      =>
142	      pure <| synthConst T0 (((List.lookup d dat).map (·.length)).getD 0)
143	           ++ storeSlot rd T0
144	
145	/-! ### Frame accounting -/
146	
147	/-- Largest IL register mentioned in a statement. -/
148	def maxRegS : PStmt → Nat
149	  | .skip | .annot _ | .ret | .brkB _ | .contL _ => 0
150	  | .seq a b          => max (maxRegS a) (maxRegS b)
151	  | .addi rd rs _     => max rd rs
152	  | .add  rd r1 r2    => max rd (max r1 r2)
153	  | .sub  rd r1 r2    => max rd (max r1 r2)
154	  | .orr  rd r1 r2    => max rd (max r1 r2)
155	  | .slli rd rs _     => max rd rs
156	  | .srli rd rs _     => max rd rs
157	  | .lbu  rd rs _     => max rd rs
158	  | .ld   rd rs _     => max rd rs
159	  | .sb   rb rv _     => max rb rv
160	  | .sd   rb rv _     => max rb rv
161	  | .ife _ a b t e    => max (max a b) (max (maxRegS t) (maxRegS e))
162	  | .while _ a b body => max (max a b) (maxRegS body)
163	  | .block body       => maxRegS body
164	  | .call _ _ _ args rets =>
165	      max (args.toList.foldl max 0) (rets.toList.foldl max 0)
166	  | .cref rd _        => rd
167	  | .clen rd _        => rd
168	
169	/-- Largest IL register a function touches (body + declared regs + frameReg). -/
170	def maxRegF (fd : FunDef) : Nat :=
171	  max (maxRegS fd.body) <|
172	  max fd.frameReg <|
173	  max (fd.params.toList.foldl max 0) (fd.rets.toList.foldl max 0)
174	
175	/-- Byte offset of the user frame within the machine frame. -/
176	def userOff (fd : FunDef) : Nat := 8 * (maxRegF fd + 2)
177	
178	/-- Total machine frame size. -/
179	def totalFrame (fd : FunDef) : Nat := userOff fd + fd.frameSize
180	
181	/-- Per-function compilability limits for this first cut: a0..a7 marshalling
182	    and all slot offsets / frame adjustments within imm12. -/
183	def fnOk (fd : FunDef) : Bool :=
184	  fd.argc ≤ 8 && fd.rvc ≤ 8 && totalFrame fd ≤ 2000
185	
186	/-- Prologue: drop sp, save ra, park params from a0.., `sd x0` every other
187	    slot (matches IL zero-init EXACTLY), materialize the user-frame base into
188	    frameReg's slot. -/
189	def prologue (fd : FunDef) : List SymInstr :=
190	  let tf := totalFrame fd
191	  let params := fd.params.toList
192	  [.ins (.addi SP SP (BitVec.ofInt 12 (-(tf : Int)))),
193	   .ins (.sd SP RA 0)]
194	  ++ params.zipIdx.flatMap (fun pi => storeSlot pi.1 (A pi.2))
195	  ++ ((List.range (maxRegF fd + 1)).filter
196	        (fun r => r != 0 && !params.contains r && r != fd.frameReg)).map
197	       (fun r => .ins (.sd SP 0 (BitVec.ofNat 12 (slotOff r))))
198	  ++ (if fd.frameReg = 0 then [] else
199	        [.ins (.addi T0 SP (BitVec.ofNat 12 (userOff fd)))] ++ storeSlot fd.frameReg T0)
200	
201	/-- Epilogue (jumped to by `ret`, fallen into on `normal`): rets → a0..,
202	    restore ra + sp, return. -/
203	def epilogue (fd : FunDef) : List SymInstr :=
204	  fd.rets.toList

… [truncated 678 chars]
```

### 🤖 **Assistant**  ·  14:42:09; model 4.4s

> 💭 (thinking)
> 

Now the IL `exec` semantics for call. Let me read the exec_call unfolder and the underlying Prog.exec call equation.

**🔧 tool → Read**  ·  19ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/ExecFacts.lean", "offset": 205, "limit": 30}
```

### 🛠️ **Tool result**  ·  14:42:11; tool 19ms

```
205	/-! ### `call` — the D7 activation. The successful case (callee falls off the
206	    end or `ret`s) returns to the caller with only `rets` copied back and the
207	    callee's memory kept. -/
208	
209	theorem exec_call_ok (fuel argc rvc : Nat) (f : Name) (args : Vector Reg argc)
210	    (rets : Vector Reg rvc) (s s1 callee : St) (fd : FunDef) (o : Outcome)
211	    (hlk : List.lookup f P.env = some fd)
212	    (harity : (fd.argc == argc && fd.rvc == rvc) = true)
213	    (hfe : frameEnter stackLo fd (pad f) (args.toList.map s.rget) s.mem s.sp = some callee)
214	    (hbody : exec P dbase pad stackLo fuel fd.body callee = some (s1, o))
215	    (ho : o = .normal ∨ o = .ret) :
216	    exec P dbase pad stackLo (fuel+1) (.call argc rvc f args rets) s
217	      = some ((rets.toList.zip (fd.rets.toList.map s1.rget)).foldl
218	                (fun st rv => st.rset rv.1 rv.2) { s with mem := s1.mem }, .normal) := by
219	  rcases ho with rfl | rfl <;> simp [exec, hlk, harity, hfe, hbody]
220	
221	theorem exec_call_lookup_none (fuel argc rvc : Nat) (f : Name) (args : Vector Reg argc)
222	    (rets : Vector Reg rvc) (s : St) (hlk : List.lookup f P.env = none) :
223	    exec P dbase pad stackLo (fuel+1) (.call argc rvc f args rets) s = none := by simp [exec, hlk]
224	
225	end LowIR.Prog
226	
```

### 🤖 **Assistant**  ·  14:42:33; model 24.7s

> 💭 (thinking)
> 

This is the summit (Phase 5). The structural crux: the `call` case needs the *callee's* body to simulate, which the current per-statement IH doesn't provide. Let me read the full `lower_sim_cf` signature and its induction setup to understand what IH is available.

**🔧 tool → Read**  ·  23ms
```json
{"file_path": "/var/data/bootstrap/lean/LowIR/ProgSim/CtrlSim.lean", "offset": 208, "limit": 90}
```

### 🛠️ **Tool result**  ·  14:42:36; tool 23ms

```
208	
209	/-- A conditional branch `condInstr c T0 T1 δ` whose registers hold `x`, `y`: when
210	    `evalCond c x y` holds the branch is taken (`pc += δ`), matching the compiler's
211	    positive-form encoding (`.ge`/`.geu` take the *else* arm of `bge`/`bgeu`). -/
212	theorem cond_taken (m : State) (c : Cond) (δ : Int) (x y : Word)
213	    (hd : decode (fetch32 m) = condInstr c T0 T1 δ)
214	    (hx : m.rget T0 = x) (hy : m.rget T1 = y) (hev : evalCond c x y = true) :
215	    step m = m.setPc (m.pc + (BitVec.ofInt 13 δ).signExtend 64) := by
216	  cases c <;> simp only [condInstr] at hd
217	  · have h : x = y := by simpa [evalCond] using hev
218	    rw [step_beq m T0 T1 _ hd, hx, hy, if_pos h]
219	  · have h : x.slt y = true := by simpa [evalCond] using hev
220	    rw [step_blt m T0 T1 _ hd, hx, hy, if_pos h]
221	  · have h : x.slt y = false := by simpa [evalCond] using hev
222	    rw [step_bge m T0 T1 _ hd, hx, hy, if_neg (by simp [h])]
223	  · have h : x.ult y = false := by simpa [evalCond] using hev
224	    rw [step_bgeu m T0 T1 _ hd, hx, hy, if_neg (by simp [h])]
225	
226	/-- …and when `evalCond c x y` fails the branch falls through (`pc += 4`). -/
227	theorem cond_not_taken (m : State) (c : Cond) (δ : Int) (x y : Word)
228	    (hd : decode (fetch32 m) = condInstr c T0 T1 δ)
229	    (hx : m.rget T0 = x) (hy : m.rget T1 = y) (hev : evalCond c x y = false) :
230	    step m = m.setPc (m.pc + 4) := by
231	  cases c <;> simp only [condInstr] at hd
232	  · have h : ¬ x = y := by simpa [evalCond] using hev
233	    rw [step_beq m T0 T1 _ hd, hx, hy, if_neg h]
234	  · have h : x.slt y = false := by simpa [evalCond] using hev
235	    rw [step_blt m T0 T1 _ hd, hx, hy, if_neg (by simp [h])]
236	  · have h : x.slt y = true := by simpa [evalCond] using hev
237	    rw [step_bge m T0 T1 _ hd, hx, hy, if_pos h]
238	  · have h : x.ult y = true := by simpa [evalCond] using hev
239	    rw [step_bgeu m T0 T1 _ hd, hx, hy, if_pos h]
240	
241	/-- A `getD` into an all-bounded list is bounded (the default `0` is too). Used to
242	    keep `brkB`/`contL`'s jump target inside the offset window. -/
243	theorem getD_lt (l : List Nat) (k b : Nat) (hb : 0 < b) (h : ∀ p ∈ l, p < b) :
244	    l.getD k 0 < b := by
245	  induction l generalizing k with
246	  | nil => simpa using hb
247	  | cons x t ih =>
248	    cases k with
249	    | zero => simpa using h x (by simp)
250	    | succ k => simpa using ih k (fun p hp => h p (by simp [hp]))
251	
252	theorem lower_sim_cf
253	    {P : Program} {dbase : Name → Option Word} {pad : Name → Nat} {stackLo : Word}
254	    {L : Layout} {fd : FunDef} {holes : List Hole} {epiPos : Nat} {dpos : Name → Nat}
255	    (fuel : Nat) (stmt : PStmt) (s s' : St) (oc : Outcome) (m : State)
256	    (here : Nat) (brkPos contPos : List Nat)
257	    (hexec : LowIR.Prog.exec P dbase pad stackLo fuel stmt s = some (s', oc))
258	    (hinv  : StInv L fd holes s m)
259	    (hpc   : m.pc = L.codeBase + BitVec.ofNat 64 here)
260	    (hem   : Emitted L here (emitCF P.data dpos brkPos contPos epiPos here stmt))
261	    (hreg  : maxRegS stmt ≤ maxRegF fd)
262	    (hnw   : s.sp.toNat + userOff fd ≤ 2 ^ 64)
263	    (hbd   : L.codeBase.toNat + L.blobLen ≤ s.sp.toNat
264	               ∨ s.sp.toNat + userOff fd ≤ L.codeBase.toNat)
265	    (haccess : MemAccOff L holes P dbase pad stackLo fuel stmt s)
266	    (hlbl  : LabelsOk brkPos contPos epiPos)
267	    (hbnd  : here + 4 * csize stmt < 2 ^ 20)
268	    (hbr   : BranchOk stmt)
269	    (hframe : userOff fd ≤ 2000)
270	    (hseg  : 4 * L.instrs.length ≤ L.segStart)
271	    (hblob : L.codeBase.toNat + L.blobLen ≤ 2 ^ 64)
272	    (hdat  : ∀ d, -2048 ≤ synthHi (((List.lookup d P.data).map (·.length)).getD 0)
273	                ∧ synthHi (((List.lookup d P.data).map (·.length)).getD 0) ≤ 2047)
274	    (hdbase : ∀ d a, dbase d = some a → a = L.codeBase + BitVec.ofNat 64 (dpos d))
275	    (hdpos : ∀ d, dpos d < 2 ^ 20) :
276	    ∃ k, StInv L fd holes s' (stepN k m)
277	       ∧ (stepN k m).pc = L.codeBase
27

… [truncated 1226 chars]
```

### 👤 **User**  ·  14:43:14; waited 38.3s

[Request interrupted by user]
