# Project instructions

## Documentation

- All project documentation lives in `docs/` (specs, proof plans, status, handoffs, analyses). Write any new doc into `docs/`, not the repo root.
- `docs/README.md` is the documentation index. When you add a new doc, add a link line for it in the appropriate section of `docs/README.md`.
